{
  "info": {
    "name": "Redfish Discovery - Supermicro X14DBI",
    "description": "Generated from a versioned Redfish Inventory. Mutation requests are disabled by default.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "AccountService",
      "item": [
        {
          "name": "Accounts",
          "item": [
            {
              "name": "2",
              "item": [
                {
                  "name": "GET User Account",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/AccountService/Accounts/2",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                },
                {
                  "name": "PATCH User Account",
                  "request": {
                    "method": "PATCH",
                    "header": [
                      {
                        "key": "Content-Type",
                        "value": "application/json",
                        "type": "text"
                      },
                      {
                        "key": "If-Match",
                        "value": "\"4a99afdf\"",
                        "type": "text"
                      }
                    ],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/AccountService/Accounts/2",
                    "description": "Evidence: allow-header",
                    "body": {
                      "mode": "raw",
                      "raw": "{}",
                      "options": {
                        "raw": {
                          "language": "json"
                        }
                      }
                    }
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                          "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                          "    pm.execution.skipRequest();",
                          "  } else {",
                          "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                          "  }",
                          "}",
                          "",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/AccountService/Accounts/2\nType: #ManagerAccount.v1_14_0.ManagerAccount"
            },
            {
              "name": "GET Accounts Collection",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/AccountService/Accounts",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            },
            {
              "name": "POST Create Account",
              "request": {
                "method": "POST",
                "header": [
                  {
                    "key": "Content-Type",
                    "value": "application/json",
                    "type": "text"
                  }
                ],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/AccountService/Accounts",
                "description": "Evidence: allow-header\nCreate schema: https://redfish.dmtf.org/schemas/v1/ManagerAccount.v1_10_0.json. Review BMC support and roles.",
                "body": {
                  "mode": "raw",
                  "raw": "{\"UserName\":\"{{ACCOUNT_USERNAME}}\",\"Password\":\"{{ACCOUNT_PASSWORD}}\",\"RoleId\":\"{{ACCOUNT_ROLE_ID}}\",\"Enabled\":true,\"AccountTypes\":[\"Redfish\"]}",
                  "options": {
                    "raw": {
                      "language": "json"
                    }
                  }
                }
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                      "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                      "    pm.execution.skipRequest();",
                      "  } else {",
                      "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                      "  }",
                      "}",
                      "",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}",
                      "",
                      "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() === 'false') {",
                      "  const username = pm.environment.get('ACCOUNT_USERNAME');",
                      "  const password = pm.environment.get('ACCOUNT_PASSWORD');",
                      "  const role = pm.environment.get('ACCOUNT_ROLE_ID');",
                      "  try {",
                      "    if (!username || !password || !role) throw new Error('Set ACCOUNT_USERNAME, ACCOUNT_PASSWORD, ACCOUNT_ROLE_ID locally');",
                      "    const enabled = JSON.parse(pm.environment.get('ACCOUNT_ENABLED') || 'true');",
                      "    const types = JSON.parse(pm.environment.get('ACCOUNT_TYPES') || '[\"Redfish\"]');",
                      "    if (typeof enabled !== 'boolean' || !Array.isArray(types) || !types.every(x => typeof x === 'string')) throw new Error('Invalid account parameters');",
                      "    pm.request.body.update(JSON.stringify({UserName: username, Password: password, RoleId: role, Enabled: enabled, AccountTypes: types}));",
                      "  } catch (error) {",
                      "    pm.execution.skipRequest();",
                      "    throw error;",
                      "  }",
                      "}"
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/AccountService/Accounts\nType: #ManagerAccountCollection.ManagerAccountCollection"
        },
        {
          "name": "Roles",
          "item": [
            {
              "name": "Administrator",
              "item": [
                {
                  "name": "GET User Role",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/AccountService/Roles/Administrator",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/AccountService/Roles/Administrator\nType: #Role.v1_3_2.Role"
            },
            {
              "name": "NoAccess",
              "item": [
                {
                  "name": "GET User Role",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/AccountService/Roles/NoAccess",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/AccountService/Roles/NoAccess\nType: #Role.v1_3_2.Role"
            },
            {
              "name": "Operator",
              "item": [
                {
                  "name": "GET User Role",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/AccountService/Roles/Operator",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/AccountService/Roles/Operator\nType: #Role.v1_3_2.Role"
            },
            {
              "name": "ReadOnly",
              "item": [
                {
                  "name": "GET User Role",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/AccountService/Roles/ReadOnly",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/AccountService/Roles/ReadOnly\nType: #Role.v1_3_2.Role"
            },
            {
              "name": "GET Roles Collection",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/AccountService/Roles",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/AccountService/Roles\nType: #RoleCollection.RoleCollection"
        },
        {
          "name": "GET Account Service",
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/AccountService",
            "description": "Evidence: allow-header"
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const urlTools = require('url');",
                  "function parseURL(raw) {",
                  "  const value = urlTools.parse(raw);",
                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                  "  return value;",
                  "}",
                  "function targetBase() {",
                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                  "}",
                  "function authorizeSession(raw, baseURL) {",
                  "  const base = targetBase();",
                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                  "  let expanded = raw;",
                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                  "  const decoded = decodeURIComponent(session.pathname || '');",
                  "  if (session.origin !== base.origin || session.auth ||",
                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                  "  return session;",
                  "}",
                  "",
                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                  "pm.request.headers.remove('Authorization');",
                  "pm.request.headers.remove('X-Auth-Token');",
                  "if (mode === 'basic') {",
                  "  const username = pm.environment.get('USERNAME') || '';",
                  "  const password = pm.environment.get('PASSWORD') || '';",
                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                  "} else {",
                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                  "    pm.execution.skipRequest();",
                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                  "  }",
                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Response status is below 400', function () {",
                  "  pm.expect(pm.response.code).to.be.below(400);",
                  "});",
                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                  "if (pm.response.code !== 204) {",
                  "  pm.test('Response is JSON', function () {",
                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                  "  });",
                  "}",
                  ""
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "PATCH Account Service",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "If-Match",
                "value": "\"a257725d\"",
                "type": "text"
              }
            ],
            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/AccountService",
            "description": "Evidence: allow-header",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const urlTools = require('url');",
                  "function parseURL(raw) {",
                  "  const value = urlTools.parse(raw);",
                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                  "  return value;",
                  "}",
                  "function targetBase() {",
                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                  "}",
                  "function authorizeSession(raw, baseURL) {",
                  "  const base = targetBase();",
                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                  "  let expanded = raw;",
                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                  "  const decoded = decodeURIComponent(session.pathname || '');",
                  "  if (session.origin !== base.origin || session.auth ||",
                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                  "  return session;",
                  "}",
                  "",
                  "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                  "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                  "    pm.execution.skipRequest();",
                  "  } else {",
                  "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                  "  }",
                  "}",
                  "",
                  "",
                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                  "pm.request.headers.remove('Authorization');",
                  "pm.request.headers.remove('X-Auth-Token');",
                  "if (mode === 'basic') {",
                  "  const username = pm.environment.get('USERNAME') || '';",
                  "  const password = pm.environment.get('PASSWORD') || '';",
                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                  "} else {",
                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                  "    pm.execution.skipRequest();",
                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                  "  }",
                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                  "}"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        }
      ],
      "description": "URI: /redfish/v1/AccountService\nType: #AccountService.v1_18_0.AccountService"
    },
    {
      "name": "Authentication",
      "item": [
        {
          "name": "Login (Session)",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const urlTools = require('url');",
                  "function parseURL(raw) {",
                  "  const value = urlTools.parse(raw);",
                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                  "  return value;",
                  "}",
                  "function targetBase() {",
                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                  "}",
                  "function authorizeSession(raw, baseURL) {",
                  "  const base = targetBase();",
                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                  "  let expanded = raw;",
                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                  "  const decoded = decodeURIComponent(session.pathname || '');",
                  "  if (session.origin !== base.origin || session.auth ||",
                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                  "  return session;",
                  "}",
                  "",
                  "pm.environment.unset('X_AUTH_TOKEN');",
                  "pm.environment.unset('SESSION_URI');",
                  "pm.environment.unset('SESSION_ORIGIN');",
                  "const token = pm.response.headers.get('X-Auth-Token');",
                  "const location = pm.response.headers.get('Location');",
                  "pm.test('Session was created', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf([200, 201]);",
                  "  pm.expect(token).to.be.a('string').and.not.empty;",
                  "  pm.expect(location).to.be.a('string').and.not.empty;",
                  "});",
                  "if ([200, 201].includes(pm.response.code) && token && location) {",
                  "  const session = authorizeSession(location, pm.variables.replaceIn(pm.request.url.toString()));",
                  "  pm.environment.set('X_AUTH_TOKEN', token);",
                  "  pm.environment.set('SESSION_URI', session.pathname);",
                  "  pm.environment.set('SESSION_ORIGIN', session.origin);",
                  "}"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"UserName\": \"{{USERNAME}}\",\n  \"Password\": \"{{PASSWORD}}\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/SessionService/Sessions",
            "description": "Authentication lifecycle request; not a resource mutation."
          },
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "Logout (Session)",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const urlTools = require('url');",
                  "function parseURL(raw) {",
                  "  const value = urlTools.parse(raw);",
                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                  "  return value;",
                  "}",
                  "function targetBase() {",
                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                  "}",
                  "function authorizeSession(raw, baseURL) {",
                  "  const base = targetBase();",
                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                  "  let expanded = raw;",
                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                  "  const decoded = decodeURIComponent(session.pathname || '');",
                  "  if (session.origin !== base.origin || session.auth ||",
                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                  "  return session;",
                  "}",
                  "",
                  "try {",
                  "  if (pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) throw new Error('Session belongs to a different BMC; log in again');",
                  "  const session = authorizeSession(pm.environment.get('SESSION_URI') || '');",
                  "  if (!pm.environment.get('SESSION_URI') || !pm.environment.get('X_AUTH_TOKEN')) throw new Error('No active session');",
                  "  pm.request.url = session.href;",
                  "} catch (error) {",
                  "  pm.execution.skipRequest();",
                  "  throw error;",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Session was deleted', function () {",
                  "  pm.expect(pm.response.code).to.be.oneOf([200, 202, 204]);",
                  "});",
                  "if (pm.response.code < 300) {",
                  "  pm.environment.unset('X_AUTH_TOKEN');",
                  "  pm.environment.unset('SESSION_URI');",
                  "  pm.environment.unset('SESSION_ORIGIN');",
                  "}",
                  ""
                ]
              }
            }
          ],
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Auth-Token",
                "value": "{{X_AUTH_TOKEN}}"
              }
            ],
            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}{{SESSION_URI}}",
            "description": "Authentication lifecycle request; not a resource mutation."
          },
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        }
      ]
    },
    {
      "name": "CertificateService",
      "item": [
        {
          "name": "Actions",
          "item": [
            {
              "name": "POST CertificateService.GenerateCSR",
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                      "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                      "    pm.execution.skipRequest();",
                      "  } else {",
                      "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                      "  }",
                      "}",
                      "",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                }
              ],
              "request": {
                "method": "POST",
                "header": [
                  {
                    "key": "Content-Type",
                    "value": "application/json",
                    "type": "text"
                  }
                ],
                "body": {
                  "mode": "raw",
                  "raw": "{}",
                  "options": {
                    "raw": {
                      "language": "json"
                    }
                  }
                },
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/CertificateService/Actions/CertificateService.GenerateCSR",
                "description": "Redfish Action. Review the payload before explicitly enabling mutations."
              },
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            },
            {
              "name": "POST CertificateService.ReplaceCertificate",
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                      "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                      "    pm.execution.skipRequest();",
                      "  } else {",
                      "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                      "  }",
                      "}",
                      "",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                }
              ],
              "request": {
                "method": "POST",
                "header": [
                  {
                    "key": "Content-Type",
                    "value": "application/json",
                    "type": "text"
                  }
                ],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"CertificateType\": \"\"\n}",
                  "options": {
                    "raw": {
                      "language": "json"
                    }
                  }
                },
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/CertificateService/Actions/CertificateService.ReplaceCertificate",
                "description": "Redfish Action. Review the payload before explicitly enabling mutations."
              },
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ]
        },
        {
          "name": "CertificateLocations",
          "item": [
            {
              "name": "GET Certificate Locations",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/CertificateService/CertificateLocations",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/CertificateService/CertificateLocations\nType: #CertificateLocations.v1_0_4.CertificateLocations"
        },
        {
          "name": "GET Certificate Service",
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/CertificateService",
            "description": "Evidence: allow-header"
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const urlTools = require('url');",
                  "function parseURL(raw) {",
                  "  const value = urlTools.parse(raw);",
                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                  "  return value;",
                  "}",
                  "function targetBase() {",
                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                  "}",
                  "function authorizeSession(raw, baseURL) {",
                  "  const base = targetBase();",
                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                  "  let expanded = raw;",
                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                  "  const decoded = decodeURIComponent(session.pathname || '');",
                  "  if (session.origin !== base.origin || session.auth ||",
                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                  "  return session;",
                  "}",
                  "",
                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                  "pm.request.headers.remove('Authorization');",
                  "pm.request.headers.remove('X-Auth-Token');",
                  "if (mode === 'basic') {",
                  "  const username = pm.environment.get('USERNAME') || '';",
                  "  const password = pm.environment.get('PASSWORD') || '';",
                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                  "} else {",
                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                  "    pm.execution.skipRequest();",
                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                  "  }",
                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Response status is below 400', function () {",
                  "  pm.expect(pm.response.code).to.be.below(400);",
                  "});",
                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                  "if (pm.response.code !== 204) {",
                  "  pm.test('Response is JSON', function () {",
                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                  "  });",
                  "}",
                  ""
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        }
      ],
      "description": "URI: /redfish/v1/CertificateService\nType: #CertificateService.v1_1_0.CertificateService"
    },
    {
      "name": "Chassis",
      "item": [
        {
          "name": "1",
          "item": [
            {
              "name": "Actions",
              "item": [
                {
                  "name": "POST Chassis.Reset",
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                          "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                          "    pm.execution.skipRequest();",
                          "  } else {",
                          "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                          "  }",
                          "}",
                          "",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    }
                  ],
                  "request": {
                    "method": "POST",
                    "header": [
                      {
                        "key": "Content-Type",
                        "value": "application/json",
                        "type": "text"
                      }
                    ],
                    "body": {
                      "mode": "raw",
                      "raw": "{\n  \"ResetType\": \"<REQUIRED>\"\n}",
                      "options": {
                        "raw": {
                          "language": "json"
                        }
                      }
                    },
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Actions/Chassis.Reset",
                    "description": "Redfish Action. Review the payload before explicitly enabling mutations."
                  },
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                },
                {
                  "name": "POST Chassis.Reset",
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                          "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                          "    pm.execution.skipRequest();",
                          "  } else {",
                          "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                          "  }",
                          "}",
                          "",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    }
                  ],
                  "request": {
                    "method": "POST",
                    "header": [
                      {
                        "key": "Content-Type",
                        "value": "application/json",
                        "type": "text"
                      }
                    ],
                    "body": {
                      "mode": "raw",
                      "raw": "{\n  \"ResetType\": \"<REQUIRED>\"\n}",
                      "options": {
                        "raw": {
                          "language": "json"
                        }
                      }
                    },
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Actions/Chassis.Reset",
                    "description": "Redfish Action. Review the payload before explicitly enabling mutations."
                  },
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ]
            },
            {
              "name": "Assembly",
              "item": [
                {
                  "name": "GET Assembly data for 1",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Assembly",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/Chassis/1/Assembly\nType: #Assembly.v1_5_1.Assembly"
            },
            {
              "name": "Controls",
              "item": [
                {
                  "name": "CPU_PowerLimit",
                  "item": [
                    {
                      "name": "GET CPU PowerLimit",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Controls/CPU_PowerLimit",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH CPU PowerLimit",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"a44b4a42\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Controls/CPU_PowerLimit",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Controls/CPU_PowerLimit\nType: #Control.v1_7_0.Control"
                },
                {
                  "name": "GET Control Collection",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Controls",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/Chassis/1/Controls\nType: #ControlCollection.ControlCollection"
            },
            {
              "name": "EnvironmentMetrics",
              "item": [
                {
                  "name": "GET Environment Metrics",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/EnvironmentMetrics",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/Chassis/1/EnvironmentMetrics\nType: #EnvironmentMetrics.v1_5_0.EnvironmentMetrics"
            },
            {
              "name": "NetworkAdapters",
              "item": [
                {
                  "name": "GET Network Adapter Collection",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/NetworkAdapters",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/Chassis/1/NetworkAdapters\nType: #NetworkAdapterCollection.NetworkAdapterCollection"
            },
            {
              "name": "PCIeDevices",
              "item": [
                {
                  "name": "GET PCIe Device Collection",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/PCIeDevices",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/Chassis/1/PCIeDevices\nType: #PCIeDeviceCollection.PCIeDeviceCollection"
            },
            {
              "name": "PCIeSlots",
              "item": [
                {
                  "name": "GET PCIe Slot Information",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/PCIeSlots",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/Chassis/1/PCIeSlots\nType: #PCIeSlots.v1_6_1.PCIeSlots"
            },
            {
              "name": "PowerSubsystem",
              "item": [
                {
                  "name": "PowerSupplies",
                  "item": [
                    {
                      "name": "GET PowerSupplyCollection",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/PowerSubsystem/PowerSupplies",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/PowerSubsystem/PowerSupplies\nType: #PowerSupplyCollection.PowerSupplyCollection"
                },
                {
                  "name": "GET Power Subsystem for Chassis",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/PowerSubsystem",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/Chassis/1/PowerSubsystem\nType: #PowerSubsystem.v1_1_2.PowerSubsystem"
            },
            {
              "name": "Sensors",
              "item": [
                {
                  "name": "BMC1.0V",
                  "item": [
                    {
                      "name": "GET BMC 1.0V",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/BMC1.0V",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH BMC 1.0V",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"c8da6e4e\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/BMC1.0V",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/BMC1.0V\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "BMC1.2V",
                  "item": [
                    {
                      "name": "GET BMC 1.2V",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/BMC1.2V",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH BMC 1.2V",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"927b005e\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/BMC1.2V",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/BMC1.2V\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "BMC1.8V",
                  "item": [
                    {
                      "name": "GET BMC 1.8V",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/BMC1.8V",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH BMC 1.8V",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"369aa5fa\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/BMC1.8V",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/BMC1.8V\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "BMC2.5V",
                  "item": [
                    {
                      "name": "GET BMC 2.5V",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/BMC2.5V",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH BMC 2.5V",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"a8cae7c4\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/BMC2.5V",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/BMC2.5V\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "CPU1Temp",
                  "item": [
                    {
                      "name": "GET CPU1 Temp",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/CPU1Temp",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH CPU1 Temp",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"d5814200\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/CPU1Temp",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/CPU1Temp\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "CPU1_VCCHV0",
                  "item": [
                    {
                      "name": "GET CPU1_VCCHV0",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/CPU1_VCCHV0",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH CPU1_VCCHV0",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"f36d060d\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/CPU1_VCCHV0",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/CPU1_VCCHV0\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "CPU1_VCCIN",
                  "item": [
                    {
                      "name": "GET CPU1_VCCIN",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/CPU1_VCCIN",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH CPU1_VCCIN",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"be9b2f60\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/CPU1_VCCIN",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/CPU1_VCCIN\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "CPU1_VCCON",
                  "item": [
                    {
                      "name": "GET CPU1_VCCON",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/CPU1_VCCON",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH CPU1_VCCON",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"c5b046b6\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/CPU1_VCCON",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/CPU1_VCCON\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "CPU1_VRMHV0Temp",
                  "item": [
                    {
                      "name": "GET CPU1_VRMHV0 Temp",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/CPU1_VRMHV0Temp",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH CPU1_VRMHV0 Temp",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"485cccf7\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/CPU1_VRMHV0Temp",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/CPU1_VRMHV0Temp\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "CPU1_VRMINTemp",
                  "item": [
                    {
                      "name": "GET CPU1_VRMIN Temp",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/CPU1_VRMINTemp",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH CPU1_VRMIN Temp",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"bc9fe5e3\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/CPU1_VRMINTemp",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/CPU1_VRMINTemp\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "CPU1_VRMONTemp",
                  "item": [
                    {
                      "name": "GET CPU1_VRMON Temp",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/CPU1_VRMONTemp",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH CPU1_VRMON Temp",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"699accd5\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/CPU1_VRMONTemp",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/CPU1_VRMONTemp\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "CPU2Temp",
                  "item": [
                    {
                      "name": "GET CPU2 Temp",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/CPU2Temp",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH CPU2 Temp",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"2e60c2da\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/CPU2Temp",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/CPU2Temp\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "CPU2_VCCHV0",
                  "item": [
                    {
                      "name": "GET CPU2_VCCHV0",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/CPU2_VCCHV0",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH CPU2_VCCHV0",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"e9163563\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/CPU2_VCCHV0",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/CPU2_VCCHV0\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "CPU2_VCCIN",
                  "item": [
                    {
                      "name": "GET CPU2_VCCIN",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/CPU2_VCCIN",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH CPU2_VCCIN",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"3b1330e8\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/CPU2_VCCIN",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/CPU2_VCCIN\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "CPU2_VCCON",
                  "item": [
                    {
                      "name": "GET CPU2_VCCON",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/CPU2_VCCON",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH CPU2_VCCON",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"6faf532e\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/CPU2_VCCON",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/CPU2_VCCON\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "CPU2_VRMHV0Temp",
                  "item": [
                    {
                      "name": "GET CPU2_VRMHV0 Temp",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/CPU2_VRMHV0Temp",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH CPU2_VRMHV0 Temp",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"5eb39cc5\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/CPU2_VRMHV0Temp",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/CPU2_VRMHV0Temp\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "CPU2_VRMINTemp",
                  "item": [
                    {
                      "name": "GET CPU2_VRMIN Temp",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/CPU2_VRMINTemp",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH CPU2_VRMIN Temp",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"00bc33a6\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/CPU2_VRMINTemp",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/CPU2_VRMINTemp\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "CPU2_VRMONTemp",
                  "item": [
                    {
                      "name": "GET CPU2_VRMON Temp",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/CPU2_VRMONTemp",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH CPU2_VRMON Temp",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"b76050c9\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/CPU2_VRMONTemp",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/CPU2_VRMONTemp\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "ChassisIntru",
                  "item": [
                    {
                      "name": "GET Chassis Intru",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/ChassisIntru",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH Chassis Intru",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"1cd1681f\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/ChassisIntru",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/ChassisIntru\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "FAN1",
                  "item": [
                    {
                      "name": "GET FAN1",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/FAN1",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH FAN1",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"a0b9e11b\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/FAN1",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/FAN1\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "FAN2",
                  "item": [
                    {
                      "name": "GET FAN2",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/FAN2",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH FAN2",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"58cd8553\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/FAN2",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/FAN2\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "FAN3",
                  "item": [
                    {
                      "name": "GET FAN3",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/FAN3",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH FAN3",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"68f47660\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/FAN3",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/FAN3\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "FAN4",
                  "item": [
                    {
                      "name": "GET FAN4",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/FAN4",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH FAN4",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"3aa29810\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/FAN4",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/FAN4\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "FAN5",
                  "item": [
                    {
                      "name": "GET FAN5",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/FAN5",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH FAN5",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"5abd4b27\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/FAN5",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/FAN5\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "FAN6",
                  "item": [
                    {
                      "name": "GET FAN6",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/FAN6",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH FAN6",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"79d8532b\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/FAN6",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/FAN6\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "FANA",
                  "item": [
                    {
                      "name": "GET FANA",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/FANA",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH FANA",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"a7032bec\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/FANA",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/FANA\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "FANB",
                  "item": [
                    {
                      "name": "GET FANB",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/FANB",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH FANB",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"74267256\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/FANB",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/FANB\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "InletTemp",
                  "item": [
                    {
                      "name": "GET Inlet Temp",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/InletTemp",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH Inlet Temp",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"3c85fc19\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/InletTemp",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/InletTemp\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "M2_SSD1Temp",
                  "item": [
                    {
                      "name": "GET M2_SSD1 Temp",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/M2_SSD1Temp",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH M2_SSD1 Temp",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"34531edc\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/M2_SSD1Temp",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/M2_SSD1Temp\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "M2_SSD2Temp",
                  "item": [
                    {
                      "name": "GET M2_SSD2 Temp",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/M2_SSD2Temp",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH M2_SSD2 Temp",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"2ebbe066\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/M2_SSD2Temp",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/M2_SSD2Temp\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "MB12V",
                  "item": [
                    {
                      "name": "GET MB 12V",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/MB12V",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH MB 12V",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"17ac15ac\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/MB12V",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/MB12V\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "MB3.3VCC",
                  "item": [
                    {
                      "name": "GET MB 3.3VCC",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/MB3.3VCC",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH MB 3.3VCC",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"0c371460\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/MB3.3VCC",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/MB3.3VCC\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "MB3.3VSB",
                  "item": [
                    {
                      "name": "GET MB 3.3VSB",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/MB3.3VSB",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH MB 3.3VSB",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"bce996d0\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/MB3.3VSB",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/MB3.3VSB\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "MB5VCC",
                  "item": [
                    {
                      "name": "GET MB 5VCC",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/MB5VCC",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH MB 5VCC",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"9d698919\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/MB5VCC",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/MB5VCC\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "MB5VSB",
                  "item": [
                    {
                      "name": "GET MB 5VSB",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/MB5VSB",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH MB 5VSB",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"b8e30022\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/MB5VSB",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/MB5VSB\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "NICTemp",
                  "item": [
                    {
                      "name": "GET NIC Temp",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/NICTemp",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH NIC Temp",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"064ccb36\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/NICTemp",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/NICTemp\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "P1_DIMMA~DTemp",
                  "item": [
                    {
                      "name": "GET P1_DIMMA~D Temp",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/P1_DIMMA~DTemp",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH P1_DIMMA~D Temp",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"57041cf2\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/P1_DIMMA~DTemp",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/P1_DIMMA~DTemp\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "P1_DIMME~HTemp",
                  "item": [
                    {
                      "name": "GET P1_DIMME~H Temp",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/P1_DIMME~HTemp",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH P1_DIMME~H Temp",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"bd38e0a4\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/P1_DIMME~HTemp",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/P1_DIMME~HTemp\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "P2_DIMMA~DTemp",
                  "item": [
                    {
                      "name": "GET P2_DIMMA~D Temp",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/P2_DIMMA~DTemp",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH P2_DIMMA~D Temp",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"713bd22f\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/P2_DIMMA~DTemp",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/P2_DIMMA~DTemp\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "P2_DIMME~HTemp",
                  "item": [
                    {
                      "name": "GET P2_DIMME~H Temp",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/P2_DIMME~HTemp",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH P2_DIMME~H Temp",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"61d7283f\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/P2_DIMME~HTemp",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/P2_DIMME~HTemp\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "PeripheralTemp",
                  "item": [
                    {
                      "name": "GET Peripheral Temp",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/PeripheralTemp",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH Peripheral Temp",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"dc97de8f\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/PeripheralTemp",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/PeripheralTemp\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "SystemTemp",
                  "item": [
                    {
                      "name": "GET System Temp",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/SystemTemp",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH System Temp",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"c47347b9\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/SystemTemp",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/SystemTemp\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "VBAT",
                  "item": [
                    {
                      "name": "GET VBAT",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/VBAT",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH VBAT",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"3fa6a68d\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors/VBAT",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/Sensors/VBAT\nType: #Sensor.v1_11_0.Sensor"
                },
                {
                  "name": "GET Sensor Collection",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/Sensors",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/Chassis/1/Sensors\nType: #SensorCollection.SensorCollection"
            },
            {
              "name": "ThermalSubsystem",
              "item": [
                {
                  "name": "Fans",
                  "item": [
                    {
                      "name": "FAN1",
                      "item": [
                        {
                          "name": "GET FAN1",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/ThermalSubsystem/Fans/FAN1",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Chassis/1/ThermalSubsystem/Fans/FAN1\nType: #Fan.v1_5_2.Fan"
                    },
                    {
                      "name": "FAN2",
                      "item": [
                        {
                          "name": "GET FAN2",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/ThermalSubsystem/Fans/FAN2",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Chassis/1/ThermalSubsystem/Fans/FAN2\nType: #Fan.v1_5_2.Fan"
                    },
                    {
                      "name": "FAN3",
                      "item": [
                        {
                          "name": "GET FAN3",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/ThermalSubsystem/Fans/FAN3",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Chassis/1/ThermalSubsystem/Fans/FAN3\nType: #Fan.v1_5_2.Fan"
                    },
                    {
                      "name": "FAN4",
                      "item": [
                        {
                          "name": "GET FAN4",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/ThermalSubsystem/Fans/FAN4",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Chassis/1/ThermalSubsystem/Fans/FAN4\nType: #Fan.v1_5_2.Fan"
                    },
                    {
                      "name": "FAN5",
                      "item": [
                        {
                          "name": "GET FAN5",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/ThermalSubsystem/Fans/FAN5",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Chassis/1/ThermalSubsystem/Fans/FAN5\nType: #Fan.v1_5_2.Fan"
                    },
                    {
                      "name": "FAN6",
                      "item": [
                        {
                          "name": "GET FAN6",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/ThermalSubsystem/Fans/FAN6",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Chassis/1/ThermalSubsystem/Fans/FAN6\nType: #Fan.v1_5_2.Fan"
                    },
                    {
                      "name": "FANA",
                      "item": [
                        {
                          "name": "GET FANA",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/ThermalSubsystem/Fans/FANA",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Chassis/1/ThermalSubsystem/Fans/FANA\nType: #Fan.v1_5_2.Fan"
                    },
                    {
                      "name": "FANB",
                      "item": [
                        {
                          "name": "GET FANB",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/ThermalSubsystem/Fans/FANB",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Chassis/1/ThermalSubsystem/Fans/FANB\nType: #Fan.v1_5_2.Fan"
                    },
                    {
                      "name": "GET Fan Collection",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/ThermalSubsystem/Fans",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/ThermalSubsystem/Fans\nType: #FanCollection.FanCollection"
                },
                {
                  "name": "ThermalMetrics",
                  "item": [
                    {
                      "name": "GET ThermalMetrics",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/ThermalSubsystem/ThermalMetrics",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Chassis/1/ThermalSubsystem/ThermalMetrics\nType: #ThermalMetrics.v1_3_2.ThermalMetrics"
                },
                {
                  "name": "GET Thermal Subsystem for Chassis",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/ThermalSubsystem",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/Chassis/1/ThermalSubsystem\nType: #ThermalSubsystem.v1_4_0.ThermalSubsystem"
            },
            {
              "name": "GET 1",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            },
            {
              "name": "GET 1",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            },
            {
              "name": "PATCH 1",
              "request": {
                "method": "PATCH",
                "header": [
                  {
                    "key": "Content-Type",
                    "value": "application/json",
                    "type": "text"
                  },
                  {
                    "key": "If-Match",
                    "value": "\"dd0a6056\"",
                    "type": "text"
                  }
                ],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1",
                "description": "Evidence: allow-header",
                "body": {
                  "mode": "raw",
                  "raw": "{}",
                  "options": {
                    "raw": {
                      "language": "json"
                    }
                  }
                }
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                      "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                      "    pm.execution.skipRequest();",
                      "  } else {",
                      "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                      "  }",
                      "}",
                      "",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            },
            {
              "name": "PATCH 1",
              "request": {
                "method": "PATCH",
                "header": [
                  {
                    "key": "Content-Type",
                    "value": "application/json",
                    "type": "text"
                  },
                  {
                    "key": "If-Match",
                    "value": "\"dd0a6056\"",
                    "type": "text"
                  }
                ],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis/1/",
                "description": "Evidence: allow-header",
                "body": {
                  "mode": "raw",
                  "raw": "{}",
                  "options": {
                    "raw": {
                      "language": "json"
                    }
                  }
                }
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                      "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                      "    pm.execution.skipRequest();",
                      "  } else {",
                      "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                      "  }",
                      "}",
                      "",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/Chassis/1/\nType: #Chassis.v1_27_0.Chassis"
        },
        {
          "name": "GET Chassis Collection",
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Chassis",
            "description": "Evidence: allow-header"
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const urlTools = require('url');",
                  "function parseURL(raw) {",
                  "  const value = urlTools.parse(raw);",
                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                  "  return value;",
                  "}",
                  "function targetBase() {",
                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                  "}",
                  "function authorizeSession(raw, baseURL) {",
                  "  const base = targetBase();",
                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                  "  let expanded = raw;",
                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                  "  const decoded = decodeURIComponent(session.pathname || '');",
                  "  if (session.origin !== base.origin || session.auth ||",
                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                  "  return session;",
                  "}",
                  "",
                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                  "pm.request.headers.remove('Authorization');",
                  "pm.request.headers.remove('X-Auth-Token');",
                  "if (mode === 'basic') {",
                  "  const username = pm.environment.get('USERNAME') || '';",
                  "  const password = pm.environment.get('PASSWORD') || '';",
                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                  "} else {",
                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                  "    pm.execution.skipRequest();",
                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                  "  }",
                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Response status is below 400', function () {",
                  "  pm.expect(pm.response.code).to.be.below(400);",
                  "});",
                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                  "if (pm.response.code !== 204) {",
                  "  pm.test('Response is JSON', function () {",
                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                  "  });",
                  "}",
                  ""
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        }
      ],
      "description": "URI: /redfish/v1/Chassis\nType: #ChassisCollection.ChassisCollection"
    },
    {
      "name": "ComponentIntegrity",
      "item": [
        {
          "name": "GET ComponentIntegrity Collection",
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/ComponentIntegrity",
            "description": "Evidence: allow-header"
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const urlTools = require('url');",
                  "function parseURL(raw) {",
                  "  const value = urlTools.parse(raw);",
                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                  "  return value;",
                  "}",
                  "function targetBase() {",
                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                  "}",
                  "function authorizeSession(raw, baseURL) {",
                  "  const base = targetBase();",
                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                  "  let expanded = raw;",
                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                  "  const decoded = decodeURIComponent(session.pathname || '');",
                  "  if (session.origin !== base.origin || session.auth ||",
                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                  "  return session;",
                  "}",
                  "",
                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                  "pm.request.headers.remove('Authorization');",
                  "pm.request.headers.remove('X-Auth-Token');",
                  "if (mode === 'basic') {",
                  "  const username = pm.environment.get('USERNAME') || '';",
                  "  const password = pm.environment.get('PASSWORD') || '';",
                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                  "} else {",
                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                  "    pm.execution.skipRequest();",
                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                  "  }",
                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Response status is below 400', function () {",
                  "  pm.expect(pm.response.code).to.be.below(400);",
                  "});",
                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                  "if (pm.response.code !== 204) {",
                  "  pm.test('Response is JSON', function () {",
                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                  "  });",
                  "}",
                  ""
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        }
      ],
      "description": "URI: /redfish/v1/ComponentIntegrity\nType: #ComponentIntegrityCollection.ComponentIntegrityCollection"
    },
    {
      "name": "EventService",
      "item": [
        {
          "name": "Actions",
          "item": [
            {
              "name": "POST EventService.SubmitTestEvent",
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                      "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                      "    pm.execution.skipRequest();",
                      "  } else {",
                      "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                      "  }",
                      "}",
                      "",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                }
              ],
              "request": {
                "method": "POST",
                "header": [
                  {
                    "key": "Content-Type",
                    "value": "application/json",
                    "type": "text"
                  }
                ],
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"EventId\": \"\",\n  \"EventTimestamp\": \"\",\n  \"MessageSeverity\": \"\",\n  \"Severity\": \"\",\n  \"Message\": \"\",\n  \"MessageId\": \"<REQUIRED>\",\n  \"MessageArgs\": \"\",\n  \"OriginOfCondition\": \"\"\n}",
                  "options": {
                    "raw": {
                      "language": "json"
                    }
                  }
                },
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/EventService/Actions/EventService.SubmitTestEvent",
                "description": "Redfish Action. Review the payload before explicitly enabling mutations."
              },
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ]
        },
        {
          "name": "Subscriptions",
          "item": [
            {
              "name": "GET Event Destination Collections",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/EventService/Subscriptions",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/EventService/Subscriptions\nType: #EventDestinationCollection.EventDestinationCollection"
        },
        {
          "name": "GET Event Service",
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/EventService",
            "description": "Evidence: allow-header"
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const urlTools = require('url');",
                  "function parseURL(raw) {",
                  "  const value = urlTools.parse(raw);",
                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                  "  return value;",
                  "}",
                  "function targetBase() {",
                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                  "}",
                  "function authorizeSession(raw, baseURL) {",
                  "  const base = targetBase();",
                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                  "  let expanded = raw;",
                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                  "  const decoded = decodeURIComponent(session.pathname || '');",
                  "  if (session.origin !== base.origin || session.auth ||",
                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                  "  return session;",
                  "}",
                  "",
                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                  "pm.request.headers.remove('Authorization');",
                  "pm.request.headers.remove('X-Auth-Token');",
                  "if (mode === 'basic') {",
                  "  const username = pm.environment.get('USERNAME') || '';",
                  "  const password = pm.environment.get('PASSWORD') || '';",
                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                  "} else {",
                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                  "    pm.execution.skipRequest();",
                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                  "  }",
                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Response status is below 400', function () {",
                  "  pm.expect(pm.response.code).to.be.below(400);",
                  "});",
                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                  "if (pm.response.code !== 204) {",
                  "  pm.test('Response is JSON', function () {",
                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                  "  });",
                  "}",
                  ""
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "PATCH Event Service",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "If-Match",
                "value": "\"fbdf49df\"",
                "type": "text"
              }
            ],
            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/EventService",
            "description": "Evidence: allow-header",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const urlTools = require('url');",
                  "function parseURL(raw) {",
                  "  const value = urlTools.parse(raw);",
                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                  "  return value;",
                  "}",
                  "function targetBase() {",
                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                  "}",
                  "function authorizeSession(raw, baseURL) {",
                  "  const base = targetBase();",
                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                  "  let expanded = raw;",
                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                  "  const decoded = decodeURIComponent(session.pathname || '');",
                  "  if (session.origin !== base.origin || session.auth ||",
                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                  "  return session;",
                  "}",
                  "",
                  "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                  "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                  "    pm.execution.skipRequest();",
                  "  } else {",
                  "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                  "  }",
                  "}",
                  "",
                  "",
                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                  "pm.request.headers.remove('Authorization');",
                  "pm.request.headers.remove('X-Auth-Token');",
                  "if (mode === 'basic') {",
                  "  const username = pm.environment.get('USERNAME') || '';",
                  "  const password = pm.environment.get('PASSWORD') || '';",
                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                  "} else {",
                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                  "    pm.execution.skipRequest();",
                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                  "  }",
                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                  "}"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        }
      ],
      "description": "URI: /redfish/v1/EventService\nType: #EventService.v1_11_0.EventService"
    },
    {
      "name": "Fabrics",
      "item": [
        {
          "name": "GET Fabric Collection",
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Fabrics",
            "description": "Evidence: allow-header"
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const urlTools = require('url');",
                  "function parseURL(raw) {",
                  "  const value = urlTools.parse(raw);",
                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                  "  return value;",
                  "}",
                  "function targetBase() {",
                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                  "}",
                  "function authorizeSession(raw, baseURL) {",
                  "  const base = targetBase();",
                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                  "  let expanded = raw;",
                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                  "  const decoded = decodeURIComponent(session.pathname || '');",
                  "  if (session.origin !== base.origin || session.auth ||",
                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                  "  return session;",
                  "}",
                  "",
                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                  "pm.request.headers.remove('Authorization');",
                  "pm.request.headers.remove('X-Auth-Token');",
                  "if (mode === 'basic') {",
                  "  const username = pm.environment.get('USERNAME') || '';",
                  "  const password = pm.environment.get('PASSWORD') || '';",
                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                  "} else {",
                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                  "    pm.execution.skipRequest();",
                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                  "  }",
                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Response status is below 400', function () {",
                  "  pm.expect(pm.response.code).to.be.below(400);",
                  "});",
                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                  "if (pm.response.code !== 204) {",
                  "  pm.test('Response is JSON', function () {",
                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                  "  });",
                  "}",
                  ""
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        }
      ],
      "description": "URI: /redfish/v1/Fabrics\nType: #FabricCollection.FabricCollection"
    },
    {
      "name": "JsonSchemas",
      "item": [
        {
          "name": "AccountService",
          "item": [
            {
              "name": "GET AccountService Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/AccountService",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/AccountService\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "ActionInfo",
          "item": [
            {
              "name": "GET ActionInfo Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/ActionInfo",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/ActionInfo\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "ActivateLicense",
          "item": [
            {
              "name": "GET ActivateLicense Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/ActivateLicense",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/ActivateLicense\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "AggregationService",
          "item": [
            {
              "name": "GET AggregationService Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/AggregationService",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/AggregationService\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "AggregationSource",
          "item": [
            {
              "name": "GET AggregationSource Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/AggregationSource",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/AggregationSource\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "AggregationSourceCollection",
          "item": [
            {
              "name": "GET AggregationSourceCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/AggregationSourceCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/AggregationSourceCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "Assembly",
          "item": [
            {
              "name": "GET Assembly Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/Assembly",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/Assembly\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "AttributeRegistry",
          "item": [
            {
              "name": "GET AttributeRegistry Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/AttributeRegistry",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/AttributeRegistry\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "Bios",
          "item": [
            {
              "name": "GET Bios Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/Bios",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/Bios\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "Cable",
          "item": [
            {
              "name": "GET Cable Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/Cable",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/Cable\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "CableCollection",
          "item": [
            {
              "name": "GET CableCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/CableCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/CableCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "Certificate",
          "item": [
            {
              "name": "GET Certificate Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/Certificate",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/Certificate\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "CertificateCollection",
          "item": [
            {
              "name": "GET CertificateCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/CertificateCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/CertificateCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "CertificateLocations",
          "item": [
            {
              "name": "GET CertificateLocations Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/CertificateLocations",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/CertificateLocations\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "CertificateService",
          "item": [
            {
              "name": "GET CertificateService Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/CertificateService",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/CertificateService\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "Chassis",
          "item": [
            {
              "name": "GET Chassis Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/Chassis",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/Chassis\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "ChassisCollection",
          "item": [
            {
              "name": "GET ChassisCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/ChassisCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/ChassisCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "ClearLicense",
          "item": [
            {
              "name": "GET ClearLicense Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/ClearLicense",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/ClearLicense\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "ComponentIntegrity",
          "item": [
            {
              "name": "GET ComponentIntegrity Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/ComponentIntegrity",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/ComponentIntegrity\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "ComponentIntegrityCollection",
          "item": [
            {
              "name": "GET ComponentIntegrityCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/ComponentIntegrityCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/ComponentIntegrityCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "ComputerSystem",
          "item": [
            {
              "name": "GET ComputerSystem Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/ComputerSystem",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/ComputerSystem\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "ComputerSystemCollection",
          "item": [
            {
              "name": "GET ComputerSystemCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/ComputerSystemCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/ComputerSystemCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "DeviceUpdate",
          "item": [
            {
              "name": "GET DeviceUpdate Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/DeviceUpdate",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/DeviceUpdate\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "Drive",
          "item": [
            {
              "name": "GET Drive Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/Drive",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/Drive\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "DriveCollection",
          "item": [
            {
              "name": "GET DriveCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/DriveCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/DriveCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "Dump",
          "item": [
            {
              "name": "GET Dump Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/Dump",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/Dump\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "DumpCollection",
          "item": [
            {
              "name": "GET DumpCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/DumpCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/DumpCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "DumpService",
          "item": [
            {
              "name": "GET DumpService Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/DumpService",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/DumpService\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "EnvironmentMetrics",
          "item": [
            {
              "name": "GET EnvironmentMetrics Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/EnvironmentMetrics",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/EnvironmentMetrics\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "EthernetInterface",
          "item": [
            {
              "name": "GET EthernetInterface Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/EthernetInterface",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/EthernetInterface\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "EthernetInterfaceCollection",
          "item": [
            {
              "name": "GET EthernetInterfaceCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/EthernetInterfaceCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/EthernetInterfaceCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "Event",
          "item": [
            {
              "name": "GET Event Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/Event",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/Event\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "EventDestination",
          "item": [
            {
              "name": "GET EventDestination Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/EventDestination",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/EventDestination\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "EventDestinationCollection",
          "item": [
            {
              "name": "GET EventDestinationCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/EventDestinationCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/EventDestinationCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "EventService",
          "item": [
            {
              "name": "GET EventService Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/EventService",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/EventService\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "FabricAdapter",
          "item": [
            {
              "name": "GET FabricAdapter Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/FabricAdapter",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/FabricAdapter\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "FabricAdapterCollection",
          "item": [
            {
              "name": "GET FabricAdapterCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/FabricAdapterCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/FabricAdapterCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "Fan",
          "item": [
            {
              "name": "GET Fan Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/Fan",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/Fan\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "FanCollection",
          "item": [
            {
              "name": "GET FanCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/FanCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/FanCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "FanMode",
          "item": [
            {
              "name": "GET FanMode Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/FanMode",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/FanMode\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "FilterRule",
          "item": [
            {
              "name": "GET FilterRule Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/FilterRule",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/FilterRule\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "FilterRuleCollection",
          "item": [
            {
              "name": "GET FilterRuleCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/FilterRuleCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/FilterRuleCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "GPUDevice",
          "item": [
            {
              "name": "GET GPUDevice Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/GPUDevice",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/GPUDevice\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "HARAIDController",
          "item": [
            {
              "name": "GET HARAIDController Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/HARAIDController",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/HARAIDController\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "IKVM",
          "item": [
            {
              "name": "GET IKVM Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/IKVM",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/IKVM\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "IPAccessControl",
          "item": [
            {
              "name": "GET IPAccessControl Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/IPAccessControl",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/IPAccessControl\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "IPAddresses",
          "item": [
            {
              "name": "GET IPAddresses Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/IPAddresses",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/IPAddresses\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "IPMIConfig",
          "item": [
            {
              "name": "GET IPMIConfig Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/IPMIConfig",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/IPMIConfig\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "JsonSchemaFile",
          "item": [
            {
              "name": "GET JsonSchemaFile Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/JsonSchemaFile",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/JsonSchemaFile\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "JsonSchemaFileCollection",
          "item": [
            {
              "name": "GET JsonSchemaFileCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/JsonSchemaFileCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/JsonSchemaFileCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "KCSInterface",
          "item": [
            {
              "name": "GET KCSInterface Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/KCSInterface",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/KCSInterface\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "LicenseManager",
          "item": [
            {
              "name": "GET LicenseManager Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/LicenseManager",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/LicenseManager\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "LldpService",
          "item": [
            {
              "name": "GET LldpService Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/LldpService",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/LldpService\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "LogEntry",
          "item": [
            {
              "name": "GET LogEntry Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/LogEntry",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/LogEntry\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "LogEntryCollection",
          "item": [
            {
              "name": "GET LogEntryCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/LogEntryCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/LogEntryCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "LogService",
          "item": [
            {
              "name": "GET LogService Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/LogService",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/LogService\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "LogServiceCollection",
          "item": [
            {
              "name": "GET LogServiceCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/LogServiceCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/LogServiceCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "Manager",
          "item": [
            {
              "name": "GET Manager Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/Manager",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/Manager\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "ManagerAccount",
          "item": [
            {
              "name": "GET ManagerAccount Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/ManagerAccount",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/ManagerAccount\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "ManagerAccountCollection",
          "item": [
            {
              "name": "GET ManagerAccountCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/ManagerAccountCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/ManagerAccountCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "ManagerCollection",
          "item": [
            {
              "name": "GET ManagerCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/ManagerCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/ManagerCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "ManagerConfig",
          "item": [
            {
              "name": "GET ManagerConfig Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/ManagerConfig",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/ManagerConfig\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "ManagerDiagnosticData",
          "item": [
            {
              "name": "GET ManagerDiagnosticData Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/ManagerDiagnosticData",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/ManagerDiagnosticData\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "ManagerNetworkProtocol",
          "item": [
            {
              "name": "GET ManagerNetworkProtocol Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/ManagerNetworkProtocol",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/ManagerNetworkProtocol\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "Memory",
          "item": [
            {
              "name": "GET Memory Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/Memory",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/Memory\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "MemoryCollection",
          "item": [
            {
              "name": "GET MemoryCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/MemoryCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/MemoryCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "MemoryHealthComp",
          "item": [
            {
              "name": "GET MemoryHealthComp Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/MemoryHealthComp",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/MemoryHealthComp\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "MemoryPFA",
          "item": [
            {
              "name": "GET MemoryPFA Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/MemoryPFA",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/MemoryPFA\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "Message",
          "item": [
            {
              "name": "GET Message Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/Message",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/Message\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "MessageRegistry",
          "item": [
            {
              "name": "GET MessageRegistry Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/MessageRegistry",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/MessageRegistry\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "MessageRegistryCollection",
          "item": [
            {
              "name": "GET MessageRegistryCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/MessageRegistryCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/MessageRegistryCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "MessageRegistryFile",
          "item": [
            {
              "name": "GET MessageRegistryFile Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/MessageRegistryFile",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/MessageRegistryFile\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "MessageRegistryFileCollection",
          "item": [
            {
              "name": "GET MessageRegistryFileCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/MessageRegistryFileCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/MessageRegistryFileCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "MetricDefinition",
          "item": [
            {
              "name": "GET MetricDefinition Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/MetricDefinition",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/MetricDefinition\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "MetricDefinitionCollection",
          "item": [
            {
              "name": "GET MetricDefinitionCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/MetricDefinitionCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/MetricDefinitionCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "MetricReport",
          "item": [
            {
              "name": "GET MetricReport Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/MetricReport",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/MetricReport\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "MetricReportCollection",
          "item": [
            {
              "name": "GET MetricReportCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/MetricReportCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/MetricReportCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "MetricReportDefinition",
          "item": [
            {
              "name": "GET MetricReportDefinition Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/MetricReportDefinition",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/MetricReportDefinition\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "MetricReportDefinitionCollection",
          "item": [
            {
              "name": "GET MetricReportDefinitionCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/MetricReportDefinitionCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/MetricReportDefinitionCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "MouseMode",
          "item": [
            {
              "name": "GET MouseMode Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/MouseMode",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/MouseMode\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "NTP",
          "item": [
            {
              "name": "GET NTP Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/NTP",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/NTP\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "OperatingConfig",
          "item": [
            {
              "name": "GET OperatingConfig Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/OperatingConfig",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/OperatingConfig\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "OperatingConfigCollection",
          "item": [
            {
              "name": "GET OperatingConfigCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/OperatingConfigCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/OperatingConfigCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "PCIeDevice",
          "item": [
            {
              "name": "GET PCIeDevice Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/PCIeDevice",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/PCIeDevice\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "PCIeDeviceCollection",
          "item": [
            {
              "name": "GET PCIeDeviceCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/PCIeDeviceCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/PCIeDeviceCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "PCIeFunction",
          "item": [
            {
              "name": "GET PCIeFunction Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/PCIeFunction",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/PCIeFunction\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "PCIeFunctionCollection",
          "item": [
            {
              "name": "GET PCIeFunctionCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/PCIeFunctionCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/PCIeFunctionCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "PCIeSlots",
          "item": [
            {
              "name": "GET PCIeSlots Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/PCIeSlots",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/PCIeSlots\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "Peci",
          "item": [
            {
              "name": "GET Peci Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/Peci",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/Peci\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "PhysicalContext",
          "item": [
            {
              "name": "GET PhysicalContext Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/PhysicalContext",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/PhysicalContext\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "Port",
          "item": [
            {
              "name": "GET Port Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/Port",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/Port\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "PortCollection",
          "item": [
            {
              "name": "GET PortCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/PortCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/PortCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "Power",
          "item": [
            {
              "name": "GET Power Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/Power",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/Power\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "PowerSubsystem",
          "item": [
            {
              "name": "GET PowerSubsystem Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/PowerSubsystem",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/PowerSubsystem\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "PowerSupply",
          "item": [
            {
              "name": "GET PowerSupply Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/PowerSupply",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/PowerSupply\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "PowerSupplyCollection",
          "item": [
            {
              "name": "GET PowerSupplyCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/PowerSupplyCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/PowerSupplyCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "Privileges",
          "item": [
            {
              "name": "GET Privileges Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/Privileges",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/Privileges\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "Processor",
          "item": [
            {
              "name": "GET Processor Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/Processor",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/Processor\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "ProcessorCollection",
          "item": [
            {
              "name": "GET ProcessorCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/ProcessorCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/ProcessorCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "Protocol",
          "item": [
            {
              "name": "GET Protocol Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/Protocol",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/Protocol\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "QueryLicense",
          "item": [
            {
              "name": "GET QueryLicense Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/QueryLicense",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/QueryLicense\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "RADIUS",
          "item": [
            {
              "name": "GET RADIUS Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/RADIUS",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/RADIUS\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "Redundancy",
          "item": [
            {
              "name": "GET Redundancy Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/Redundancy",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/Redundancy\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "Resource",
          "item": [
            {
              "name": "GET Resource Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/Resource",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/Resource\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "Role",
          "item": [
            {
              "name": "GET Role Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/Role",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/Role\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "RoleCollection",
          "item": [
            {
              "name": "GET RoleCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/RoleCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/RoleCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SMCRAKP",
          "item": [
            {
              "name": "GET SMCRAKP Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SMCRAKP",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SMCRAKP\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SSLCert",
          "item": [
            {
              "name": "GET SSLCert Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SSLCert",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SSLCert\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "Sensor",
          "item": [
            {
              "name": "GET Sensor Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/Sensor",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/Sensor\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SensorCollection",
          "item": [
            {
              "name": "GET SensorCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SensorCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SensorCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "ServiceRoot",
          "item": [
            {
              "name": "GET ServiceRoot Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/ServiceRoot",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/ServiceRoot\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "Session",
          "item": [
            {
              "name": "GET Session Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/Session",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/Session\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SessionCollection",
          "item": [
            {
              "name": "GET SessionCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SessionCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SessionCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SessionService",
          "item": [
            {
              "name": "GET SessionService Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SessionService",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SessionService\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "Settings",
          "item": [
            {
              "name": "GET Settings Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/Settings",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/Settings\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SmartPower",
          "item": [
            {
              "name": "GET SmartPower Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SmartPower",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SmartPower\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SmcAccountServiceExtensions",
          "item": [
            {
              "name": "GET SmcAccountServiceExtensions Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SmcAccountServiceExtensions",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SmcAccountServiceExtensions\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SmcChassisExtensions",
          "item": [
            {
              "name": "GET SmcChassisExtensions Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SmcChassisExtensions",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SmcChassisExtensions\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SmcChassisNVMESSDExtensions",
          "item": [
            {
              "name": "GET SmcChassisNVMESSDExtensions Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SmcChassisNVMESSDExtensions",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SmcChassisNVMESSDExtensions\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SmcDriveExtensions",
          "item": [
            {
              "name": "GET SmcDriveExtensions Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SmcDriveExtensions",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SmcDriveExtensions\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SmcEthernetInterfaceExtensions",
          "item": [
            {
              "name": "GET SmcEthernetInterfaceExtensions Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SmcEthernetInterfaceExtensions",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SmcEthernetInterfaceExtensions\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SmcEventDestinationExtensions",
          "item": [
            {
              "name": "GET SmcEventDestinationExtensions Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SmcEventDestinationExtensions",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SmcEventDestinationExtensions\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SmcFanExtensions",
          "item": [
            {
              "name": "GET SmcFanExtensions Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SmcFanExtensions",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SmcFanExtensions\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SmcFirmwareInventory",
          "item": [
            {
              "name": "GET SmcFirmwareInventory Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SmcFirmwareInventory",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SmcFirmwareInventory\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SmcFirmwareInventoryCollection",
          "item": [
            {
              "name": "GET SmcFirmwareInventoryCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SmcFirmwareInventoryCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SmcFirmwareInventoryCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SmcFixedBootOrder",
          "item": [
            {
              "name": "GET SmcFixedBootOrder Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SmcFixedBootOrder",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SmcFixedBootOrder\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SmcGPUExtensions",
          "item": [
            {
              "name": "GET SmcGPUExtensions Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SmcGPUExtensions",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SmcGPUExtensions\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SmcHARAIDController",
          "item": [
            {
              "name": "GET SmcHARAIDController Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SmcHARAIDController",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SmcHARAIDController\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SmcIPMIConfig",
          "item": [
            {
              "name": "GET SmcIPMIConfig Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SmcIPMIConfig",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SmcIPMIConfig\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SmcLogEntryExtensions",
          "item": [
            {
              "name": "GET SmcLogEntryExtensions Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SmcLogEntryExtensions",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SmcLogEntryExtensions\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SmcLogService",
          "item": [
            {
              "name": "GET SmcLogService Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SmcLogService",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SmcLogService\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SmcLogServiceExtensions",
          "item": [
            {
              "name": "GET SmcLogServiceExtensions Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SmcLogServiceExtensions",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SmcLogServiceExtensions\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SmcManagerExtensions",
          "item": [
            {
              "name": "GET SmcManagerExtensions Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SmcManagerExtensions",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SmcManagerExtensions\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SmcMemoryExtensions",
          "item": [
            {
              "name": "GET SmcMemoryExtensions Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SmcMemoryExtensions",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SmcMemoryExtensions\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SmcMultiNodeCollection",
          "item": [
            {
              "name": "GET SmcMultiNodeCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SmcMultiNodeCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SmcMultiNodeCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SmcNode",
          "item": [
            {
              "name": "GET SmcNode Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SmcNode",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SmcNode\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SmcNodeManager",
          "item": [
            {
              "name": "GET SmcNodeManager Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SmcNodeManager",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SmcNodeManager\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SmcPowerExtensions",
          "item": [
            {
              "name": "GET SmcPowerExtensions Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SmcPowerExtensions",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SmcPowerExtensions\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SmcProcessorsExtensions",
          "item": [
            {
              "name": "GET SmcProcessorsExtensions Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SmcProcessorsExtensions",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SmcProcessorsExtensions\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SmcSSLCert",
          "item": [
            {
              "name": "GET SmcSSLCert Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SmcSSLCert",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SmcSSLCert\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SmcSensorExtensions",
          "item": [
            {
              "name": "GET SmcSensorExtensions Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SmcSensorExtensions",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SmcSensorExtensions\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SmcServiceRootExtensions",
          "item": [
            {
              "name": "GET SmcServiceRootExtensions Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SmcServiceRootExtensions",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SmcServiceRootExtensions\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SmcSoftwareInventoryExtensions",
          "item": [
            {
              "name": "GET SmcSoftwareInventoryExtensions Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SmcSoftwareInventoryExtensions",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SmcSoftwareInventoryExtensions\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SmcStorageExtensions",
          "item": [
            {
              "name": "GET SmcStorageExtensions Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SmcStorageExtensions",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SmcStorageExtensions\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SmcSystemExtensions",
          "item": [
            {
              "name": "GET SmcSystemExtensions Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SmcSystemExtensions",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SmcSystemExtensions\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SmcTemperatureExtensions",
          "item": [
            {
              "name": "GET SmcTemperatureExtensions Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SmcTemperatureExtensions",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SmcTemperatureExtensions\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SmcUpdateServiceExtensions",
          "item": [
            {
              "name": "GET SmcUpdateServiceExtensions Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SmcUpdateServiceExtensions",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SmcUpdateServiceExtensions\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SmcVolumeExtensions",
          "item": [
            {
              "name": "GET SmcVolumeExtensions Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SmcVolumeExtensions",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SmcVolumeExtensions\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "Snooping",
          "item": [
            {
              "name": "GET Snooping Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/Snooping",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/Snooping\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SoftwareInventory",
          "item": [
            {
              "name": "GET SoftwareInventory Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SoftwareInventory",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SoftwareInventory\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SoftwareInventoryCollection",
          "item": [
            {
              "name": "GET SoftwareInventoryCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SoftwareInventoryCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SoftwareInventoryCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "Storage",
          "item": [
            {
              "name": "GET Storage Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/Storage",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/Storage\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "StorageCollection",
          "item": [
            {
              "name": "GET StorageCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/StorageCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/StorageCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "StorageController",
          "item": [
            {
              "name": "GET StorageController Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/StorageController",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/StorageController\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "StorageControllerCollection",
          "item": [
            {
              "name": "GET StorageControllerCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/StorageControllerCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/StorageControllerCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "SysLockdown",
          "item": [
            {
              "name": "GET SysLockdown Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/SysLockdown",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/SysLockdown\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "Syslog",
          "item": [
            {
              "name": "GET Syslog Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/Syslog",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/Syslog\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "Task",
          "item": [
            {
              "name": "GET Task Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/Task",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/Task\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "TaskCollection",
          "item": [
            {
              "name": "GET TaskCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/TaskCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/TaskCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "TaskService",
          "item": [
            {
              "name": "GET TaskService Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/TaskService",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/TaskService\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "TelemetryService",
          "item": [
            {
              "name": "GET TelemetryService Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/TelemetryService",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/TelemetryService\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "Thermal",
          "item": [
            {
              "name": "GET Thermal Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/Thermal",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/Thermal\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "ThermalMetrics",
          "item": [
            {
              "name": "GET ThermalMetrics Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/ThermalMetrics",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/ThermalMetrics\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "ThermalSubsystem",
          "item": [
            {
              "name": "GET ThermalSubsystem Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/ThermalSubsystem",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/ThermalSubsystem\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "Triggers",
          "item": [
            {
              "name": "GET Triggers Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/Triggers",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/Triggers\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "TriggersCollection",
          "item": [
            {
              "name": "GET TriggersCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/TriggersCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/TriggersCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "UpdateService",
          "item": [
            {
              "name": "GET UpdateService Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/UpdateService",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/UpdateService\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "VirtualMedia",
          "item": [
            {
              "name": "GET VirtualMedia Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/VirtualMedia",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/VirtualMedia\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "VirtualMediaCollection",
          "item": [
            {
              "name": "GET VirtualMediaCollection Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/VirtualMediaCollection",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/VirtualMediaCollection\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "VirtualMediaConfig",
          "item": [
            {
              "name": "GET VirtualMediaConfig Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/VirtualMediaConfig",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/VirtualMediaConfig\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "VolumeConfig",
          "item": [
            {
              "name": "GET VolumeConfig Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/VolumeConfig",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/VolumeConfig\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "odata",
          "item": [
            {
              "name": "GET odata Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/odata",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/odata\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "odata-v4",
          "item": [
            {
              "name": "GET odata-v4 Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/odata-v4",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/odata-v4\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "redfish-error",
          "item": [
            {
              "name": "GET redfish-error Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/redfish-error",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/redfish-error\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "redfish-payload-annotations",
          "item": [
            {
              "name": "GET redfish-payload-annotations Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/redfish-payload-annotations",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/redfish-payload-annotations\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "redfish-schema",
          "item": [
            {
              "name": "GET redfish-schema Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/redfish-schema",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/redfish-schema\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "redfish-schema-v1",
          "item": [
            {
              "name": "GET redfish-schema-v1 Schema File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas/redfish-schema-v1",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/JsonSchemas/redfish-schema-v1\nType: #JsonSchemaFile.v1_0_2.JsonSchemaFile"
        },
        {
          "name": "GET JsonSchemaFile Collection",
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/JsonSchemas",
            "description": "Evidence: allow-header"
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const urlTools = require('url');",
                  "function parseURL(raw) {",
                  "  const value = urlTools.parse(raw);",
                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                  "  return value;",
                  "}",
                  "function targetBase() {",
                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                  "}",
                  "function authorizeSession(raw, baseURL) {",
                  "  const base = targetBase();",
                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                  "  let expanded = raw;",
                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                  "  const decoded = decodeURIComponent(session.pathname || '');",
                  "  if (session.origin !== base.origin || session.auth ||",
                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                  "  return session;",
                  "}",
                  "",
                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                  "pm.request.headers.remove('Authorization');",
                  "pm.request.headers.remove('X-Auth-Token');",
                  "if (mode === 'basic') {",
                  "  const username = pm.environment.get('USERNAME') || '';",
                  "  const password = pm.environment.get('PASSWORD') || '';",
                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                  "} else {",
                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                  "    pm.execution.skipRequest();",
                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                  "  }",
                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Response status is below 400', function () {",
                  "  pm.expect(pm.response.code).to.be.below(400);",
                  "});",
                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                  "if (pm.response.code !== 204) {",
                  "  pm.test('Response is JSON', function () {",
                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                  "  });",
                  "}",
                  ""
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        }
      ],
      "description": "URI: /redfish/v1/JsonSchemas\nType: #JsonSchemaFileCollection.JsonSchemaFileCollection"
    },
    {
      "name": "LicenseService",
      "item": [
        {
          "name": "Actions",
          "item": [
            {
              "name": "POST LicenseService.Install",
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                      "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                      "    pm.execution.skipRequest();",
                      "  } else {",
                      "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                      "  }",
                      "}",
                      "",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                }
              ],
              "request": {
                "method": "POST",
                "header": [
                  {
                    "key": "Content-Type",
                    "value": "application/json",
                    "type": "text"
                  }
                ],
                "body": {
                  "mode": "raw",
                  "raw": "{}",
                  "options": {
                    "raw": {
                      "language": "json"
                    }
                  }
                },
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/LicenseService/Actions/LicenseService.Install",
                "description": "Redfish Action. Review the payload before explicitly enabling mutations."
              },
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ]
        },
        {
          "name": "Licenses",
          "item": [
            {
              "name": "SFT-DCMS-SINGLE",
              "item": [
                {
                  "name": "GET License",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/LicenseService/Licenses/SFT-DCMS-SINGLE",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/LicenseService/Licenses/SFT-DCMS-SINGLE\nType: #License.v1_1_3.License"
            },
            {
              "name": "GET License Collection",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/LicenseService/Licenses",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/LicenseService/Licenses\nType: #LicenseCollection.LicenseCollection"
        },
        {
          "name": "GET License Service",
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/LicenseService",
            "description": "Evidence: allow-header"
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const urlTools = require('url');",
                  "function parseURL(raw) {",
                  "  const value = urlTools.parse(raw);",
                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                  "  return value;",
                  "}",
                  "function targetBase() {",
                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                  "}",
                  "function authorizeSession(raw, baseURL) {",
                  "  const base = targetBase();",
                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                  "  let expanded = raw;",
                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                  "  const decoded = decodeURIComponent(session.pathname || '');",
                  "  if (session.origin !== base.origin || session.auth ||",
                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                  "  return session;",
                  "}",
                  "",
                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                  "pm.request.headers.remove('Authorization');",
                  "pm.request.headers.remove('X-Auth-Token');",
                  "if (mode === 'basic') {",
                  "  const username = pm.environment.get('USERNAME') || '';",
                  "  const password = pm.environment.get('PASSWORD') || '';",
                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                  "} else {",
                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                  "    pm.execution.skipRequest();",
                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                  "  }",
                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Response status is below 400', function () {",
                  "  pm.expect(pm.response.code).to.be.below(400);",
                  "});",
                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                  "if (pm.response.code !== 204) {",
                  "  pm.test('Response is JSON', function () {",
                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                  "  });",
                  "}",
                  ""
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        }
      ],
      "description": "URI: /redfish/v1/LicenseService\nType: #LicenseService.v1_1_2.LicenseService"
    },
    {
      "name": "Managers",
      "item": [
        {
          "name": "1",
          "item": [
            {
              "name": "Actions",
              "item": [
                {
                  "name": "POST Manager.Reset",
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                          "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                          "    pm.execution.skipRequest();",
                          "  } else {",
                          "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                          "  }",
                          "}",
                          "",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    }
                  ],
                  "request": {
                    "method": "POST",
                    "header": [
                      {
                        "key": "Content-Type",
                        "value": "application/json",
                        "type": "text"
                      }
                    ],
                    "body": {
                      "mode": "raw",
                      "raw": "{\n  \"ResetType\": \"<REQUIRED>\"\n}",
                      "options": {
                        "raw": {
                          "language": "json"
                        }
                      }
                    },
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Actions/Manager.Reset",
                    "description": "Redfish Action. Review the payload before explicitly enabling mutations."
                  },
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                },
                {
                  "name": "POST Manager.ResetToDefaults",
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                          "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                          "    pm.execution.skipRequest();",
                          "  } else {",
                          "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                          "  }",
                          "}",
                          "",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    }
                  ],
                  "request": {
                    "method": "POST",
                    "header": [
                      {
                        "key": "Content-Type",
                        "value": "application/json",
                        "type": "text"
                      }
                    ],
                    "body": {
                      "mode": "raw",
                      "raw": "{\n  \"ResetType\": \"<REQUIRED>\"\n}",
                      "options": {
                        "raw": {
                          "language": "json"
                        }
                      }
                    },
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Actions/Manager.ResetToDefaults",
                    "description": "Redfish Action. Review the payload before explicitly enabling mutations."
                  },
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ]
            },
            {
              "name": "DedicatedNetworkPorts",
              "item": [
                {
                  "name": "1",
                  "item": [
                    {
                      "name": "GET Ethernet Port",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/DedicatedNetworkPorts/1",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH Ethernet Port",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"f4a506a7\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/DedicatedNetworkPorts/1",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Managers/1/DedicatedNetworkPorts/1\nType: #Port.v1_16_0.Port"
                },
                {
                  "name": "GET DedicatedNetworkPorts Collection",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/DedicatedNetworkPorts",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/Managers/1/DedicatedNetworkPorts\nType: #PortCollection.PortCollection"
            },
            {
              "name": "EthernetInterfaces",
              "item": [
                {
                  "name": "1",
                  "item": [
                    {
                      "name": "GET Manager Ethernet Interface",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/EthernetInterfaces/1",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH Manager Ethernet Interface",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"0c137442\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/EthernetInterfaces/1",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Managers/1/EthernetInterfaces/1\nType: #EthernetInterface.v1_12_2.EthernetInterface"
                },
                {
                  "name": "ToHost",
                  "item": [
                    {
                      "name": "GET Manager Ethernet Interface",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/EthernetInterfaces/ToHost",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH Manager Ethernet Interface",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"953c4999\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/EthernetInterfaces/ToHost",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Managers/1/EthernetInterfaces/ToHost\nType: #EthernetInterface.v1_12_2.EthernetInterface"
                },
                {
                  "name": "GET Ethernet Network Interface Collection",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/EthernetInterfaces",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/Managers/1/EthernetInterfaces\nType: #EthernetInterfaceCollection.EthernetInterfaceCollection"
            },
            {
              "name": "HostInterfaces",
              "item": [
                {
                  "name": "1",
                  "item": [
                    {
                      "name": "HostEthernetInterfaces",
                      "item": [
                        {
                          "name": "GET Ethernet Network Interface Collection",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/HostInterfaces/1/HostEthernetInterfaces",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Managers/1/HostInterfaces/1/HostEthernetInterfaces\nType: #EthernetInterfaceCollection.EthernetInterfaceCollection"
                    },
                    {
                      "name": "GET HostInterface",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/HostInterfaces/1",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH HostInterface",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"c25c38d4\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/HostInterfaces/1",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Managers/1/HostInterfaces/1\nType: #HostInterface.v1_3_2.HostInterface"
                },
                {
                  "name": "GET Host Interface Collection",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/HostInterfaces",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/Managers/1/HostInterfaces\nType: #HostInterfaceCollection.HostInterfaceCollection"
            },
            {
              "name": "LicenseManager",
              "item": [
                {
                  "name": "Actions",
                  "item": [
                    {
                      "name": "POST LicenseManager.ActivateLicense",
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "request": {
                        "method": "POST",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          }
                        ],
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        },
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/LicenseManager/Actions/LicenseManager.ActivateLicense",
                        "description": "Redfish Action. Review the payload before explicitly enabling mutations."
                      },
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "POST LicenseManager.ClearLicense",
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "request": {
                        "method": "POST",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          }
                        ],
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        },
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/LicenseManager/Actions/LicenseManager.ClearLicense",
                        "description": "Redfish Action. Review the payload before explicitly enabling mutations."
                      },
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ]
                },
                {
                  "name": "QueryLicense",
                  "item": [
                    {
                      "name": "GET LicenseManager",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/LicenseManager/QueryLicense",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Managers/1/LicenseManager/QueryLicense\nType: #QueryLicense.v1_0_0.QueryLicense"
                },
                {
                  "name": "GET License Manager",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/LicenseManager",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/Managers/1/LicenseManager\nType: #LicenseManager.v1_0_0.LicenseManager"
            },
            {
              "name": "LogServices",
              "item": [
                {
                  "name": "Dump",
                  "item": [
                    {
                      "name": "Actions",
                      "item": [
                        {
                          "name": "POST LogService.ClearLog",
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                  "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                  "    pm.execution.skipRequest();",
                                  "  } else {",
                                  "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                  "  }",
                                  "}",
                                  "",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            }
                          ],
                          "request": {
                            "method": "POST",
                            "header": [
                              {
                                "key": "Content-Type",
                                "value": "application/json",
                                "type": "text"
                              }
                            ],
                            "body": {
                              "mode": "raw",
                              "raw": "{}",
                              "options": {
                                "raw": {
                                  "language": "json"
                                }
                              }
                            },
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/LogServices/Dump/Actions/LogService.ClearLog",
                            "description": "Redfish Action. Review the payload before explicitly enabling mutations."
                          },
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        },
                        {
                          "name": "POST LogService.CollectDiagnosticData",
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                  "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                  "    pm.execution.skipRequest();",
                                  "  } else {",
                                  "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                  "  }",
                                  "}",
                                  "",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            }
                          ],
                          "request": {
                            "method": "POST",
                            "header": [
                              {
                                "key": "Content-Type",
                                "value": "application/json",
                                "type": "text"
                              }
                            ],
                            "body": {
                              "mode": "raw",
                              "raw": "{}",
                              "options": {
                                "raw": {
                                  "language": "json"
                                }
                              }
                            },
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/LogServices/Dump/Actions/LogService.CollectDiagnosticData",
                            "description": "Redfish Action. Review the payload before explicitly enabling mutations."
                          },
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ]
                    },
                    {
                      "name": "Entries",
                      "item": [
                        {
                          "name": "GET Dump Log Entry Collection",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/LogServices/Dump/Entries",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Managers/1/LogServices/Dump/Entries\nType: #LogEntryCollection.LogEntryCollection"
                    },
                    {
                      "name": "GET Dump LogService",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/LogServices/Dump",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Managers/1/LogServices/Dump\nType: #LogService.v1_8_0.LogService"
                },
                {
                  "name": "Log1",
                  "item": [
                    {
                      "name": "Actions",
                      "item": [
                        {
                          "name": "POST LogService.ClearLog",
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                  "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                  "    pm.execution.skipRequest();",
                                  "  } else {",
                                  "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                  "  }",
                                  "}",
                                  "",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            }
                          ],
                          "request": {
                            "method": "POST",
                            "header": [
                              {
                                "key": "Content-Type",
                                "value": "application/json",
                                "type": "text"
                              }
                            ],
                            "body": {
                              "mode": "raw",
                              "raw": "{}",
                              "options": {
                                "raw": {
                                  "language": "json"
                                }
                              }
                            },
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/LogServices/Log1/Actions/LogService.ClearLog",
                            "description": "Redfish Action. Review the payload before explicitly enabling mutations."
                          },
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ]
                    },
                    {
                      "name": "Entries",
                      "item": [
                        {
                          "name": "1",
                          "item": [
                            {
                              "name": "GET Maintenance Event Log Entry",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/LogServices/Log1/Entries/1",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Managers/1/LogServices/Log1/Entries/1\nType: #LogEntry.v1_19_0.LogEntry"
                        },
                        {
                          "name": "10",
                          "item": [
                            {
                              "name": "GET Maintenance Event Log Entry",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/LogServices/Log1/Entries/10",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Managers/1/LogServices/Log1/Entries/10\nType: #LogEntry.v1_19_0.LogEntry"
                        },
                        {
                          "name": "11",
                          "item": [
                            {
                              "name": "GET Maintenance Event Log Entry",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/LogServices/Log1/Entries/11",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Managers/1/LogServices/Log1/Entries/11\nType: #LogEntry.v1_19_0.LogEntry"
                        },
                        {
                          "name": "12",
                          "item": [
                            {
                              "name": "GET Maintenance Event Log Entry",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/LogServices/Log1/Entries/12",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Managers/1/LogServices/Log1/Entries/12\nType: #LogEntry.v1_19_0.LogEntry"
                        },
                        {
                          "name": "13",
                          "item": [
                            {
                              "name": "GET Maintenance Event Log Entry",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/LogServices/Log1/Entries/13",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Managers/1/LogServices/Log1/Entries/13\nType: #LogEntry.v1_19_0.LogEntry"
                        },
                        {
                          "name": "14",
                          "item": [
                            {
                              "name": "GET Maintenance Event Log Entry",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/LogServices/Log1/Entries/14",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Managers/1/LogServices/Log1/Entries/14\nType: #LogEntry.v1_19_0.LogEntry"
                        },
                        {
                          "name": "15",
                          "item": [
                            {
                              "name": "GET Maintenance Event Log Entry",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/LogServices/Log1/Entries/15",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Managers/1/LogServices/Log1/Entries/15\nType: #LogEntry.v1_19_0.LogEntry"
                        },
                        {
                          "name": "16",
                          "item": [
                            {
                              "name": "GET Maintenance Event Log Entry",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/LogServices/Log1/Entries/16",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Managers/1/LogServices/Log1/Entries/16\nType: #LogEntry.v1_19_0.LogEntry"
                        },
                        {
                          "name": "17",
                          "item": [
                            {
                              "name": "GET Maintenance Event Log Entry",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/LogServices/Log1/Entries/17",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Managers/1/LogServices/Log1/Entries/17\nType: #LogEntry.v1_19_0.LogEntry"
                        },
                        {
                          "name": "2",
                          "item": [
                            {
                              "name": "GET Maintenance Event Log Entry",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/LogServices/Log1/Entries/2",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Managers/1/LogServices/Log1/Entries/2\nType: #LogEntry.v1_19_0.LogEntry"
                        },
                        {
                          "name": "3",
                          "item": [
                            {
                              "name": "GET Maintenance Event Log Entry",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/LogServices/Log1/Entries/3",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Managers/1/LogServices/Log1/Entries/3\nType: #LogEntry.v1_19_0.LogEntry"
                        },
                        {
                          "name": "4",
                          "item": [
                            {
                              "name": "GET Maintenance Event Log Entry",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/LogServices/Log1/Entries/4",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Managers/1/LogServices/Log1/Entries/4\nType: #LogEntry.v1_19_0.LogEntry"
                        },
                        {
                          "name": "5",
                          "item": [
                            {
                              "name": "GET Maintenance Event Log Entry",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/LogServices/Log1/Entries/5",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Managers/1/LogServices/Log1/Entries/5\nType: #LogEntry.v1_19_0.LogEntry"
                        },
                        {
                          "name": "6",
                          "item": [
                            {
                              "name": "GET Maintenance Event Log Entry",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/LogServices/Log1/Entries/6",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Managers/1/LogServices/Log1/Entries/6\nType: #LogEntry.v1_19_0.LogEntry"
                        },
                        {
                          "name": "7",
                          "item": [
                            {
                              "name": "GET Maintenance Event Log Entry",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/LogServices/Log1/Entries/7",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Managers/1/LogServices/Log1/Entries/7\nType: #LogEntry.v1_19_0.LogEntry"
                        },
                        {
                          "name": "8",
                          "item": [
                            {
                              "name": "GET Maintenance Event Log Entry",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/LogServices/Log1/Entries/8",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Managers/1/LogServices/Log1/Entries/8\nType: #LogEntry.v1_19_0.LogEntry"
                        },
                        {
                          "name": "9",
                          "item": [
                            {
                              "name": "GET Maintenance Event Log Entry",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/LogServices/Log1/Entries/9",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Managers/1/LogServices/Log1/Entries/9\nType: #LogEntry.v1_19_0.LogEntry"
                        },
                        {
                          "name": "GET Maintenance Event Log Entries",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/LogServices/Log1/Entries",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Managers/1/LogServices/Log1/Entries\nType: #LogEntryCollection.LogEntryCollection"
                    },
                    {
                      "name": "GET Maintenance Event Log Service",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/LogServices/Log1",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH Maintenance Event Log Service",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"fee3f38f\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/LogServices/Log1",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Managers/1/LogServices/Log1\nType: #LogService.v1_8_0.LogService"
                },
                {
                  "name": "GET Open BMC Log Services Collection",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/LogServices",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/Managers/1/LogServices\nType: #LogServiceCollection.LogServiceCollection"
            },
            {
              "name": "NetworkProtocol",
              "item": [
                {
                  "name": "HTTPS",
                  "item": [
                    {
                      "name": "Certificates",
                      "item": [
                        {
                          "name": "1",
                          "item": [
                            {
                              "name": "GET HTTPS Certificate",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/NetworkProtocol/HTTPS/Certificates/1",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Managers/1/NetworkProtocol/HTTPS/Certificates/1\nType: #Certificate.v1_10_0.Certificate"
                        },
                        {
                          "name": "GET HTTPS Certificates Collection",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/NetworkProtocol/HTTPS/Certificates",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Managers/1/NetworkProtocol/HTTPS/Certificates\nType: #CertificateCollection.CertificateCollection"
                    }
                  ]
                },
                {
                  "name": "GET Manager Network Protocol",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/NetworkProtocol",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                },
                {
                  "name": "PATCH Manager Network Protocol",
                  "request": {
                    "method": "PATCH",
                    "header": [
                      {
                        "key": "Content-Type",
                        "value": "application/json",
                        "type": "text"
                      },
                      {
                        "key": "If-Match",
                        "value": "\"a9d09f8e\"",
                        "type": "text"
                      }
                    ],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/NetworkProtocol",
                    "description": "Evidence: allow-header",
                    "body": {
                      "mode": "raw",
                      "raw": "{}",
                      "options": {
                        "raw": {
                          "language": "json"
                        }
                      }
                    }
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                          "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                          "    pm.execution.skipRequest();",
                          "  } else {",
                          "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                          "  }",
                          "}",
                          "",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/Managers/1/NetworkProtocol\nType: #ManagerNetworkProtocol.v1_12_0.ManagerNetworkProtocol"
            },
            {
              "name": "Oem",
              "item": [
                {
                  "name": "Supermicro",
                  "item": [
                    {
                      "name": "FanMode",
                      "item": [
                        {
                          "name": "GET FanMode",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/FanMode",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        },
                        {
                          "name": "PATCH FanMode",
                          "request": {
                            "method": "PATCH",
                            "header": [
                              {
                                "key": "Content-Type",
                                "value": "application/json",
                                "type": "text"
                              },
                              {
                                "key": "If-Match",
                                "value": "\"b694158a\"",
                                "type": "text"
                              }
                            ],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/FanMode",
                            "description": "Evidence: allow-header",
                            "body": {
                              "mode": "raw",
                              "raw": "{}",
                              "options": {
                                "raw": {
                                  "language": "json"
                                }
                              }
                            }
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                  "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                  "    pm.execution.skipRequest();",
                                  "  } else {",
                                  "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                  "  }",
                                  "}",
                                  "",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Managers/1/Oem/Supermicro/FanMode\nType: #FanMode.v1_0_1.FanMode"
                    },
                    {
                      "name": "IKVM",
                      "item": [
                        {
                          "name": "GET IKVM",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/IKVM",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        },
                        {
                          "name": "PATCH IKVM",
                          "request": {
                            "method": "PATCH",
                            "header": [
                              {
                                "key": "Content-Type",
                                "value": "application/json",
                                "type": "text"
                              },
                              {
                                "key": "If-Match",
                                "value": "\"a89163f6\"",
                                "type": "text"
                              }
                            ],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/IKVM",
                            "description": "Evidence: allow-header",
                            "body": {
                              "mode": "raw",
                              "raw": "{}",
                              "options": {
                                "raw": {
                                  "language": "json"
                                }
                              }
                            }
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                  "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                  "    pm.execution.skipRequest();",
                                  "  } else {",
                                  "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                  "  }",
                                  "}",
                                  "",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Managers/1/Oem/Supermicro/IKVM\nType: #IKVM.v1_0_3.IKVM"
                    },
                    {
                      "name": "IPAccessControl",
                      "item": [
                        {
                          "name": "FilterRules",
                          "item": [
                            {
                              "name": "GET Filter Rule Collection",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/IPAccessControl/FilterRules",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Managers/1/Oem/Supermicro/IPAccessControl/FilterRules\nType: #FilterRuleCollection.FilterRuleCollection"
                        },
                        {
                          "name": "GET IP Access Control",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/IPAccessControl",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        },
                        {
                          "name": "PATCH IP Access Control",
                          "request": {
                            "method": "PATCH",
                            "header": [
                              {
                                "key": "Content-Type",
                                "value": "application/json",
                                "type": "text"
                              },
                              {
                                "key": "If-Match",
                                "value": "\"7b5c6d09\"",
                                "type": "text"
                              }
                            ],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/IPAccessControl",
                            "description": "Evidence: allow-header",
                            "body": {
                              "mode": "raw",
                              "raw": "{}",
                              "options": {
                                "raw": {
                                  "language": "json"
                                }
                              }
                            }
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                  "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                  "    pm.execution.skipRequest();",
                                  "  } else {",
                                  "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                  "  }",
                                  "}",
                                  "",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Managers/1/Oem/Supermicro/IPAccessControl\nType: #IPAccessControl.v1_0_1.IPAccessControl"
                    },
                    {
                      "name": "KCSInterface",
                      "item": [
                        {
                          "name": "GET KCS Interface",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/KCSInterface",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        },
                        {
                          "name": "PATCH KCS Interface",
                          "request": {
                            "method": "PATCH",
                            "header": [
                              {
                                "key": "Content-Type",
                                "value": "application/json",
                                "type": "text"
                              },
                              {
                                "key": "If-Match",
                                "value": "\"5117ffa9\"",
                                "type": "text"
                              }
                            ],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/KCSInterface",
                            "description": "Evidence: allow-header",
                            "body": {
                              "mode": "raw",
                              "raw": "{}",
                              "options": {
                                "raw": {
                                  "language": "json"
                                }
                              }
                            }
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                  "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                  "    pm.execution.skipRequest();",
                                  "  } else {",
                                  "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                  "  }",
                                  "}",
                                  "",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Managers/1/Oem/Supermicro/KCSInterface\nType: #KCSInterface.v1_0_0.KCSInterface"
                    },
                    {
                      "name": "LLDP",
                      "item": [
                        {
                          "name": "GET LLDP",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/LLDP",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        },
                        {
                          "name": "PATCH LLDP",
                          "request": {
                            "method": "PATCH",
                            "header": [
                              {
                                "key": "Content-Type",
                                "value": "application/json",
                                "type": "text"
                              },
                              {
                                "key": "If-Match",
                                "value": "\"a58c62f8\"",
                                "type": "text"
                              }
                            ],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/LLDP",
                            "description": "Evidence: allow-header",
                            "body": {
                              "mode": "raw",
                              "raw": "{}",
                              "options": {
                                "raw": {
                                  "language": "json"
                                }
                              }
                            }
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                  "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                  "    pm.execution.skipRequest();",
                                  "  } else {",
                                  "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                  "  }",
                                  "}",
                                  "",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Managers/1/Oem/Supermicro/LLDP\nType: #LldpService.v1_0_0.LldpService"
                    },
                    {
                      "name": "NTP",
                      "item": [
                        {
                          "name": "GET NTP Service",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/NTP",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        },
                        {
                          "name": "PATCH NTP Service",
                          "request": {
                            "method": "PATCH",
                            "header": [
                              {
                                "key": "Content-Type",
                                "value": "application/json",
                                "type": "text"
                              },
                              {
                                "key": "If-Match",
                                "value": "\"9f8d5fa8\"",
                                "type": "text"
                              }
                            ],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/NTP",
                            "description": "Evidence: allow-header",
                            "body": {
                              "mode": "raw",
                              "raw": "{}",
                              "options": {
                                "raw": {
                                  "language": "json"
                                }
                              }
                            }
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                  "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                  "    pm.execution.skipRequest();",
                                  "  } else {",
                                  "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                  "  }",
                                  "}",
                                  "",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Managers/1/Oem/Supermicro/NTP\nType: #NTP.v1_0_3.NTP"
                    },
                    {
                      "name": "NodeManager",
                      "item": [
                        {
                          "name": "Actions",
                          "item": [
                            {
                              "name": "POST NodeManager.Reset",
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                      "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                      "    pm.execution.skipRequest();",
                                      "  } else {",
                                      "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                      "  }",
                                      "}",
                                      "",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                }
                              ],
                              "request": {
                                "method": "POST",
                                "header": [
                                  {
                                    "key": "Content-Type",
                                    "value": "application/json",
                                    "type": "text"
                                  }
                                ],
                                "body": {
                                  "mode": "raw",
                                  "raw": "{}",
                                  "options": {
                                    "raw": {
                                      "language": "json"
                                    }
                                  }
                                },
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/NodeManager/Actions/NodeManager.Reset",
                                "description": "Redfish Action. Review the payload before explicitly enabling mutations."
                              },
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            },
                            {
                              "name": "POST NodeManager.Reset",
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                      "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                      "    pm.execution.skipRequest();",
                                      "  } else {",
                                      "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                      "  }",
                                      "}",
                                      "",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                }
                              ],
                              "request": {
                                "method": "POST",
                                "header": [
                                  {
                                    "key": "Content-Type",
                                    "value": "application/json",
                                    "type": "text"
                                  }
                                ],
                                "body": {
                                  "mode": "raw",
                                  "raw": "{}",
                                  "options": {
                                    "raw": {
                                      "language": "json"
                                    }
                                  }
                                },
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/NodeManager/Actions/NodeManager.Reset",
                                "description": "Redfish Action. Review the payload before explicitly enabling mutations."
                              },
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ]
                        },
                        {
                          "name": "Domains",
                          "item": [
                            {
                              "name": "CPUSubsystem",
                              "item": [
                                {
                                  "name": "Actions",
                                  "item": [
                                    {
                                      "name": "POST Domain.ResetStatistics",
                                      "event": [
                                        {
                                          "listen": "prerequest",
                                          "script": {
                                            "type": "text/javascript",
                                            "exec": [
                                              "const urlTools = require('url');",
                                              "function parseURL(raw) {",
                                              "  const value = urlTools.parse(raw);",
                                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                              "  return value;",
                                              "}",
                                              "function targetBase() {",
                                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                              "}",
                                              "function authorizeSession(raw, baseURL) {",
                                              "  const base = targetBase();",
                                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                              "  let expanded = raw;",
                                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                              "  const decoded = decodeURIComponent(session.pathname || '');",
                                              "  if (session.origin !== base.origin || session.auth ||",
                                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                              "  return session;",
                                              "}",
                                              "",
                                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                              "    pm.execution.skipRequest();",
                                              "  } else {",
                                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                              "  }",
                                              "}",
                                              "",
                                              "",
                                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                              "pm.request.headers.remove('Authorization');",
                                              "pm.request.headers.remove('X-Auth-Token');",
                                              "if (mode === 'basic') {",
                                              "  const username = pm.environment.get('USERNAME') || '';",
                                              "  const password = pm.environment.get('PASSWORD') || '';",
                                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                              "} else {",
                                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                              "    pm.execution.skipRequest();",
                                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                                              "  }",
                                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                              "}"
                                            ]
                                          }
                                        }
                                      ],
                                      "request": {
                                        "method": "POST",
                                        "header": [
                                          {
                                            "key": "Content-Type",
                                            "value": "application/json",
                                            "type": "text"
                                          }
                                        ],
                                        "body": {
                                          "mode": "raw",
                                          "raw": "{}",
                                          "options": {
                                            "raw": {
                                              "language": "json"
                                            }
                                          }
                                        },
                                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/NodeManager/Domains/CPUSubsystem/Actions/Domain.ResetStatistics",
                                        "description": "Redfish Action. Review the payload before explicitly enabling mutations."
                                      },
                                      "protocolProfileBehavior": {
                                        "followRedirects": false
                                      }
                                    }
                                  ]
                                },
                                {
                                  "name": "GET CPUSubsystem",
                                  "request": {
                                    "method": "GET",
                                    "header": [],
                                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/NodeManager/Domains/CPUSubsystem",
                                    "description": "Evidence: allow-header"
                                  },
                                  "event": [
                                    {
                                      "listen": "prerequest",
                                      "script": {
                                        "type": "text/javascript",
                                        "exec": [
                                          "const urlTools = require('url');",
                                          "function parseURL(raw) {",
                                          "  const value = urlTools.parse(raw);",
                                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                          "  return value;",
                                          "}",
                                          "function targetBase() {",
                                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                          "}",
                                          "function authorizeSession(raw, baseURL) {",
                                          "  const base = targetBase();",
                                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                          "  let expanded = raw;",
                                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                          "  const decoded = decodeURIComponent(session.pathname || '');",
                                          "  if (session.origin !== base.origin || session.auth ||",
                                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                          "  return session;",
                                          "}",
                                          "",
                                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                          "pm.request.headers.remove('Authorization');",
                                          "pm.request.headers.remove('X-Auth-Token');",
                                          "if (mode === 'basic') {",
                                          "  const username = pm.environment.get('USERNAME') || '';",
                                          "  const password = pm.environment.get('PASSWORD') || '';",
                                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                          "} else {",
                                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                          "    pm.execution.skipRequest();",
                                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                                          "  }",
                                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                          "}"
                                        ]
                                      }
                                    },
                                    {
                                      "listen": "test",
                                      "script": {
                                        "type": "text/javascript",
                                        "exec": [
                                          "pm.test('Response status is below 400', function () {",
                                          "  pm.expect(pm.response.code).to.be.below(400);",
                                          "});",
                                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                                          "if (pm.response.code !== 204) {",
                                          "  pm.test('Response is JSON', function () {",
                                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                          "  });",
                                          "}",
                                          ""
                                        ]
                                      }
                                    }
                                  ],
                                  "protocolProfileBehavior": {
                                    "followRedirects": false
                                  }
                                },
                                {
                                  "name": "PATCH CPUSubsystem",
                                  "request": {
                                    "method": "PATCH",
                                    "header": [
                                      {
                                        "key": "Content-Type",
                                        "value": "application/json",
                                        "type": "text"
                                      },
                                      {
                                        "key": "If-Match",
                                        "value": "\"febb733a\"",
                                        "type": "text"
                                      }
                                    ],
                                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/NodeManager/Domains/CPUSubsystem",
                                    "description": "Evidence: allow-header",
                                    "body": {
                                      "mode": "raw",
                                      "raw": "{}",
                                      "options": {
                                        "raw": {
                                          "language": "json"
                                        }
                                      }
                                    }
                                  },
                                  "event": [
                                    {
                                      "listen": "prerequest",
                                      "script": {
                                        "type": "text/javascript",
                                        "exec": [
                                          "const urlTools = require('url');",
                                          "function parseURL(raw) {",
                                          "  const value = urlTools.parse(raw);",
                                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                          "  return value;",
                                          "}",
                                          "function targetBase() {",
                                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                          "}",
                                          "function authorizeSession(raw, baseURL) {",
                                          "  const base = targetBase();",
                                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                          "  let expanded = raw;",
                                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                          "  const decoded = decodeURIComponent(session.pathname || '');",
                                          "  if (session.origin !== base.origin || session.auth ||",
                                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                          "  return session;",
                                          "}",
                                          "",
                                          "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                          "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                          "    pm.execution.skipRequest();",
                                          "  } else {",
                                          "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                          "  }",
                                          "}",
                                          "",
                                          "",
                                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                          "pm.request.headers.remove('Authorization');",
                                          "pm.request.headers.remove('X-Auth-Token');",
                                          "if (mode === 'basic') {",
                                          "  const username = pm.environment.get('USERNAME') || '';",
                                          "  const password = pm.environment.get('PASSWORD') || '';",
                                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                          "} else {",
                                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                          "    pm.execution.skipRequest();",
                                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                                          "  }",
                                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                          "}"
                                        ]
                                      }
                                    }
                                  ],
                                  "protocolProfileBehavior": {
                                    "followRedirects": false
                                  }
                                }
                              ],
                              "description": "URI: /redfish/v1/Managers/1/Oem/Supermicro/NodeManager/Domains/CPUSubsystem\nType: #NmDomain.v1_4_0.Domain"
                            },
                            {
                              "name": "MemorySubsystem",
                              "item": [
                                {
                                  "name": "Actions",
                                  "item": [
                                    {
                                      "name": "POST Domain.ResetStatistics",
                                      "event": [
                                        {
                                          "listen": "prerequest",
                                          "script": {
                                            "type": "text/javascript",
                                            "exec": [
                                              "const urlTools = require('url');",
                                              "function parseURL(raw) {",
                                              "  const value = urlTools.parse(raw);",
                                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                              "  return value;",
                                              "}",
                                              "function targetBase() {",
                                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                              "}",
                                              "function authorizeSession(raw, baseURL) {",
                                              "  const base = targetBase();",
                                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                              "  let expanded = raw;",
                                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                              "  const decoded = decodeURIComponent(session.pathname || '');",
                                              "  if (session.origin !== base.origin || session.auth ||",
                                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                              "  return session;",
                                              "}",
                                              "",
                                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                              "    pm.execution.skipRequest();",
                                              "  } else {",
                                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                              "  }",
                                              "}",
                                              "",
                                              "",
                                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                              "pm.request.headers.remove('Authorization');",
                                              "pm.request.headers.remove('X-Auth-Token');",
                                              "if (mode === 'basic') {",
                                              "  const username = pm.environment.get('USERNAME') || '';",
                                              "  const password = pm.environment.get('PASSWORD') || '';",
                                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                              "} else {",
                                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                              "    pm.execution.skipRequest();",
                                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                                              "  }",
                                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                              "}"
                                            ]
                                          }
                                        }
                                      ],
                                      "request": {
                                        "method": "POST",
                                        "header": [
                                          {
                                            "key": "Content-Type",
                                            "value": "application/json",
                                            "type": "text"
                                          }
                                        ],
                                        "body": {
                                          "mode": "raw",
                                          "raw": "{}",
                                          "options": {
                                            "raw": {
                                              "language": "json"
                                            }
                                          }
                                        },
                                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/NodeManager/Domains/MemorySubsystem/Actions/Domain.ResetStatistics",
                                        "description": "Redfish Action. Review the payload before explicitly enabling mutations."
                                      },
                                      "protocolProfileBehavior": {
                                        "followRedirects": false
                                      }
                                    }
                                  ]
                                },
                                {
                                  "name": "GET MemorySubsystem",
                                  "request": {
                                    "method": "GET",
                                    "header": [],
                                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/NodeManager/Domains/MemorySubsystem",
                                    "description": "Evidence: allow-header"
                                  },
                                  "event": [
                                    {
                                      "listen": "prerequest",
                                      "script": {
                                        "type": "text/javascript",
                                        "exec": [
                                          "const urlTools = require('url');",
                                          "function parseURL(raw) {",
                                          "  const value = urlTools.parse(raw);",
                                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                          "  return value;",
                                          "}",
                                          "function targetBase() {",
                                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                          "}",
                                          "function authorizeSession(raw, baseURL) {",
                                          "  const base = targetBase();",
                                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                          "  let expanded = raw;",
                                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                          "  const decoded = decodeURIComponent(session.pathname || '');",
                                          "  if (session.origin !== base.origin || session.auth ||",
                                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                          "  return session;",
                                          "}",
                                          "",
                                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                          "pm.request.headers.remove('Authorization');",
                                          "pm.request.headers.remove('X-Auth-Token');",
                                          "if (mode === 'basic') {",
                                          "  const username = pm.environment.get('USERNAME') || '';",
                                          "  const password = pm.environment.get('PASSWORD') || '';",
                                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                          "} else {",
                                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                          "    pm.execution.skipRequest();",
                                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                                          "  }",
                                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                          "}"
                                        ]
                                      }
                                    },
                                    {
                                      "listen": "test",
                                      "script": {
                                        "type": "text/javascript",
                                        "exec": [
                                          "pm.test('Response status is below 400', function () {",
                                          "  pm.expect(pm.response.code).to.be.below(400);",
                                          "});",
                                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                                          "if (pm.response.code !== 204) {",
                                          "  pm.test('Response is JSON', function () {",
                                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                          "  });",
                                          "}",
                                          ""
                                        ]
                                      }
                                    }
                                  ],
                                  "protocolProfileBehavior": {
                                    "followRedirects": false
                                  }
                                },
                                {
                                  "name": "PATCH MemorySubsystem",
                                  "request": {
                                    "method": "PATCH",
                                    "header": [
                                      {
                                        "key": "Content-Type",
                                        "value": "application/json",
                                        "type": "text"
                                      },
                                      {
                                        "key": "If-Match",
                                        "value": "\"d1e034f1\"",
                                        "type": "text"
                                      }
                                    ],
                                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/NodeManager/Domains/MemorySubsystem",
                                    "description": "Evidence: allow-header",
                                    "body": {
                                      "mode": "raw",
                                      "raw": "{}",
                                      "options": {
                                        "raw": {
                                          "language": "json"
                                        }
                                      }
                                    }
                                  },
                                  "event": [
                                    {
                                      "listen": "prerequest",
                                      "script": {
                                        "type": "text/javascript",
                                        "exec": [
                                          "const urlTools = require('url');",
                                          "function parseURL(raw) {",
                                          "  const value = urlTools.parse(raw);",
                                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                          "  return value;",
                                          "}",
                                          "function targetBase() {",
                                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                          "}",
                                          "function authorizeSession(raw, baseURL) {",
                                          "  const base = targetBase();",
                                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                          "  let expanded = raw;",
                                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                          "  const decoded = decodeURIComponent(session.pathname || '');",
                                          "  if (session.origin !== base.origin || session.auth ||",
                                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                          "  return session;",
                                          "}",
                                          "",
                                          "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                          "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                          "    pm.execution.skipRequest();",
                                          "  } else {",
                                          "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                          "  }",
                                          "}",
                                          "",
                                          "",
                                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                          "pm.request.headers.remove('Authorization');",
                                          "pm.request.headers.remove('X-Auth-Token');",
                                          "if (mode === 'basic') {",
                                          "  const username = pm.environment.get('USERNAME') || '';",
                                          "  const password = pm.environment.get('PASSWORD') || '';",
                                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                          "} else {",
                                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                          "    pm.execution.skipRequest();",
                                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                                          "  }",
                                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                          "}"
                                        ]
                                      }
                                    }
                                  ],
                                  "protocolProfileBehavior": {
                                    "followRedirects": false
                                  }
                                }
                              ],
                              "description": "URI: /redfish/v1/Managers/1/Oem/Supermicro/NodeManager/Domains/MemorySubsystem\nType: #NmDomain.v1_4_0.Domain"
                            },
                            {
                              "name": "GET NM Domains Collection",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/NodeManager/Domains",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Managers/1/Oem/Supermicro/NodeManager/Domains\nType: #NmDomainCollection.NmDomainCollection"
                        },
                        {
                          "name": "Policies",
                          "item": [
                            {
                              "name": "CPU_PowerLimit",
                              "item": [
                                {
                                  "name": "Actions",
                                  "item": [
                                    {
                                      "name": "POST NmPolicy.ResetStatistics",
                                      "event": [
                                        {
                                          "listen": "prerequest",
                                          "script": {
                                            "type": "text/javascript",
                                            "exec": [
                                              "const urlTools = require('url');",
                                              "function parseURL(raw) {",
                                              "  const value = urlTools.parse(raw);",
                                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                              "  return value;",
                                              "}",
                                              "function targetBase() {",
                                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                              "}",
                                              "function authorizeSession(raw, baseURL) {",
                                              "  const base = targetBase();",
                                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                              "  let expanded = raw;",
                                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                              "  const decoded = decodeURIComponent(session.pathname || '');",
                                              "  if (session.origin !== base.origin || session.auth ||",
                                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                              "  return session;",
                                              "}",
                                              "",
                                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                              "    pm.execution.skipRequest();",
                                              "  } else {",
                                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                              "  }",
                                              "}",
                                              "",
                                              "",
                                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                              "pm.request.headers.remove('Authorization');",
                                              "pm.request.headers.remove('X-Auth-Token');",
                                              "if (mode === 'basic') {",
                                              "  const username = pm.environment.get('USERNAME') || '';",
                                              "  const password = pm.environment.get('PASSWORD') || '';",
                                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                              "} else {",
                                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                              "    pm.execution.skipRequest();",
                                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                                              "  }",
                                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                              "}"
                                            ]
                                          }
                                        }
                                      ],
                                      "request": {
                                        "method": "POST",
                                        "header": [
                                          {
                                            "key": "Content-Type",
                                            "value": "application/json",
                                            "type": "text"
                                          }
                                        ],
                                        "body": {
                                          "mode": "raw",
                                          "raw": "{}",
                                          "options": {
                                            "raw": {
                                              "language": "json"
                                            }
                                          }
                                        },
                                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/NodeManager/Policies/CPU_PowerLimit/Actions/Policy.ResetStatistics",
                                        "description": "Redfish Action. Review the payload before explicitly enabling mutations."
                                      },
                                      "protocolProfileBehavior": {
                                        "followRedirects": false
                                      }
                                    }
                                  ]
                                },
                                {
                                  "name": "GET CPU_PowerLimit",
                                  "request": {
                                    "method": "GET",
                                    "header": [],
                                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/NodeManager/Policies/CPU_PowerLimit",
                                    "description": "Evidence: allow-header"
                                  },
                                  "event": [
                                    {
                                      "listen": "prerequest",
                                      "script": {
                                        "type": "text/javascript",
                                        "exec": [
                                          "const urlTools = require('url');",
                                          "function parseURL(raw) {",
                                          "  const value = urlTools.parse(raw);",
                                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                          "  return value;",
                                          "}",
                                          "function targetBase() {",
                                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                          "}",
                                          "function authorizeSession(raw, baseURL) {",
                                          "  const base = targetBase();",
                                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                          "  let expanded = raw;",
                                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                          "  const decoded = decodeURIComponent(session.pathname || '');",
                                          "  if (session.origin !== base.origin || session.auth ||",
                                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                          "  return session;",
                                          "}",
                                          "",
                                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                          "pm.request.headers.remove('Authorization');",
                                          "pm.request.headers.remove('X-Auth-Token');",
                                          "if (mode === 'basic') {",
                                          "  const username = pm.environment.get('USERNAME') || '';",
                                          "  const password = pm.environment.get('PASSWORD') || '';",
                                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                          "} else {",
                                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                          "    pm.execution.skipRequest();",
                                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                                          "  }",
                                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                          "}"
                                        ]
                                      }
                                    },
                                    {
                                      "listen": "test",
                                      "script": {
                                        "type": "text/javascript",
                                        "exec": [
                                          "pm.test('Response status is below 400', function () {",
                                          "  pm.expect(pm.response.code).to.be.below(400);",
                                          "});",
                                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                                          "if (pm.response.code !== 204) {",
                                          "  pm.test('Response is JSON', function () {",
                                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                          "  });",
                                          "}",
                                          ""
                                        ]
                                      }
                                    }
                                  ],
                                  "protocolProfileBehavior": {
                                    "followRedirects": false
                                  }
                                },
                                {
                                  "name": "PATCH CPU_PowerLimit",
                                  "request": {
                                    "method": "PATCH",
                                    "header": [
                                      {
                                        "key": "Content-Type",
                                        "value": "application/json",
                                        "type": "text"
                                      },
                                      {
                                        "key": "If-Match",
                                        "value": "\"00ce937f\"",
                                        "type": "text"
                                      }
                                    ],
                                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/NodeManager/Policies/CPU_PowerLimit",
                                    "description": "Evidence: allow-header",
                                    "body": {
                                      "mode": "raw",
                                      "raw": "{}",
                                      "options": {
                                        "raw": {
                                          "language": "json"
                                        }
                                      }
                                    }
                                  },
                                  "event": [
                                    {
                                      "listen": "prerequest",
                                      "script": {
                                        "type": "text/javascript",
                                        "exec": [
                                          "const urlTools = require('url');",
                                          "function parseURL(raw) {",
                                          "  const value = urlTools.parse(raw);",
                                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                          "  return value;",
                                          "}",
                                          "function targetBase() {",
                                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                          "}",
                                          "function authorizeSession(raw, baseURL) {",
                                          "  const base = targetBase();",
                                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                          "  let expanded = raw;",
                                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                          "  const decoded = decodeURIComponent(session.pathname || '');",
                                          "  if (session.origin !== base.origin || session.auth ||",
                                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                          "  return session;",
                                          "}",
                                          "",
                                          "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                          "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                          "    pm.execution.skipRequest();",
                                          "  } else {",
                                          "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                          "  }",
                                          "}",
                                          "",
                                          "",
                                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                          "pm.request.headers.remove('Authorization');",
                                          "pm.request.headers.remove('X-Auth-Token');",
                                          "if (mode === 'basic') {",
                                          "  const username = pm.environment.get('USERNAME') || '';",
                                          "  const password = pm.environment.get('PASSWORD') || '';",
                                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                          "} else {",
                                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                          "    pm.execution.skipRequest();",
                                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                                          "  }",
                                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                          "}"
                                        ]
                                      }
                                    }
                                  ],
                                  "protocolProfileBehavior": {
                                    "followRedirects": false
                                  }
                                }
                              ],
                              "description": "URI: /redfish/v1/Managers/1/Oem/Supermicro/NodeManager/Policies/CPU_PowerLimit\nType: #NmPolicy.v1_2_0.Policy"
                            },
                            {
                              "name": "GET NM Policies Collection",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/NodeManager/Policies",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Managers/1/Oem/Supermicro/NodeManager/Policies\nType: #NmPolicyCollection.NmPolicyCollection"
                        },
                        {
                          "name": "ThrottlingStatus",
                          "item": [
                            {
                              "name": "GET NM Throttling Status",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/NodeManager/ThrottlingStatus",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Managers/1/Oem/Supermicro/NodeManager/ThrottlingStatus\nType: #NmThrottlingStatus.v1_1_0.ThrottlingStatus"
                        },
                        {
                          "name": "Triggers",
                          "item": [
                            {
                              "name": "AlwaysOn",
                              "item": [
                                {
                                  "name": "GET Always On",
                                  "request": {
                                    "method": "GET",
                                    "header": [],
                                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/NodeManager/Triggers/AlwaysOn",
                                    "description": "Evidence: allow-header"
                                  },
                                  "event": [
                                    {
                                      "listen": "prerequest",
                                      "script": {
                                        "type": "text/javascript",
                                        "exec": [
                                          "const urlTools = require('url');",
                                          "function parseURL(raw) {",
                                          "  const value = urlTools.parse(raw);",
                                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                          "  return value;",
                                          "}",
                                          "function targetBase() {",
                                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                          "}",
                                          "function authorizeSession(raw, baseURL) {",
                                          "  const base = targetBase();",
                                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                          "  let expanded = raw;",
                                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                          "  const decoded = decodeURIComponent(session.pathname || '');",
                                          "  if (session.origin !== base.origin || session.auth ||",
                                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                          "  return session;",
                                          "}",
                                          "",
                                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                          "pm.request.headers.remove('Authorization');",
                                          "pm.request.headers.remove('X-Auth-Token');",
                                          "if (mode === 'basic') {",
                                          "  const username = pm.environment.get('USERNAME') || '';",
                                          "  const password = pm.environment.get('PASSWORD') || '';",
                                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                          "} else {",
                                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                          "    pm.execution.skipRequest();",
                                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                                          "  }",
                                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                          "}"
                                        ]
                                      }
                                    },
                                    {
                                      "listen": "test",
                                      "script": {
                                        "type": "text/javascript",
                                        "exec": [
                                          "pm.test('Response status is below 400', function () {",
                                          "  pm.expect(pm.response.code).to.be.below(400);",
                                          "});",
                                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                                          "if (pm.response.code !== 204) {",
                                          "  pm.test('Response is JSON', function () {",
                                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                          "  });",
                                          "}",
                                          ""
                                        ]
                                      }
                                    }
                                  ],
                                  "protocolProfileBehavior": {
                                    "followRedirects": false
                                  }
                                }
                              ],
                              "description": "URI: /redfish/v1/Managers/1/Oem/Supermicro/NodeManager/Triggers/AlwaysOn\nType: #NmTrigger.v1_0_0.NmTrigger"
                            },
                            {
                              "name": "CPUUtilization",
                              "item": [
                                {
                                  "name": "GET C0 Residency",
                                  "request": {
                                    "method": "GET",
                                    "header": [],
                                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/NodeManager/Triggers/CPUUtilization",
                                    "description": "Evidence: allow-header"
                                  },
                                  "event": [
                                    {
                                      "listen": "prerequest",
                                      "script": {
                                        "type": "text/javascript",
                                        "exec": [
                                          "const urlTools = require('url');",
                                          "function parseURL(raw) {",
                                          "  const value = urlTools.parse(raw);",
                                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                          "  return value;",
                                          "}",
                                          "function targetBase() {",
                                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                          "}",
                                          "function authorizeSession(raw, baseURL) {",
                                          "  const base = targetBase();",
                                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                          "  let expanded = raw;",
                                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                          "  const decoded = decodeURIComponent(session.pathname || '');",
                                          "  if (session.origin !== base.origin || session.auth ||",
                                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                          "  return session;",
                                          "}",
                                          "",
                                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                          "pm.request.headers.remove('Authorization');",
                                          "pm.request.headers.remove('X-Auth-Token');",
                                          "if (mode === 'basic') {",
                                          "  const username = pm.environment.get('USERNAME') || '';",
                                          "  const password = pm.environment.get('PASSWORD') || '';",
                                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                          "} else {",
                                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                          "    pm.execution.skipRequest();",
                                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                                          "  }",
                                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                          "}"
                                        ]
                                      }
                                    },
                                    {
                                      "listen": "test",
                                      "script": {
                                        "type": "text/javascript",
                                        "exec": [
                                          "pm.test('Response status is below 400', function () {",
                                          "  pm.expect(pm.response.code).to.be.below(400);",
                                          "});",
                                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                                          "if (pm.response.code !== 204) {",
                                          "  pm.test('Response is JSON', function () {",
                                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                          "  });",
                                          "}",
                                          ""
                                        ]
                                      }
                                    }
                                  ],
                                  "protocolProfileBehavior": {
                                    "followRedirects": false
                                  }
                                }
                              ],
                              "description": "URI: /redfish/v1/Managers/1/Oem/Supermicro/NodeManager/Triggers/CPUUtilization\nType: #NmTrigger.v1_0_0.NmTrigger"
                            },
                            {
                              "name": "HostReset",
                              "item": [
                                {
                                  "name": "GET Host Reset",
                                  "request": {
                                    "method": "GET",
                                    "header": [],
                                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/NodeManager/Triggers/HostReset",
                                    "description": "Evidence: allow-header"
                                  },
                                  "event": [
                                    {
                                      "listen": "prerequest",
                                      "script": {
                                        "type": "text/javascript",
                                        "exec": [
                                          "const urlTools = require('url');",
                                          "function parseURL(raw) {",
                                          "  const value = urlTools.parse(raw);",
                                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                          "  return value;",
                                          "}",
                                          "function targetBase() {",
                                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                          "}",
                                          "function authorizeSession(raw, baseURL) {",
                                          "  const base = targetBase();",
                                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                          "  let expanded = raw;",
                                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                          "  const decoded = decodeURIComponent(session.pathname || '');",
                                          "  if (session.origin !== base.origin || session.auth ||",
                                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                          "  return session;",
                                          "}",
                                          "",
                                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                          "pm.request.headers.remove('Authorization');",
                                          "pm.request.headers.remove('X-Auth-Token');",
                                          "if (mode === 'basic') {",
                                          "  const username = pm.environment.get('USERNAME') || '';",
                                          "  const password = pm.environment.get('PASSWORD') || '';",
                                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                          "} else {",
                                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                          "    pm.execution.skipRequest();",
                                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                                          "  }",
                                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                          "}"
                                        ]
                                      }
                                    },
                                    {
                                      "listen": "test",
                                      "script": {
                                        "type": "text/javascript",
                                        "exec": [
                                          "pm.test('Response status is below 400', function () {",
                                          "  pm.expect(pm.response.code).to.be.below(400);",
                                          "});",
                                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                                          "if (pm.response.code !== 204) {",
                                          "  pm.test('Response is JSON', function () {",
                                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                          "  });",
                                          "}",
                                          ""
                                        ]
                                      }
                                    }
                                  ],
                                  "protocolProfileBehavior": {
                                    "followRedirects": false
                                  }
                                }
                              ],
                              "description": "URI: /redfish/v1/Managers/1/Oem/Supermicro/NodeManager/Triggers/HostReset\nType: #NmTrigger.v1_0_0.NmTrigger"
                            },
                            {
                              "name": "InletTemperature",
                              "item": [
                                {
                                  "name": "GET Inlet Temperature",
                                  "request": {
                                    "method": "GET",
                                    "header": [],
                                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/NodeManager/Triggers/InletTemperature",
                                    "description": "Evidence: allow-header"
                                  },
                                  "event": [
                                    {
                                      "listen": "prerequest",
                                      "script": {
                                        "type": "text/javascript",
                                        "exec": [
                                          "const urlTools = require('url');",
                                          "function parseURL(raw) {",
                                          "  const value = urlTools.parse(raw);",
                                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                          "  return value;",
                                          "}",
                                          "function targetBase() {",
                                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                          "}",
                                          "function authorizeSession(raw, baseURL) {",
                                          "  const base = targetBase();",
                                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                          "  let expanded = raw;",
                                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                          "  const decoded = decodeURIComponent(session.pathname || '');",
                                          "  if (session.origin !== base.origin || session.auth ||",
                                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                          "  return session;",
                                          "}",
                                          "",
                                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                          "pm.request.headers.remove('Authorization');",
                                          "pm.request.headers.remove('X-Auth-Token');",
                                          "if (mode === 'basic') {",
                                          "  const username = pm.environment.get('USERNAME') || '';",
                                          "  const password = pm.environment.get('PASSWORD') || '';",
                                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                          "} else {",
                                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                          "    pm.execution.skipRequest();",
                                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                                          "  }",
                                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                          "}"
                                        ]
                                      }
                                    },
                                    {
                                      "listen": "test",
                                      "script": {
                                        "type": "text/javascript",
                                        "exec": [
                                          "pm.test('Response status is below 400', function () {",
                                          "  pm.expect(pm.response.code).to.be.below(400);",
                                          "});",
                                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                                          "if (pm.response.code !== 204) {",
                                          "  pm.test('Response is JSON', function () {",
                                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                          "  });",
                                          "}",
                                          ""
                                        ]
                                      }
                                    }
                                  ],
                                  "protocolProfileBehavior": {
                                    "followRedirects": false
                                  }
                                }
                              ],
                              "description": "URI: /redfish/v1/Managers/1/Oem/Supermicro/NodeManager/Triggers/InletTemperature\nType: #NmTrigger.v1_0_0.NmTrigger"
                            },
                            {
                              "name": "SMBAlertInterrupt",
                              "item": [
                                {
                                  "name": "GET SMBAlert",
                                  "request": {
                                    "method": "GET",
                                    "header": [],
                                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/NodeManager/Triggers/SMBAlertInterrupt",
                                    "description": "Evidence: allow-header"
                                  },
                                  "event": [
                                    {
                                      "listen": "prerequest",
                                      "script": {
                                        "type": "text/javascript",
                                        "exec": [
                                          "const urlTools = require('url');",
                                          "function parseURL(raw) {",
                                          "  const value = urlTools.parse(raw);",
                                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                          "  return value;",
                                          "}",
                                          "function targetBase() {",
                                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                          "}",
                                          "function authorizeSession(raw, baseURL) {",
                                          "  const base = targetBase();",
                                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                          "  let expanded = raw;",
                                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                          "  const decoded = decodeURIComponent(session.pathname || '');",
                                          "  if (session.origin !== base.origin || session.auth ||",
                                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                          "  return session;",
                                          "}",
                                          "",
                                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                          "pm.request.headers.remove('Authorization');",
                                          "pm.request.headers.remove('X-Auth-Token');",
                                          "if (mode === 'basic') {",
                                          "  const username = pm.environment.get('USERNAME') || '';",
                                          "  const password = pm.environment.get('PASSWORD') || '';",
                                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                          "} else {",
                                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                          "    pm.execution.skipRequest();",
                                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                                          "  }",
                                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                          "}"
                                        ]
                                      }
                                    },
                                    {
                                      "listen": "test",
                                      "script": {
                                        "type": "text/javascript",
                                        "exec": [
                                          "pm.test('Response status is below 400', function () {",
                                          "  pm.expect(pm.response.code).to.be.below(400);",
                                          "});",
                                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                                          "if (pm.response.code !== 204) {",
                                          "  pm.test('Response is JSON', function () {",
                                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                          "  });",
                                          "}",
                                          ""
                                        ]
                                      }
                                    }
                                  ],
                                  "protocolProfileBehavior": {
                                    "followRedirects": false
                                  }
                                }
                              ],
                              "description": "URI: /redfish/v1/Managers/1/Oem/Supermicro/NodeManager/Triggers/SMBAlertInterrupt\nType: #NmTrigger.v1_0_0.NmTrigger"
                            },
                            {
                              "name": "GET NM Triggers Collection",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/NodeManager/Triggers",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Managers/1/Oem/Supermicro/NodeManager/Triggers\nType: #NmTriggerCollection.NmTriggerCollection"
                        },
                        {
                          "name": "GET Node Manager",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/NodeManager",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        },
                        {
                          "name": "GET Node Manager",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/NodeManager/",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        },
                        {
                          "name": "PATCH Node Manager",
                          "request": {
                            "method": "PATCH",
                            "header": [
                              {
                                "key": "Content-Type",
                                "value": "application/json",
                                "type": "text"
                              },
                              {
                                "key": "If-Match",
                                "value": "\"39a0746d\"",
                                "type": "text"
                              }
                            ],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/NodeManager",
                            "description": "Evidence: allow-header",
                            "body": {
                              "mode": "raw",
                              "raw": "{}",
                              "options": {
                                "raw": {
                                  "language": "json"
                                }
                              }
                            }
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                  "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                  "    pm.execution.skipRequest();",
                                  "  } else {",
                                  "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                  "  }",
                                  "}",
                                  "",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        },
                        {
                          "name": "PATCH Node Manager",
                          "request": {
                            "method": "PATCH",
                            "header": [
                              {
                                "key": "Content-Type",
                                "value": "application/json",
                                "type": "text"
                              },
                              {
                                "key": "If-Match",
                                "value": "\"39a0746d\"",
                                "type": "text"
                              }
                            ],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/NodeManager/",
                            "description": "Evidence: allow-header",
                            "body": {
                              "mode": "raw",
                              "raw": "{}",
                              "options": {
                                "raw": {
                                  "language": "json"
                                }
                              }
                            }
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                  "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                  "    pm.execution.skipRequest();",
                                  "  } else {",
                                  "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                  "  }",
                                  "}",
                                  "",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Managers/1/Oem/Supermicro/NodeManager/\nType: #NodeManager.v1_0_0.NodeManager"
                    },
                    {
                      "name": "RADIUS",
                      "item": [
                        {
                          "name": "GET RADIUS",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/RADIUS",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        },
                        {
                          "name": "PATCH RADIUS",
                          "request": {
                            "method": "PATCH",
                            "header": [
                              {
                                "key": "Content-Type",
                                "value": "application/json",
                                "type": "text"
                              },
                              {
                                "key": "If-Match",
                                "value": "\"03432c44\"",
                                "type": "text"
                              }
                            ],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/RADIUS",
                            "description": "Evidence: allow-header",
                            "body": {
                              "mode": "raw",
                              "raw": "{}",
                              "options": {
                                "raw": {
                                  "language": "json"
                                }
                              }
                            }
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                  "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                  "    pm.execution.skipRequest();",
                                  "  } else {",
                                  "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                  "  }",
                                  "}",
                                  "",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Managers/1/Oem/Supermicro/RADIUS\nType: #RADIUS.v1_0_3.RADIUS"
                    },
                    {
                      "name": "SMCRAKP",
                      "item": [
                        {
                          "name": "GET SMCRAKP",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/SMCRAKP",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        },
                        {
                          "name": "PATCH SMCRAKP",
                          "request": {
                            "method": "PATCH",
                            "header": [
                              {
                                "key": "Content-Type",
                                "value": "application/json",
                                "type": "text"
                              },
                              {
                                "key": "If-Match",
                                "value": "\"6cb35353\"",
                                "type": "text"
                              }
                            ],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/SMCRAKP",
                            "description": "Evidence: allow-header",
                            "body": {
                              "mode": "raw",
                              "raw": "{}",
                              "options": {
                                "raw": {
                                  "language": "json"
                                }
                              }
                            }
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                  "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                  "    pm.execution.skipRequest();",
                                  "  } else {",
                                  "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                  "  }",
                                  "}",
                                  "",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Managers/1/Oem/Supermicro/SMCRAKP\nType: #SMCRAKP.v1_0_0.SMCRAKP"
                    },
                    {
                      "name": "Snooping",
                      "item": [
                        {
                          "name": "GET Snooping",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/Snooping",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Managers/1/Oem/Supermicro/Snooping\nType: #Snooping.v1_0_0.Snooping"
                    },
                    {
                      "name": "SysLockdown",
                      "item": [
                        {
                          "name": "GET SysLockdown",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/SysLockdown",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        },
                        {
                          "name": "PATCH SysLockdown",
                          "request": {
                            "method": "PATCH",
                            "header": [
                              {
                                "key": "Content-Type",
                                "value": "application/json",
                                "type": "text"
                              },
                              {
                                "key": "If-Match",
                                "value": "\"a94c97b5\"",
                                "type": "text"
                              }
                            ],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/SysLockdown",
                            "description": "Evidence: allow-header",
                            "body": {
                              "mode": "raw",
                              "raw": "{}",
                              "options": {
                                "raw": {
                                  "language": "json"
                                }
                              }
                            }
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                  "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                  "    pm.execution.skipRequest();",
                                  "  } else {",
                                  "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                  "  }",
                                  "}",
                                  "",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Managers/1/Oem/Supermicro/SysLockdown\nType: #SysLockdown.v1_0_0.SysLockdown"
                    },
                    {
                      "name": "Syslog",
                      "item": [
                        {
                          "name": "GET Syslog",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/Syslog",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        },
                        {
                          "name": "PATCH Syslog",
                          "request": {
                            "method": "PATCH",
                            "header": [
                              {
                                "key": "Content-Type",
                                "value": "application/json",
                                "type": "text"
                              },
                              {
                                "key": "If-Match",
                                "value": "\"fe5e405f\"",
                                "type": "text"
                              }
                            ],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Oem/Supermicro/Syslog",
                            "description": "Evidence: allow-header",
                            "body": {
                              "mode": "raw",
                              "raw": "{}",
                              "options": {
                                "raw": {
                                  "language": "json"
                                }
                              }
                            }
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                  "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                  "    pm.execution.skipRequest();",
                                  "  } else {",
                                  "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                  "  }",
                                  "}",
                                  "",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Managers/1/Oem/Supermicro/Syslog\nType: #Syslog.v1_0_1.Syslog"
                    }
                  ]
                }
              ]
            },
            {
              "name": "SecurityPolicy",
              "item": [
                {
                  "name": "SPDM",
                  "item": [
                    {
                      "name": "RevokedCertificates",
                      "item": [
                        {
                          "name": "GET Certificates Collection",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/SecurityPolicy/SPDM/RevokedCertificates",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Managers/1/SecurityPolicy/SPDM/RevokedCertificates\nType: #CertificateCollection.CertificateCollection"
                    },
                    {
                      "name": "TrustedCertificates",
                      "item": [
                        {
                          "name": "1",
                          "item": [
                            {
                              "name": "GET SPDM CA Certificate",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/SecurityPolicy/SPDM/TrustedCertificates/1",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Managers/1/SecurityPolicy/SPDM/TrustedCertificates/1\nType: #Certificate.v1_10_0.Certificate"
                        },
                        {
                          "name": "2",
                          "item": [
                            {
                              "name": "GET SPDM CA Certificate",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/SecurityPolicy/SPDM/TrustedCertificates/2",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Managers/1/SecurityPolicy/SPDM/TrustedCertificates/2\nType: #Certificate.v1_10_0.Certificate"
                        },
                        {
                          "name": "GET Certificates Collection",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/SecurityPolicy/SPDM/TrustedCertificates",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Managers/1/SecurityPolicy/SPDM/TrustedCertificates\nType: #CertificateCollection.CertificateCollection"
                    }
                  ]
                },
                {
                  "name": "GET ManagerGlobalSecurityPolicy",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/SecurityPolicy",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                },
                {
                  "name": "PATCH ManagerGlobalSecurityPolicy",
                  "request": {
                    "method": "PATCH",
                    "header": [
                      {
                        "key": "Content-Type",
                        "value": "application/json",
                        "type": "text"
                      },
                      {
                        "key": "If-Match",
                        "value": "\"d8e86fbd\"",
                        "type": "text"
                      }
                    ],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/SecurityPolicy",
                    "description": "Evidence: allow-header",
                    "body": {
                      "mode": "raw",
                      "raw": "{}",
                      "options": {
                        "raw": {
                          "language": "json"
                        }
                      }
                    }
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                          "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                          "    pm.execution.skipRequest();",
                          "  } else {",
                          "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                          "  }",
                          "}",
                          "",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/Managers/1/SecurityPolicy\nType: #SecurityPolicy.v1_0_0.SecurityPolicy"
            },
            {
              "name": "SerialInterfaces",
              "item": [
                {
                  "name": "1",
                  "item": [
                    {
                      "name": "GET SerialInterface",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/SerialInterfaces/1",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH SerialInterface",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"f2013dc4\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/SerialInterfaces/1",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Managers/1/SerialInterfaces/1\nType: #SerialInterface.v1_3_0.SerialInterface"
                },
                {
                  "name": "GET Serial Interface Collection",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/SerialInterfaces",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/Managers/1/SerialInterfaces\nType: #SerialInterfaceCollection.SerialInterfaceCollection"
            },
            {
              "name": "SharedNetworkPorts",
              "item": [
                {
                  "name": "GET SharedNetworkPorts Collection",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/SharedNetworkPorts",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/Managers/1/SharedNetworkPorts\nType: #PortCollection.PortCollection"
            },
            {
              "name": "Truststore",
              "item": [
                {
                  "name": "Certificates",
                  "item": [
                    {
                      "name": "GET TrustStore Certificates Collection",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Truststore/Certificates",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "GET TrustStore Certificates Collection",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/Truststore/Certificates/",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Managers/1/Truststore/Certificates/\nType: #CertificateCollection.CertificateCollection"
                }
              ]
            },
            {
              "name": "VirtualMedia",
              "item": [
                {
                  "name": "VirtualMedia1",
                  "item": [
                    {
                      "name": "Actions",
                      "item": [
                        {
                          "name": "POST VirtualMedia.EjectMedia",
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                  "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                  "    pm.execution.skipRequest();",
                                  "  } else {",
                                  "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                  "  }",
                                  "}",
                                  "",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            }
                          ],
                          "request": {
                            "method": "POST",
                            "header": [
                              {
                                "key": "Content-Type",
                                "value": "application/json",
                                "type": "text"
                              }
                            ],
                            "body": {
                              "mode": "raw",
                              "raw": "{}",
                              "options": {
                                "raw": {
                                  "language": "json"
                                }
                              }
                            },
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/VirtualMedia/VirtualMedia1/Actions/VirtualMedia.EjectMedia",
                            "description": "Redfish Action. Review the payload before explicitly enabling mutations."
                          },
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        },
                        {
                          "name": "POST VirtualMedia.InsertMedia",
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                  "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                  "    pm.execution.skipRequest();",
                                  "  } else {",
                                  "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                  "  }",
                                  "}",
                                  "",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            }
                          ],
                          "request": {
                            "method": "POST",
                            "header": [
                              {
                                "key": "Content-Type",
                                "value": "application/json",
                                "type": "text"
                              }
                            ],
                            "body": {
                              "mode": "raw",
                              "raw": "{\n  \"Image\": \"<REQUIRED>\",\n  \"UserName\": \"\",\n  \"Password\": \"\",\n  \"Inserted\": \"\",\n  \"TransferMethod\": \"\",\n  \"TransferProtocolType\": \"\",\n  \"WriteProtected\": \"\"\n}",
                              "options": {
                                "raw": {
                                  "language": "json"
                                }
                              }
                            },
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/VirtualMedia/VirtualMedia1/Actions/VirtualMedia.InsertMedia",
                            "description": "Redfish Action. Review the payload before explicitly enabling mutations."
                          },
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ]
                    },
                    {
                      "name": "GET Virtual Removable Media",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/VirtualMedia/VirtualMedia1",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH Virtual Removable Media",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"bed9483e\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/VirtualMedia/VirtualMedia1",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Managers/1/VirtualMedia/VirtualMedia1\nType: #VirtualMedia.v1_6_4.VirtualMedia"
                },
                {
                  "name": "VirtualMedia2",
                  "item": [
                    {
                      "name": "Actions",
                      "item": [
                        {
                          "name": "POST VirtualMedia.EjectMedia",
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                  "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                  "    pm.execution.skipRequest();",
                                  "  } else {",
                                  "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                  "  }",
                                  "}",
                                  "",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            }
                          ],
                          "request": {
                            "method": "POST",
                            "header": [
                              {
                                "key": "Content-Type",
                                "value": "application/json",
                                "type": "text"
                              }
                            ],
                            "body": {
                              "mode": "raw",
                              "raw": "{}",
                              "options": {
                                "raw": {
                                  "language": "json"
                                }
                              }
                            },
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/VirtualMedia/VirtualMedia2/Actions/VirtualMedia.EjectMedia",
                            "description": "Redfish Action. Review the payload before explicitly enabling mutations."
                          },
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        },
                        {
                          "name": "POST VirtualMedia.InsertMedia",
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                  "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                  "    pm.execution.skipRequest();",
                                  "  } else {",
                                  "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                  "  }",
                                  "}",
                                  "",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            }
                          ],
                          "request": {
                            "method": "POST",
                            "header": [
                              {
                                "key": "Content-Type",
                                "value": "application/json",
                                "type": "text"
                              }
                            ],
                            "body": {
                              "mode": "raw",
                              "raw": "{\n  \"Image\": \"<REQUIRED>\",\n  \"UserName\": \"\",\n  \"Password\": \"\",\n  \"Inserted\": \"\",\n  \"TransferMethod\": \"\",\n  \"TransferProtocolType\": \"\",\n  \"WriteProtected\": \"\"\n}",
                              "options": {
                                "raw": {
                                  "language": "json"
                                }
                              }
                            },
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/VirtualMedia/VirtualMedia2/Actions/VirtualMedia.InsertMedia",
                            "description": "Redfish Action. Review the payload before explicitly enabling mutations."
                          },
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ]
                    },
                    {
                      "name": "GET Virtual Removable Media",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/VirtualMedia/VirtualMedia2",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH Virtual Removable Media",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"a7157897\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/VirtualMedia/VirtualMedia2",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Managers/1/VirtualMedia/VirtualMedia2\nType: #VirtualMedia.v1_6_4.VirtualMedia"
                },
                {
                  "name": "VirtualMedia3",
                  "item": [
                    {
                      "name": "Actions",
                      "item": [
                        {
                          "name": "POST VirtualMedia.EjectMedia",
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                  "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                  "    pm.execution.skipRequest();",
                                  "  } else {",
                                  "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                  "  }",
                                  "}",
                                  "",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            }
                          ],
                          "request": {
                            "method": "POST",
                            "header": [
                              {
                                "key": "Content-Type",
                                "value": "application/json",
                                "type": "text"
                              }
                            ],
                            "body": {
                              "mode": "raw",
                              "raw": "{}",
                              "options": {
                                "raw": {
                                  "language": "json"
                                }
                              }
                            },
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/VirtualMedia/VirtualMedia3/Actions/VirtualMedia.EjectMedia",
                            "description": "Redfish Action. Review the payload before explicitly enabling mutations."
                          },
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        },
                        {
                          "name": "POST VirtualMedia.InsertMedia",
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                  "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                  "    pm.execution.skipRequest();",
                                  "  } else {",
                                  "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                  "  }",
                                  "}",
                                  "",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            }
                          ],
                          "request": {
                            "method": "POST",
                            "header": [
                              {
                                "key": "Content-Type",
                                "value": "application/json",
                                "type": "text"
                              }
                            ],
                            "body": {
                              "mode": "raw",
                              "raw": "{\n  \"Image\": \"<REQUIRED>\",\n  \"UserName\": \"\",\n  \"Password\": \"\",\n  \"Inserted\": \"\",\n  \"TransferMethod\": \"\",\n  \"TransferProtocolType\": \"\",\n  \"WriteProtected\": \"\"\n}",
                              "options": {
                                "raw": {
                                  "language": "json"
                                }
                              }
                            },
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/VirtualMedia/VirtualMedia3/Actions/VirtualMedia.InsertMedia",
                            "description": "Redfish Action. Review the payload before explicitly enabling mutations."
                          },
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ]
                    },
                    {
                      "name": "GET Virtual Removable Media",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/VirtualMedia/VirtualMedia3",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH Virtual Removable Media",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"251e3a39\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/VirtualMedia/VirtualMedia3",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Managers/1/VirtualMedia/VirtualMedia3\nType: #VirtualMedia.v1_6_4.VirtualMedia"
                },
                {
                  "name": "GET Virtual Media Services",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1/VirtualMedia",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/Managers/1/VirtualMedia\nType: #VirtualMediaCollection.VirtualMediaCollection"
            },
            {
              "name": "GET OpenBmc Manager",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            },
            {
              "name": "PATCH OpenBmc Manager",
              "request": {
                "method": "PATCH",
                "header": [
                  {
                    "key": "Content-Type",
                    "value": "application/json",
                    "type": "text"
                  },
                  {
                    "key": "If-Match",
                    "value": "\"881fb2d4\"",
                    "type": "text"
                  }
                ],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers/1",
                "description": "Evidence: allow-header",
                "body": {
                  "mode": "raw",
                  "raw": "{}",
                  "options": {
                    "raw": {
                      "language": "json"
                    }
                  }
                }
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                      "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                      "    pm.execution.skipRequest();",
                      "  } else {",
                      "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                      "  }",
                      "}",
                      "",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/Managers/1\nType: #Manager.v1_22_0.Manager"
        },
        {
          "name": "GET Manager Collection",
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Managers",
            "description": "Evidence: allow-header"
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const urlTools = require('url');",
                  "function parseURL(raw) {",
                  "  const value = urlTools.parse(raw);",
                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                  "  return value;",
                  "}",
                  "function targetBase() {",
                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                  "}",
                  "function authorizeSession(raw, baseURL) {",
                  "  const base = targetBase();",
                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                  "  let expanded = raw;",
                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                  "  const decoded = decodeURIComponent(session.pathname || '');",
                  "  if (session.origin !== base.origin || session.auth ||",
                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                  "  return session;",
                  "}",
                  "",
                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                  "pm.request.headers.remove('Authorization');",
                  "pm.request.headers.remove('X-Auth-Token');",
                  "if (mode === 'basic') {",
                  "  const username = pm.environment.get('USERNAME') || '';",
                  "  const password = pm.environment.get('PASSWORD') || '';",
                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                  "} else {",
                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                  "    pm.execution.skipRequest();",
                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                  "  }",
                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Response status is below 400', function () {",
                  "  pm.expect(pm.response.code).to.be.below(400);",
                  "});",
                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                  "if (pm.response.code !== 204) {",
                  "  pm.test('Response is JSON', function () {",
                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                  "  });",
                  "}",
                  ""
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        }
      ],
      "description": "URI: /redfish/v1/Managers\nType: #ManagerCollection.ManagerCollection"
    },
    {
      "name": "Oem",
      "item": [
        {
          "name": "Supermicro",
          "item": [
            {
              "name": "DumpService",
              "item": [
                {
                  "name": "Actions",
                  "item": [
                    {
                      "name": "POST OemDumpService.Collect",
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "request": {
                        "method": "POST",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          }
                        ],
                        "body": {
                          "mode": "raw",
                          "raw": "{\n  \"DumpType\": \"<REQUIRED>\",\n  \"ActionType\": \"<REQUIRED>\"\n}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        },
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Oem/Supermicro/DumpService/Actions/OemDumpService.Collect",
                        "description": "Redfish Action. Review the payload before explicitly enabling mutations."
                      },
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ]
                },
                {
                  "name": "Dumps",
                  "item": [
                    {
                      "name": "AttestationDump",
                      "item": [
                        {
                          "name": "GET AttestationDump",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Oem/Supermicro/DumpService/Dumps/AttestationDump",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        },
                        {
                          "name": "GET AttestationDump",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Oem/Supermicro/DumpService/Dumps/AttestationDump/",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Oem/Supermicro/DumpService/Dumps/AttestationDump/\nType: #Dump.v1_1_0.Dump"
                    },
                    {
                      "name": "GET Dump Collection",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Oem/Supermicro/DumpService/Dumps",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Oem/Supermicro/DumpService/Dumps\nType: #DumpCollection.DumpCollection"
                },
                {
                  "name": "GET Dump Service",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Oem/Supermicro/DumpService",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                },
                {
                  "name": "PATCH Dump Service",
                  "request": {
                    "method": "PATCH",
                    "header": [
                      {
                        "key": "Content-Type",
                        "value": "application/json",
                        "type": "text"
                      },
                      {
                        "key": "If-Match",
                        "value": "\"2d5e9eb6\"",
                        "type": "text"
                      }
                    ],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Oem/Supermicro/DumpService",
                    "description": "Evidence: allow-header",
                    "body": {
                      "mode": "raw",
                      "raw": "{}",
                      "options": {
                        "raw": {
                          "language": "json"
                        }
                      }
                    }
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                          "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                          "    pm.execution.skipRequest();",
                          "  } else {",
                          "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                          "  }",
                          "}",
                          "",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/Oem/Supermicro/DumpService\nType: #DumpService.v1_0_1.DumpService"
            }
          ]
        }
      ]
    },
    {
      "name": "Registries",
      "item": [
        {
          "name": "Base",
          "item": [
            {
              "name": "GET Base Message Registry File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Registries/Base",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/Registries/Base\nType: #MessageRegistryFile.v1_1_5.MessageRegistryFile"
        },
        {
          "name": "BiosAttributeRegistry",
          "item": [
            {
              "name": "GET BIOS Attribute Registry File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Registries/BiosAttributeRegistry",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/Registries/BiosAttributeRegistry\nType: #MessageRegistryFile.v1_1_5.MessageRegistryFile"
        },
        {
          "name": "Event",
          "item": [
            {
              "name": "GET Event Message Registry File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Registries/Event",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/Registries/Event\nType: #MessageRegistryFile.v1_1_5.MessageRegistryFile"
        },
        {
          "name": "OpenBMC",
          "item": [
            {
              "name": "GET OpenBMC Message Registry File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Registries/OpenBMC",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/Registries/OpenBMC\nType: #MessageRegistryFile.v1_1_5.MessageRegistryFile"
        },
        {
          "name": "ResourceEvent",
          "item": [
            {
              "name": "GET ResourceEvent Message Registry File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Registries/ResourceEvent",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/Registries/ResourceEvent\nType: #MessageRegistryFile.v1_1_5.MessageRegistryFile"
        },
        {
          "name": "SMC",
          "item": [
            {
              "name": "GET SMC Message Registry File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Registries/SMC",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/Registries/SMC\nType: #MessageRegistryFile.v1_1_5.MessageRegistryFile"
        },
        {
          "name": "TaskEvent",
          "item": [
            {
              "name": "GET TaskEvent Message Registry File",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Registries/TaskEvent",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/Registries/TaskEvent\nType: #MessageRegistryFile.v1_1_5.MessageRegistryFile"
        },
        {
          "name": "GET MessageRegistryFile Collection",
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Registries",
            "description": "Evidence: allow-header"
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const urlTools = require('url');",
                  "function parseURL(raw) {",
                  "  const value = urlTools.parse(raw);",
                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                  "  return value;",
                  "}",
                  "function targetBase() {",
                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                  "}",
                  "function authorizeSession(raw, baseURL) {",
                  "  const base = targetBase();",
                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                  "  let expanded = raw;",
                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                  "  const decoded = decodeURIComponent(session.pathname || '');",
                  "  if (session.origin !== base.origin || session.auth ||",
                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                  "  return session;",
                  "}",
                  "",
                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                  "pm.request.headers.remove('Authorization');",
                  "pm.request.headers.remove('X-Auth-Token');",
                  "if (mode === 'basic') {",
                  "  const username = pm.environment.get('USERNAME') || '';",
                  "  const password = pm.environment.get('PASSWORD') || '';",
                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                  "} else {",
                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                  "    pm.execution.skipRequest();",
                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                  "  }",
                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Response status is below 400', function () {",
                  "  pm.expect(pm.response.code).to.be.below(400);",
                  "});",
                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                  "if (pm.response.code !== 204) {",
                  "  pm.test('Response is JSON', function () {",
                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                  "  });",
                  "}",
                  ""
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        }
      ],
      "description": "URI: /redfish/v1/Registries\nType: #MessageRegistryFileCollection.MessageRegistryFileCollection"
    },
    {
      "name": "ServiceRoot",
      "item": [
        {
          "name": "GET Root Service",
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1",
            "description": "Evidence: allow-header"
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const urlTools = require('url');",
                  "function parseURL(raw) {",
                  "  const value = urlTools.parse(raw);",
                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                  "  return value;",
                  "}",
                  "function targetBase() {",
                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                  "}",
                  "function authorizeSession(raw, baseURL) {",
                  "  const base = targetBase();",
                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                  "  let expanded = raw;",
                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                  "  const decoded = decodeURIComponent(session.pathname || '');",
                  "  if (session.origin !== base.origin || session.auth ||",
                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                  "  return session;",
                  "}",
                  "",
                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                  "pm.request.headers.remove('Authorization');",
                  "pm.request.headers.remove('X-Auth-Token');",
                  "if (mode === 'basic') {",
                  "  const username = pm.environment.get('USERNAME') || '';",
                  "  const password = pm.environment.get('PASSWORD') || '';",
                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                  "} else {",
                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                  "    pm.execution.skipRequest();",
                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                  "  }",
                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Response status is below 400', function () {",
                  "  pm.expect(pm.response.code).to.be.below(400);",
                  "});",
                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                  "if (pm.response.code !== 204) {",
                  "  pm.test('Response is JSON', function () {",
                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                  "  });",
                  "}",
                  ""
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "GET Root Service",
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/",
            "description": "Evidence: allow-header"
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const urlTools = require('url');",
                  "function parseURL(raw) {",
                  "  const value = urlTools.parse(raw);",
                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                  "  return value;",
                  "}",
                  "function targetBase() {",
                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                  "}",
                  "function authorizeSession(raw, baseURL) {",
                  "  const base = targetBase();",
                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                  "  let expanded = raw;",
                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                  "  const decoded = decodeURIComponent(session.pathname || '');",
                  "  if (session.origin !== base.origin || session.auth ||",
                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                  "  return session;",
                  "}",
                  "",
                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                  "pm.request.headers.remove('Authorization');",
                  "pm.request.headers.remove('X-Auth-Token');",
                  "if (mode === 'basic') {",
                  "  const username = pm.environment.get('USERNAME') || '';",
                  "  const password = pm.environment.get('PASSWORD') || '';",
                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                  "} else {",
                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                  "    pm.execution.skipRequest();",
                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                  "  }",
                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Response status is below 400', function () {",
                  "  pm.expect(pm.response.code).to.be.below(400);",
                  "});",
                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                  "if (pm.response.code !== 204) {",
                  "  pm.test('Response is JSON', function () {",
                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                  "  });",
                  "}",
                  ""
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        }
      ],
      "description": "URI: /redfish/v1/\nType: #ServiceRoot.v1_19_0.ServiceRoot"
    },
    {
      "name": "SessionService",
      "item": [
        {
          "name": "Sessions",
          "item": [
            {
              "name": "GET Session Collection",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/SessionService/Sessions",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/SessionService/Sessions\nType: #SessionCollection.SessionCollection"
        },
        {
          "name": "GET Session Service",
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/SessionService",
            "description": "Evidence: allow-header"
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const urlTools = require('url');",
                  "function parseURL(raw) {",
                  "  const value = urlTools.parse(raw);",
                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                  "  return value;",
                  "}",
                  "function targetBase() {",
                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                  "}",
                  "function authorizeSession(raw, baseURL) {",
                  "  const base = targetBase();",
                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                  "  let expanded = raw;",
                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                  "  const decoded = decodeURIComponent(session.pathname || '');",
                  "  if (session.origin !== base.origin || session.auth ||",
                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                  "  return session;",
                  "}",
                  "",
                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                  "pm.request.headers.remove('Authorization');",
                  "pm.request.headers.remove('X-Auth-Token');",
                  "if (mode === 'basic') {",
                  "  const username = pm.environment.get('USERNAME') || '';",
                  "  const password = pm.environment.get('PASSWORD') || '';",
                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                  "} else {",
                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                  "    pm.execution.skipRequest();",
                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                  "  }",
                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Response status is below 400', function () {",
                  "  pm.expect(pm.response.code).to.be.below(400);",
                  "});",
                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                  "if (pm.response.code !== 204) {",
                  "  pm.test('Response is JSON', function () {",
                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                  "  });",
                  "}",
                  ""
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "PATCH Session Service",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "If-Match",
                "value": "\"92a937ee\"",
                "type": "text"
              }
            ],
            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/SessionService",
            "description": "Evidence: allow-header",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const urlTools = require('url');",
                  "function parseURL(raw) {",
                  "  const value = urlTools.parse(raw);",
                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                  "  return value;",
                  "}",
                  "function targetBase() {",
                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                  "}",
                  "function authorizeSession(raw, baseURL) {",
                  "  const base = targetBase();",
                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                  "  let expanded = raw;",
                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                  "  const decoded = decodeURIComponent(session.pathname || '');",
                  "  if (session.origin !== base.origin || session.auth ||",
                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                  "  return session;",
                  "}",
                  "",
                  "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                  "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                  "    pm.execution.skipRequest();",
                  "  } else {",
                  "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                  "  }",
                  "}",
                  "",
                  "",
                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                  "pm.request.headers.remove('Authorization');",
                  "pm.request.headers.remove('X-Auth-Token');",
                  "if (mode === 'basic') {",
                  "  const username = pm.environment.get('USERNAME') || '';",
                  "  const password = pm.environment.get('PASSWORD') || '';",
                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                  "} else {",
                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                  "    pm.execution.skipRequest();",
                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                  "  }",
                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                  "}"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        }
      ],
      "description": "URI: /redfish/v1/SessionService\nType: #SessionService.v1_1_9.SessionService"
    },
    {
      "name": "Systems",
      "item": [
        {
          "name": "1",
          "item": [
            {
              "name": "Actions",
              "item": [
                {
                  "name": "POST ComputerSystem.Decommission",
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                          "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                          "    pm.execution.skipRequest();",
                          "  } else {",
                          "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                          "  }",
                          "}",
                          "",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    }
                  ],
                  "request": {
                    "method": "POST",
                    "header": [
                      {
                        "key": "Content-Type",
                        "value": "application/json",
                        "type": "text"
                      }
                    ],
                    "body": {
                      "mode": "raw",
                      "raw": "{\n  \"OEMDecommissionTypes\": \"<REQUIRED>\"\n}",
                      "options": {
                        "raw": {
                          "language": "json"
                        }
                      }
                    },
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/Actions/ComputerSystem.Decommission",
                    "description": "Redfish Action. Review the payload before explicitly enabling mutations."
                  },
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                },
                {
                  "name": "POST ComputerSystem.Reset",
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                          "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                          "    pm.execution.skipRequest();",
                          "  } else {",
                          "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                          "  }",
                          "}",
                          "",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    }
                  ],
                  "request": {
                    "method": "POST",
                    "header": [
                      {
                        "key": "Content-Type",
                        "value": "application/json",
                        "type": "text"
                      }
                    ],
                    "body": {
                      "mode": "raw",
                      "raw": "{\n  \"ResetType\": \"<REQUIRED>\"\n}",
                      "options": {
                        "raw": {
                          "language": "json"
                        }
                      }
                    },
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/Actions/ComputerSystem.Reset",
                    "description": "Redfish Action. Review the payload before explicitly enabling mutations."
                  },
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ]
            },
            {
              "name": "Bios",
              "item": [
                {
                  "name": "Actions",
                  "item": [
                    {
                      "name": "POST Bios.ChangePassword",
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "request": {
                        "method": "POST",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          }
                        ],
                        "body": {
                          "mode": "raw",
                          "raw": "{\n  \"PasswordName\": \"<REQUIRED>\",\n  \"OldPassword\": \"<REQUIRED>\",\n  \"NewPassword\": \"<REQUIRED>\"\n}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        },
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/Bios/Actions/Bios.ChangePassword",
                        "description": "Redfish Action. Review the payload before explicitly enabling mutations."
                      },
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "POST Bios.ResetBios",
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "request": {
                        "method": "POST",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          }
                        ],
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        },
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/Bios/Actions/Bios.ResetBios",
                        "description": "Redfish Action. Review the payload before explicitly enabling mutations."
                      },
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ]
                },
                {
                  "name": "SD",
                  "item": [
                    {
                      "name": "GET BIOS Configuration Pending Settings",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/Bios/SD",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH BIOS Configuration Pending Settings",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"e42767d4\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/Bios/SD",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Systems/1/Bios/SD\nType: #Bios.v1_2_3.Bios"
                },
                {
                  "name": "GET BIOS Configuration Current Settings",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/Bios",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/Systems/1/Bios\nType: #Bios.v1_2_3.Bios"
            },
            {
              "name": "Boot",
              "item": [
                {
                  "name": "Certificates",
                  "item": [
                    {
                      "name": "GET Certificate Collection",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/Boot/Certificates",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Systems/1/Boot/Certificates\nType: #CertificateCollection.CertificateCollection"
                }
              ]
            },
            {
              "name": "BootOptions",
              "item": [
                {
                  "name": "Boot0001",
                  "item": [
                    {
                      "name": "GET Boot Option",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/BootOptions/Boot0001",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH Boot Option",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"f24945b0\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/BootOptions/Boot0001",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Systems/1/BootOptions/Boot0001\nType: #BootOption.v1_0_6.BootOption"
                },
                {
                  "name": "Boot0007",
                  "item": [
                    {
                      "name": "GET Boot Option",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/BootOptions/Boot0007",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH Boot Option",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"bb59081f\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/BootOptions/Boot0007",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Systems/1/BootOptions/Boot0007\nType: #BootOption.v1_0_6.BootOption"
                },
                {
                  "name": "Boot0008",
                  "item": [
                    {
                      "name": "GET Boot Option",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/BootOptions/Boot0008",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH Boot Option",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"375bffa8\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/BootOptions/Boot0008",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Systems/1/BootOptions/Boot0008\nType: #BootOption.v1_0_6.BootOption"
                },
                {
                  "name": "Boot0009",
                  "item": [
                    {
                      "name": "GET Boot Option",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/BootOptions/Boot0009",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH Boot Option",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"c2d5e442\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/BootOptions/Boot0009",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Systems/1/BootOptions/Boot0009\nType: #BootOption.v1_0_6.BootOption"
                },
                {
                  "name": "Boot000A",
                  "item": [
                    {
                      "name": "GET Boot Option",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/BootOptions/Boot000A",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH Boot Option",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"295876cc\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/BootOptions/Boot000A",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Systems/1/BootOptions/Boot000A\nType: #BootOption.v1_0_6.BootOption"
                },
                {
                  "name": "Boot000C",
                  "item": [
                    {
                      "name": "GET Boot Option",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/BootOptions/Boot000C",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH Boot Option",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"16e96b9b\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/BootOptions/Boot000C",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Systems/1/BootOptions/Boot000C\nType: #BootOption.v1_0_6.BootOption"
                },
                {
                  "name": "GET Boot Option Collection",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/BootOptions",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/Systems/1/BootOptions\nType: #BootOptionCollection.BootOptionCollection"
            },
            {
              "name": "EthernetInterfaces",
              "item": [
                {
                  "name": "1",
                  "item": [
                    {
                      "name": "GET OnBoard_NIC",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/EthernetInterfaces/1",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Systems/1/EthernetInterfaces/1\nType: #EthernetInterface.v1_12_2.EthernetInterface"
                },
                {
                  "name": "2",
                  "item": [
                    {
                      "name": "GET OnBoard_NIC",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/EthernetInterfaces/2",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Systems/1/EthernetInterfaces/2\nType: #EthernetInterface.v1_12_2.EthernetInterface"
                },
                {
                  "name": "ToManager",
                  "item": [
                    {
                      "name": "GET Host Ethernet Interface",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/EthernetInterfaces/ToManager",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH Host Ethernet Interface",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"127102e9\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/EthernetInterfaces/ToManager",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Systems/1/EthernetInterfaces/ToManager\nType: #EthernetInterface.v1_12_2.EthernetInterface"
                },
                {
                  "name": "GET Ethernet Network Interface Collection",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/EthernetInterfaces",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/Systems/1/EthernetInterfaces\nType: #EthernetInterfaceCollection.EthernetInterfaceCollection"
            },
            {
              "name": "LogServices",
              "item": [
                {
                  "name": "Crashdump",
                  "item": [
                    {
                      "name": "Actions",
                      "item": [
                        {
                          "name": "POST LogService.ClearLog",
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                  "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                  "    pm.execution.skipRequest();",
                                  "  } else {",
                                  "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                  "  }",
                                  "}",
                                  "",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            }
                          ],
                          "request": {
                            "method": "POST",
                            "header": [
                              {
                                "key": "Content-Type",
                                "value": "application/json",
                                "type": "text"
                              }
                            ],
                            "body": {
                              "mode": "raw",
                              "raw": "{}",
                              "options": {
                                "raw": {
                                  "language": "json"
                                }
                              }
                            },
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/LogServices/Crashdump/Actions/LogService.ClearLog",
                            "description": "Redfish Action. Review the payload before explicitly enabling mutations."
                          },
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        },
                        {
                          "name": "POST LogService.CollectDiagnosticData",
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                  "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                  "    pm.execution.skipRequest();",
                                  "  } else {",
                                  "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                  "  }",
                                  "}",
                                  "",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            }
                          ],
                          "request": {
                            "method": "POST",
                            "header": [
                              {
                                "key": "Content-Type",
                                "value": "application/json",
                                "type": "text"
                              }
                            ],
                            "body": {
                              "mode": "raw",
                              "raw": "{}",
                              "options": {
                                "raw": {
                                  "language": "json"
                                }
                              }
                            },
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/LogServices/Crashdump/Actions/LogService.CollectDiagnosticData",
                            "description": "Redfish Action. Review the payload before explicitly enabling mutations."
                          },
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ]
                    },
                    {
                      "name": "Entries",
                      "item": [
                        {
                          "name": "GET Open BMC Crashdump Entries",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/LogServices/Crashdump/Entries",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Systems/1/LogServices/Crashdump/Entries\nType: #LogEntryCollection.LogEntryCollection"
                    },
                    {
                      "name": "GET Open BMC Oem Crashdump Service",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/LogServices/Crashdump",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Systems/1/LogServices/Crashdump\nType: #LogService.v1_8_0.LogService"
                },
                {
                  "name": "Dump",
                  "item": [
                    {
                      "name": "Actions",
                      "item": [
                        {
                          "name": "POST LogService.ClearLog",
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                  "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                  "    pm.execution.skipRequest();",
                                  "  } else {",
                                  "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                  "  }",
                                  "}",
                                  "",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            }
                          ],
                          "request": {
                            "method": "POST",
                            "header": [
                              {
                                "key": "Content-Type",
                                "value": "application/json",
                                "type": "text"
                              }
                            ],
                            "body": {
                              "mode": "raw",
                              "raw": "{}",
                              "options": {
                                "raw": {
                                  "language": "json"
                                }
                              }
                            },
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/LogServices/Dump/Actions/LogService.ClearLog",
                            "description": "Redfish Action. Review the payload before explicitly enabling mutations."
                          },
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        },
                        {
                          "name": "POST LogService.CollectDiagnosticData",
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                  "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                  "    pm.execution.skipRequest();",
                                  "  } else {",
                                  "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                  "  }",
                                  "}",
                                  "",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            }
                          ],
                          "request": {
                            "method": "POST",
                            "header": [
                              {
                                "key": "Content-Type",
                                "value": "application/json",
                                "type": "text"
                              }
                            ],
                            "body": {
                              "mode": "raw",
                              "raw": "{\n  \"DiagnosticDataType\": \"<REQUIRED>\",\n  \"OEMDiagnosticDataType\": \"\"\n}",
                              "options": {
                                "raw": {
                                  "language": "json"
                                }
                              }
                            },
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/LogServices/Dump/Actions/LogService.CollectDiagnosticData",
                            "description": "Redfish Action. Review the payload before explicitly enabling mutations."
                          },
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ]
                    },
                    {
                      "name": "Entries",
                      "item": [
                        {
                          "name": "GET Dump Log Entry Collection",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/LogServices/Dump/Entries",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Systems/1/LogServices/Dump/Entries\nType: #LogEntryCollection.LogEntryCollection"
                    },
                    {
                      "name": "GET Dump LogService",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/LogServices/Dump",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Systems/1/LogServices/Dump\nType: #LogService.v1_8_0.LogService"
                },
                {
                  "name": "HostLogger",
                  "item": [
                    {
                      "name": "Entries",
                      "item": [
                        {
                          "name": "GET HostLogger Entries",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/LogServices/HostLogger/Entries",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Systems/1/LogServices/HostLogger/Entries\nType: #LogEntryCollection.LogEntryCollection"
                    },
                    {
                      "name": "GET Host Logger Service",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/LogServices/HostLogger",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Systems/1/LogServices/HostLogger\nType: #LogService.v1_8_0.LogService"
                },
                {
                  "name": "Log1",
                  "item": [
                    {
                      "name": "Actions",
                      "item": [
                        {
                          "name": "POST LogService.ClearLog",
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                  "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                  "    pm.execution.skipRequest();",
                                  "  } else {",
                                  "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                  "  }",
                                  "}",
                                  "",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            }
                          ],
                          "request": {
                            "method": "POST",
                            "header": [
                              {
                                "key": "Content-Type",
                                "value": "application/json",
                                "type": "text"
                              }
                            ],
                            "body": {
                              "mode": "raw",
                              "raw": "{}",
                              "options": {
                                "raw": {
                                  "language": "json"
                                }
                              }
                            },
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/LogServices/Log1/Actions/LogService.ClearLog",
                            "description": "Redfish Action. Review the payload before explicitly enabling mutations."
                          },
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ]
                    },
                    {
                      "name": "Entries",
                      "item": [
                        {
                          "name": "1",
                          "item": [
                            {
                              "name": "GET Health Event Log Entry 1",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/LogServices/Log1/Entries/1",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            },
                            {
                              "name": "PATCH Health Event Log Entry 1",
                              "request": {
                                "method": "PATCH",
                                "header": [
                                  {
                                    "key": "Content-Type",
                                    "value": "application/json",
                                    "type": "text"
                                  },
                                  {
                                    "key": "If-Match",
                                    "value": "\"b6fdf9fe\"",
                                    "type": "text"
                                  }
                                ],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/LogServices/Log1/Entries/1",
                                "description": "Evidence: allow-header",
                                "body": {
                                  "mode": "raw",
                                  "raw": "{}",
                                  "options": {
                                    "raw": {
                                      "language": "json"
                                    }
                                  }
                                }
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                      "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                      "    pm.execution.skipRequest();",
                                      "  } else {",
                                      "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                      "  }",
                                      "}",
                                      "",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Systems/1/LogServices/Log1/Entries/1\nType: #LogEntry.v1_19_0.LogEntry"
                        },
                        {
                          "name": "2",
                          "item": [
                            {
                              "name": "GET Health Event Log Entry 2",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/LogServices/Log1/Entries/2",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            },
                            {
                              "name": "PATCH Health Event Log Entry 2",
                              "request": {
                                "method": "PATCH",
                                "header": [
                                  {
                                    "key": "Content-Type",
                                    "value": "application/json",
                                    "type": "text"
                                  },
                                  {
                                    "key": "If-Match",
                                    "value": "\"f68c7c60\"",
                                    "type": "text"
                                  }
                                ],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/LogServices/Log1/Entries/2",
                                "description": "Evidence: allow-header",
                                "body": {
                                  "mode": "raw",
                                  "raw": "{}",
                                  "options": {
                                    "raw": {
                                      "language": "json"
                                    }
                                  }
                                }
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                      "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                      "    pm.execution.skipRequest();",
                                      "  } else {",
                                      "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                      "  }",
                                      "}",
                                      "",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Systems/1/LogServices/Log1/Entries/2\nType: #LogEntry.v1_19_0.LogEntry"
                        },
                        {
                          "name": "3",
                          "item": [
                            {
                              "name": "GET Health Event Log Entry 3",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/LogServices/Log1/Entries/3",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            },
                            {
                              "name": "PATCH Health Event Log Entry 3",
                              "request": {
                                "method": "PATCH",
                                "header": [
                                  {
                                    "key": "Content-Type",
                                    "value": "application/json",
                                    "type": "text"
                                  },
                                  {
                                    "key": "If-Match",
                                    "value": "\"b9794c74\"",
                                    "type": "text"
                                  }
                                ],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/LogServices/Log1/Entries/3",
                                "description": "Evidence: allow-header",
                                "body": {
                                  "mode": "raw",
                                  "raw": "{}",
                                  "options": {
                                    "raw": {
                                      "language": "json"
                                    }
                                  }
                                }
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                      "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                      "    pm.execution.skipRequest();",
                                      "  } else {",
                                      "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                      "  }",
                                      "}",
                                      "",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Systems/1/LogServices/Log1/Entries/3\nType: #LogEntry.v1_19_0.LogEntry"
                        },
                        {
                          "name": "4",
                          "item": [
                            {
                              "name": "GET Health Event Log Entry 4",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/LogServices/Log1/Entries/4",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            },
                            {
                              "name": "PATCH Health Event Log Entry 4",
                              "request": {
                                "method": "PATCH",
                                "header": [
                                  {
                                    "key": "Content-Type",
                                    "value": "application/json",
                                    "type": "text"
                                  },
                                  {
                                    "key": "If-Match",
                                    "value": "\"9895d752\"",
                                    "type": "text"
                                  }
                                ],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/LogServices/Log1/Entries/4",
                                "description": "Evidence: allow-header",
                                "body": {
                                  "mode": "raw",
                                  "raw": "{}",
                                  "options": {
                                    "raw": {
                                      "language": "json"
                                    }
                                  }
                                }
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                      "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                      "    pm.execution.skipRequest();",
                                      "  } else {",
                                      "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                      "  }",
                                      "}",
                                      "",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Systems/1/LogServices/Log1/Entries/4\nType: #LogEntry.v1_19_0.LogEntry"
                        },
                        {
                          "name": "GET Health Event Log Service Collection",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/LogServices/Log1/Entries",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Systems/1/LogServices/Log1/Entries\nType: #LogEntryCollection.LogEntryCollection"
                    },
                    {
                      "name": "GET Health Event Log Service",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/LogServices/Log1",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    },
                    {
                      "name": "PATCH Health Event Log Service",
                      "request": {
                        "method": "PATCH",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          },
                          {
                            "key": "If-Match",
                            "value": "\"8fdff8ec\"",
                            "type": "text"
                          }
                        ],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/LogServices/Log1",
                        "description": "Evidence: allow-header",
                        "body": {
                          "mode": "raw",
                          "raw": "{}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        }
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Systems/1/LogServices/Log1\nType: #LogService.v1_8_0.LogService"
                },
                {
                  "name": "Log2",
                  "item": [
                    {
                      "name": "Actions",
                      "item": [
                        {
                          "name": "POST LogService.ClearLog",
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                  "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                  "    pm.execution.skipRequest();",
                                  "  } else {",
                                  "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                  "  }",
                                  "}",
                                  "",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            }
                          ],
                          "request": {
                            "method": "POST",
                            "header": [
                              {
                                "key": "Content-Type",
                                "value": "application/json",
                                "type": "text"
                              }
                            ],
                            "body": {
                              "mode": "raw",
                              "raw": "{}",
                              "options": {
                                "raw": {
                                  "language": "json"
                                }
                              }
                            },
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/LogServices/Log2/Actions/LogService.ClearLog",
                            "description": "Redfish Action. Review the payload before explicitly enabling mutations."
                          },
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ]
                    },
                    {
                      "name": "Entries",
                      "item": [
                        {
                          "name": "1",
                          "item": [
                            {
                              "name": "GET Health Event Log Entry 1",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/LogServices/Log2/Entries/1",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Systems/1/LogServices/Log2/Entries/1\nType: #LogEntry.v1_17_0.LogEntry"
                        },
                        {
                          "name": "2",
                          "item": [
                            {
                              "name": "GET Health Event Log Entry 2",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/LogServices/Log2/Entries/2",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Systems/1/LogServices/Log2/Entries/2\nType: #LogEntry.v1_17_0.LogEntry"
                        },
                        {
                          "name": "3",
                          "item": [
                            {
                              "name": "GET Health Event Log Entry 3",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/LogServices/Log2/Entries/3",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Systems/1/LogServices/Log2/Entries/3\nType: #LogEntry.v1_17_0.LogEntry"
                        },
                        {
                          "name": "4",
                          "item": [
                            {
                              "name": "GET Health Event Log Entry 4",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/LogServices/Log2/Entries/4",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Systems/1/LogServices/Log2/Entries/4\nType: #LogEntry.v1_17_0.LogEntry"
                        },
                        {
                          "name": "GET Health Event Log Service Collection",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/LogServices/Log2/Entries",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Systems/1/LogServices/Log2/Entries\nType: #LogEntryCollection.LogEntryCollection"
                    },
                    {
                      "name": "GET Health Event Log Service",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/LogServices/Log2",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Systems/1/LogServices/Log2\nType: #LogService.v1_7_0.LogService"
                },
                {
                  "name": "PostCodes",
                  "item": [
                    {
                      "name": "Actions",
                      "item": [
                        {
                          "name": "POST LogService.ClearLog",
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                  "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                  "    pm.execution.skipRequest();",
                                  "  } else {",
                                  "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                  "  }",
                                  "}",
                                  "",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            }
                          ],
                          "request": {
                            "method": "POST",
                            "header": [
                              {
                                "key": "Content-Type",
                                "value": "application/json",
                                "type": "text"
                              }
                            ],
                            "body": {
                              "mode": "raw",
                              "raw": "{}",
                              "options": {
                                "raw": {
                                  "language": "json"
                                }
                              }
                            },
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/LogServices/PostCodes/Actions/LogService.ClearLog",
                            "description": "Redfish Action. Review the payload before explicitly enabling mutations."
                          },
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ]
                    },
                    {
                      "name": "Entries",
                      "item": [
                        {
                          "name": "GET BIOS POST Code Log Entries",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/LogServices/PostCodes/Entries",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Systems/1/LogServices/PostCodes/Entries\nType: #LogEntryCollection.LogEntryCollection"
                    },
                    {
                      "name": "GET POST Code Log Service",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/LogServices/PostCodes",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Systems/1/LogServices/PostCodes\nType: #LogService.v1_8_0.LogService"
                },
                {
                  "name": "GET System Log Services Collection",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/LogServices",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/Systems/1/LogServices\nType: #LogServiceCollection.LogServiceCollection"
            },
            {
              "name": "Memory",
              "item": [
                {
                  "name": "1",
                  "item": [
                    {
                      "name": "EnvironmentMetrics",
                      "item": [
                        {
                          "name": "GET Environment Metrics",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/Memory/1/EnvironmentMetrics",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Systems/1/Memory/1/EnvironmentMetrics\nType: #EnvironmentMetrics.v1_5_0.EnvironmentMetrics"
                    },
                    {
                      "name": "MemoryMetrics",
                      "item": [
                        {
                          "name": "GET MemoryMetrics",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/Memory/1/MemoryMetrics",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Systems/1/Memory/1/MemoryMetrics\nType: #MemoryMetrics.v1_7_3.MemoryMetrics"
                    },
                    {
                      "name": "GET Memory",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/Memory/1",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Systems/1/Memory/1\nType: #Memory.v1_21_0.Memory"
                },
                {
                  "name": "2",
                  "item": [
                    {
                      "name": "EnvironmentMetrics",
                      "item": [
                        {
                          "name": "GET Environment Metrics",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/Memory/2/EnvironmentMetrics",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Systems/1/Memory/2/EnvironmentMetrics\nType: #EnvironmentMetrics.v1_5_0.EnvironmentMetrics"
                    },
                    {
                      "name": "MemoryMetrics",
                      "item": [
                        {
                          "name": "GET MemoryMetrics",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/Memory/2/MemoryMetrics",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Systems/1/Memory/2/MemoryMetrics\nType: #MemoryMetrics.v1_7_3.MemoryMetrics"
                    },
                    {
                      "name": "GET Memory",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/Memory/2",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Systems/1/Memory/2\nType: #Memory.v1_21_0.Memory"
                },
                {
                  "name": "GET Memory Collection",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/Memory",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/Systems/1/Memory\nType: #MemoryCollection.MemoryCollection"
            },
            {
              "name": "MemorySummary",
              "item": [
                {
                  "name": "MemoryMetrics",
                  "item": [
                    {
                      "name": "GET MemoryMetrics",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/MemorySummary/MemoryMetrics",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Systems/1/MemorySummary/MemoryMetrics\nType: #MemoryMetrics.v1_7_3.MemoryMetrics"
                }
              ]
            },
            {
              "name": "NetworkInterfaces",
              "item": [
                {
                  "name": "GET Network Interface Collection",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/NetworkInterfaces",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/Systems/1/NetworkInterfaces\nType: #NetworkInterfaceCollection.NetworkInterfaceCollection"
            },
            {
              "name": "Oem",
              "item": [
                {
                  "name": "Supermicro",
                  "item": [
                    {
                      "name": "FixedBootOrder",
                      "item": [
                        {
                          "name": "GET Fixed Boot Order",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/Oem/Supermicro/FixedBootOrder",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        },
                        {
                          "name": "PATCH Fixed Boot Order",
                          "request": {
                            "method": "PATCH",
                            "header": [
                              {
                                "key": "Content-Type",
                                "value": "application/json",
                                "type": "text"
                              },
                              {
                                "key": "If-Match",
                                "value": "\"2c7b2856\"",
                                "type": "text"
                              }
                            ],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/Oem/Supermicro/FixedBootOrder",
                            "description": "Evidence: allow-header",
                            "body": {
                              "mode": "raw",
                              "raw": "{}",
                              "options": {
                                "raw": {
                                  "language": "json"
                                }
                              }
                            }
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                  "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                  "    pm.execution.skipRequest();",
                                  "  } else {",
                                  "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                  "  }",
                                  "}",
                                  "",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Systems/1/Oem/Supermicro/FixedBootOrder\nType: #SmcFixedBootOrder.v1_0_0.SmcFixedBootOrder"
                    }
                  ]
                }
              ]
            },
            {
              "name": "ProcessorSummary",
              "item": [
                {
                  "name": "ProcessorMetrics",
                  "item": [
                    {
                      "name": "GET ProcessorMetrics",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/ProcessorSummary/ProcessorMetrics",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Systems/1/ProcessorSummary/ProcessorMetrics\nType: #ProcessorMetrics.v1_6_4.ProcessorMetrics"
                }
              ]
            },
            {
              "name": "Processors",
              "item": [
                {
                  "name": "1",
                  "item": [
                    {
                      "name": "EnvironmentMetrics",
                      "item": [
                        {
                          "name": "GET Environment Metrics",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/Processors/1/EnvironmentMetrics",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Systems/1/Processors/1/EnvironmentMetrics\nType: #EnvironmentMetrics.v1_5_0.EnvironmentMetrics"
                    },
                    {
                      "name": "Ports",
                      "item": [
                        {
                          "name": "GET Processor Port Collection",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/Processors/1/Ports",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Systems/1/Processors/1/Ports\nType: #PortCollection.PortCollection"
                    },
                    {
                      "name": "ProcessorMetrics",
                      "item": [
                        {
                          "name": "GET ProcessorMetrics",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/Processors/1/ProcessorMetrics",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Systems/1/Processors/1/ProcessorMetrics\nType: #ProcessorMetrics.v1_6_4.ProcessorMetrics"
                    },
                    {
                      "name": "GET Processor",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/Processors/1",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Systems/1/Processors/1\nType: #Processor.v1_20_1.Processor"
                },
                {
                  "name": "GET Processors Collection",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/Processors",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/Systems/1/Processors\nType: #ProcessorCollection.ProcessorCollection"
            },
            {
              "name": "SecureBoot",
              "item": [
                {
                  "name": "Actions",
                  "item": [
                    {
                      "name": "POST SecureBoot.ResetKeys",
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                              "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                              "    pm.execution.skipRequest();",
                              "  } else {",
                              "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                              "  }",
                              "}",
                              "",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        }
                      ],
                      "request": {
                        "method": "POST",
                        "header": [
                          {
                            "key": "Content-Type",
                            "value": "application/json",
                            "type": "text"
                          }
                        ],
                        "body": {
                          "mode": "raw",
                          "raw": "{\n  \"ResetKeysType\": \"<REQUIRED>\"\n}",
                          "options": {
                            "raw": {
                              "language": "json"
                            }
                          }
                        },
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/SecureBoot/Actions/SecureBoot.ResetKeys",
                        "description": "Redfish Action. Review the payload before explicitly enabling mutations."
                      },
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ]
                },
                {
                  "name": "SecureBootDatabases",
                  "item": [
                    {
                      "name": "KEK",
                      "item": [
                        {
                          "name": "Certificates",
                          "item": [
                            {
                              "name": "GET Certificate Collection",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/SecureBoot/SecureBootDatabases/KEK/Certificates",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Systems/1/SecureBoot/SecureBootDatabases/KEK/Certificates\nType: #CertificateCollection.CertificateCollection"
                        },
                        {
                          "name": "GET Key Exchange Keys(KEK)",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/SecureBoot/SecureBootDatabases/KEK",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Systems/1/SecureBoot/SecureBootDatabases/KEK\nType: #SecureBootDatabase.v1_0_3.SecureBootDatabase"
                    },
                    {
                      "name": "PK",
                      "item": [
                        {
                          "name": "Certificates",
                          "item": [
                            {
                              "name": "GET Certificate Collection",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/SecureBoot/SecureBootDatabases/PK/Certificates",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Systems/1/SecureBoot/SecureBootDatabases/PK/Certificates\nType: #CertificateCollection.CertificateCollection"
                        },
                        {
                          "name": "GET Platform Key(PK)",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/SecureBoot/SecureBootDatabases/PK",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Systems/1/SecureBoot/SecureBootDatabases/PK\nType: #SecureBootDatabase.v1_0_3.SecureBootDatabase"
                    },
                    {
                      "name": "db",
                      "item": [
                        {
                          "name": "Certificates",
                          "item": [
                            {
                              "name": "GET Certificate Collection",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/SecureBoot/SecureBootDatabases/db/Certificates",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Systems/1/SecureBoot/SecureBootDatabases/db/Certificates\nType: #CertificateCollection.CertificateCollection"
                        },
                        {
                          "name": "GET Authorized Signatures(db)",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/SecureBoot/SecureBootDatabases/db",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Systems/1/SecureBoot/SecureBootDatabases/db\nType: #SecureBootDatabase.v1_0_3.SecureBootDatabase"
                    },
                    {
                      "name": "dbr",
                      "item": [
                        {
                          "name": "Certificates",
                          "item": [
                            {
                              "name": "GET Certificate Collection",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/SecureBoot/SecureBootDatabases/dbr/Certificates",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Systems/1/SecureBoot/SecureBootDatabases/dbr/Certificates\nType: #CertificateCollection.CertificateCollection"
                        },
                        {
                          "name": "GET OsRecovery Signatures(dbr)",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/SecureBoot/SecureBootDatabases/dbr",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Systems/1/SecureBoot/SecureBootDatabases/dbr\nType: #SecureBootDatabase.v1_0_3.SecureBootDatabase"
                    },
                    {
                      "name": "dbt",
                      "item": [
                        {
                          "name": "Certificates",
                          "item": [
                            {
                              "name": "GET Certificate Collection",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/SecureBoot/SecureBootDatabases/dbt/Certificates",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Systems/1/SecureBoot/SecureBootDatabases/dbt/Certificates\nType: #CertificateCollection.CertificateCollection"
                        },
                        {
                          "name": "GET Authorized TimeStamps(dbt)",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/SecureBoot/SecureBootDatabases/dbt",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Systems/1/SecureBoot/SecureBootDatabases/dbt\nType: #SecureBootDatabase.v1_0_3.SecureBootDatabase"
                    },
                    {
                      "name": "dbx",
                      "item": [
                        {
                          "name": "Signatures",
                          "item": [
                            {
                              "name": "GET Signature Collection",
                              "request": {
                                "method": "GET",
                                "header": [],
                                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/SecureBoot/SecureBootDatabases/dbx/Signatures",
                                "description": "Evidence: allow-header"
                              },
                              "event": [
                                {
                                  "listen": "prerequest",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "const urlTools = require('url');",
                                      "function parseURL(raw) {",
                                      "  const value = urlTools.parse(raw);",
                                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                      "  return value;",
                                      "}",
                                      "function targetBase() {",
                                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                      "}",
                                      "function authorizeSession(raw, baseURL) {",
                                      "  const base = targetBase();",
                                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                      "  let expanded = raw;",
                                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                      "  const decoded = decodeURIComponent(session.pathname || '');",
                                      "  if (session.origin !== base.origin || session.auth ||",
                                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                      "  return session;",
                                      "}",
                                      "",
                                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                      "pm.request.headers.remove('Authorization');",
                                      "pm.request.headers.remove('X-Auth-Token');",
                                      "if (mode === 'basic') {",
                                      "  const username = pm.environment.get('USERNAME') || '';",
                                      "  const password = pm.environment.get('PASSWORD') || '';",
                                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                      "} else {",
                                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                      "    pm.execution.skipRequest();",
                                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                                      "  }",
                                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                      "}"
                                    ]
                                  }
                                },
                                {
                                  "listen": "test",
                                  "script": {
                                    "type": "text/javascript",
                                    "exec": [
                                      "pm.test('Response status is below 400', function () {",
                                      "  pm.expect(pm.response.code).to.be.below(400);",
                                      "});",
                                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                                      "if (pm.response.code !== 204) {",
                                      "  pm.test('Response is JSON', function () {",
                                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                      "  });",
                                      "}",
                                      ""
                                    ]
                                  }
                                }
                              ],
                              "protocolProfileBehavior": {
                                "followRedirects": false
                              }
                            }
                          ],
                          "description": "URI: /redfish/v1/Systems/1/SecureBoot/SecureBootDatabases/dbx/Signatures\nType: #SignatureCollection.SignatureCollection"
                        },
                        {
                          "name": "GET Forbidden Signatures(dbx)",
                          "request": {
                            "method": "GET",
                            "header": [],
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/SecureBoot/SecureBootDatabases/dbx",
                            "description": "Evidence: allow-header"
                          },
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            },
                            {
                              "listen": "test",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "pm.test('Response status is below 400', function () {",
                                  "  pm.expect(pm.response.code).to.be.below(400);",
                                  "});",
                                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                                  "if (pm.response.code !== 204) {",
                                  "  pm.test('Response is JSON', function () {",
                                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                                  "  });",
                                  "}",
                                  ""
                                ]
                              }
                            }
                          ],
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ],
                      "description": "URI: /redfish/v1/Systems/1/SecureBoot/SecureBootDatabases/dbx\nType: #SecureBootDatabase.v1_0_3.SecureBootDatabase"
                    },
                    {
                      "name": "GET UEFI SecureBoot Database Collection",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/SecureBoot/SecureBootDatabases",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/Systems/1/SecureBoot/SecureBootDatabases\nType: #SecureBootDatabaseCollection.SecureBootDatabaseCollection"
                },
                {
                  "name": "GET SecureBoot",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/SecureBoot",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                },
                {
                  "name": "PATCH SecureBoot",
                  "request": {
                    "method": "PATCH",
                    "header": [
                      {
                        "key": "Content-Type",
                        "value": "application/json",
                        "type": "text"
                      },
                      {
                        "key": "If-Match",
                        "value": "\"889deef6\"",
                        "type": "text"
                      }
                    ],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/SecureBoot",
                    "description": "Evidence: allow-header",
                    "body": {
                      "mode": "raw",
                      "raw": "{}",
                      "options": {
                        "raw": {
                          "language": "json"
                        }
                      }
                    }
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                          "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                          "    pm.execution.skipRequest();",
                          "  } else {",
                          "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                          "  }",
                          "}",
                          "",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/Systems/1/SecureBoot\nType: #SecureBoot.v1_1_2.SecureBoot"
            },
            {
              "name": "Storage",
              "item": [
                {
                  "name": "GET Storage Collection",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1/Storage",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/Systems/1/Storage\nType: #StorageCollection.StorageCollection"
            },
            {
              "name": "GET System",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            },
            {
              "name": "PATCH System",
              "request": {
                "method": "PATCH",
                "header": [
                  {
                    "key": "Content-Type",
                    "value": "application/json",
                    "type": "text"
                  },
                  {
                    "key": "If-Match",
                    "value": "\"842428e7\"",
                    "type": "text"
                  }
                ],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems/1",
                "description": "Evidence: allow-header",
                "body": {
                  "mode": "raw",
                  "raw": "{}",
                  "options": {
                    "raw": {
                      "language": "json"
                    }
                  }
                }
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                      "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                      "    pm.execution.skipRequest();",
                      "  } else {",
                      "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                      "  }",
                      "}",
                      "",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/Systems/1\nType: #ComputerSystem.v1_25_0.ComputerSystem"
        },
        {
          "name": "GET Computer System Collection",
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/Systems",
            "description": "Evidence: allow-header"
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const urlTools = require('url');",
                  "function parseURL(raw) {",
                  "  const value = urlTools.parse(raw);",
                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                  "  return value;",
                  "}",
                  "function targetBase() {",
                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                  "}",
                  "function authorizeSession(raw, baseURL) {",
                  "  const base = targetBase();",
                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                  "  let expanded = raw;",
                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                  "  const decoded = decodeURIComponent(session.pathname || '');",
                  "  if (session.origin !== base.origin || session.auth ||",
                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                  "  return session;",
                  "}",
                  "",
                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                  "pm.request.headers.remove('Authorization');",
                  "pm.request.headers.remove('X-Auth-Token');",
                  "if (mode === 'basic') {",
                  "  const username = pm.environment.get('USERNAME') || '';",
                  "  const password = pm.environment.get('PASSWORD') || '';",
                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                  "} else {",
                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                  "    pm.execution.skipRequest();",
                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                  "  }",
                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Response status is below 400', function () {",
                  "  pm.expect(pm.response.code).to.be.below(400);",
                  "});",
                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                  "if (pm.response.code !== 204) {",
                  "  pm.test('Response is JSON', function () {",
                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                  "  });",
                  "}",
                  ""
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        }
      ],
      "description": "URI: /redfish/v1/Systems\nType: #ComputerSystemCollection.ComputerSystemCollection"
    },
    {
      "name": "TaskService",
      "item": [
        {
          "name": "Tasks",
          "item": [
            {
              "name": "GET Task Collection",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TaskService/Tasks",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/TaskService/Tasks\nType: #TaskCollection.TaskCollection"
        },
        {
          "name": "GET Task Service",
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TaskService",
            "description": "Evidence: allow-header"
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const urlTools = require('url');",
                  "function parseURL(raw) {",
                  "  const value = urlTools.parse(raw);",
                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                  "  return value;",
                  "}",
                  "function targetBase() {",
                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                  "}",
                  "function authorizeSession(raw, baseURL) {",
                  "  const base = targetBase();",
                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                  "  let expanded = raw;",
                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                  "  const decoded = decodeURIComponent(session.pathname || '');",
                  "  if (session.origin !== base.origin || session.auth ||",
                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                  "  return session;",
                  "}",
                  "",
                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                  "pm.request.headers.remove('Authorization');",
                  "pm.request.headers.remove('X-Auth-Token');",
                  "if (mode === 'basic') {",
                  "  const username = pm.environment.get('USERNAME') || '';",
                  "  const password = pm.environment.get('PASSWORD') || '';",
                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                  "} else {",
                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                  "    pm.execution.skipRequest();",
                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                  "  }",
                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Response status is below 400', function () {",
                  "  pm.expect(pm.response.code).to.be.below(400);",
                  "});",
                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                  "if (pm.response.code !== 204) {",
                  "  pm.test('Response is JSON', function () {",
                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                  "  });",
                  "}",
                  ""
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "PATCH Task Service",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "If-Match",
                "value": "\"651e544f\"",
                "type": "text"
              }
            ],
            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TaskService",
            "description": "Evidence: allow-header",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const urlTools = require('url');",
                  "function parseURL(raw) {",
                  "  const value = urlTools.parse(raw);",
                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                  "  return value;",
                  "}",
                  "function targetBase() {",
                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                  "}",
                  "function authorizeSession(raw, baseURL) {",
                  "  const base = targetBase();",
                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                  "  let expanded = raw;",
                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                  "  const decoded = decodeURIComponent(session.pathname || '');",
                  "  if (session.origin !== base.origin || session.auth ||",
                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                  "  return session;",
                  "}",
                  "",
                  "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                  "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                  "    pm.execution.skipRequest();",
                  "  } else {",
                  "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                  "  }",
                  "}",
                  "",
                  "",
                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                  "pm.request.headers.remove('Authorization');",
                  "pm.request.headers.remove('X-Auth-Token');",
                  "if (mode === 'basic') {",
                  "  const username = pm.environment.get('USERNAME') || '';",
                  "  const password = pm.environment.get('PASSWORD') || '';",
                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                  "} else {",
                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                  "    pm.execution.skipRequest();",
                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                  "  }",
                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                  "}"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        }
      ],
      "description": "URI: /redfish/v1/TaskService\nType: #TaskService.v1_2_1.TaskService"
    },
    {
      "name": "TelemetryService",
      "item": [
        {
          "name": "MetricDefinitions",
          "item": [
            {
              "name": "AvgPowerConsumedDay",
              "item": [
                {
                  "name": "GET Average Power Consumed Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/AvgPowerConsumedDay",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/AvgPowerConsumedDay\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "AvgPowerConsumedHour",
              "item": [
                {
                  "name": "GET Average Power Consumed Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/AvgPowerConsumedHour",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/AvgPowerConsumedHour\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "AvgPowerConsumedWeek",
              "item": [
                {
                  "name": "GET Average Power Consumed Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/AvgPowerConsumedWeek",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/AvgPowerConsumedWeek\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "BroadcomCriticalEvent",
              "item": [
                {
                  "name": "GET Broadcom Critical Event",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/BroadcomCriticalEvent",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/BroadcomCriticalEvent\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "BroadcomFatalEvent",
              "item": [
                {
                  "name": "GET Broadcom Fatal Event",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/BroadcomFatalEvent",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/BroadcomFatalEvent\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "BroadcomWarningEvent",
              "item": [
                {
                  "name": "GET Broadcom Warning Event",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/BroadcomWarningEvent",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/BroadcomWarningEvent\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "CUPS_AverageCupsIndex",
              "item": [
                {
                  "name": "GET CUPS_AverageCupsIndex Utilization",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/CUPS_AverageCupsIndex",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/CUPS_AverageCupsIndex\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "CUPS_AverageHostCpuUtilization",
              "item": [
                {
                  "name": "GET CUPS_AverageHostCpuUtilization Utilization",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/CUPS_AverageHostCpuUtilization",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/CUPS_AverageHostCpuUtilization\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "CUPS_AverageHostMemoryBandwidthUtilization",
              "item": [
                {
                  "name": "GET CUPS_AverageHostMemoryBandwidthUtilization Utilization",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/CUPS_AverageHostMemoryBandwidthUtilization",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/CUPS_AverageHostMemoryBandwidthUtilization\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "CUPS_AverageHostPciBandwidthUtilization",
              "item": [
                {
                  "name": "GET CUPS_AverageHostPciBandwidthUtilization Utilization",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/CUPS_AverageHostPciBandwidthUtilization",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/CUPS_AverageHostPciBandwidthUtilization\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "CUPS_CupsIndex",
              "item": [
                {
                  "name": "GET CUPS_CupsIndex Utilization",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/CUPS_CupsIndex",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/CUPS_CupsIndex\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "CUPS_HostCpuUtilization",
              "item": [
                {
                  "name": "GET CUPS_HostCpuUtilization Utilization",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/CUPS_HostCpuUtilization",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/CUPS_HostCpuUtilization\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "CUPS_HostMemoryBandwidthUtilization",
              "item": [
                {
                  "name": "GET CUPS_HostMemoryBandwidthUtilization Utilization",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/CUPS_HostMemoryBandwidthUtilization",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/CUPS_HostMemoryBandwidthUtilization\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "CUPS_HostPciBandwidthUtilization",
              "item": [
                {
                  "name": "GET CUPS_HostPciBandwidthUtilization Utilization",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/CUPS_HostPciBandwidthUtilization",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/CUPS_HostPciBandwidthUtilization\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "MaxPowerConsumedDay",
              "item": [
                {
                  "name": "GET Maximum Power Consumed Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/MaxPowerConsumedDay",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/MaxPowerConsumedDay\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "MaxPowerConsumedHour",
              "item": [
                {
                  "name": "GET Maximum Power Consumed Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/MaxPowerConsumedHour",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/MaxPowerConsumedHour\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "MaxPowerConsumedWeek",
              "item": [
                {
                  "name": "GET Maximum Power Consumed Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/MaxPowerConsumedWeek",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/MaxPowerConsumedWeek\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "MinPowerConsumedDay",
              "item": [
                {
                  "name": "GET Minimum Power Consumed Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/MinPowerConsumedDay",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/MinPowerConsumedDay\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "MinPowerConsumedHour",
              "item": [
                {
                  "name": "GET Minimum Power Consumed Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/MinPowerConsumedHour",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/MinPowerConsumedHour\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "MinPowerConsumedWeek",
              "item": [
                {
                  "name": "GET Minimum Power Consumed Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/MinPowerConsumedWeek",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/MinPowerConsumedWeek\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core0_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core0_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core0_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core0_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core10_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core10_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core10_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core10_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core11_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core11_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core11_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core11_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core12_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core12_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core12_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core12_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core13_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core13_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core13_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core13_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core14_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core14_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core14_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core14_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core15_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core15_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core15_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core15_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core16_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core16_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core16_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core16_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core17_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core17_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core17_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core17_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core18_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core18_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core18_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core18_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core19_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core19_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core19_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core19_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core1_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core1_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core1_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core1_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core20_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core20_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core20_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core20_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core21_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core21_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core21_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core21_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core22_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core22_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core22_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core22_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core23_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core23_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core23_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core23_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core24_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core24_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core24_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core24_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core25_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core25_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core25_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core25_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core26_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core26_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core26_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core26_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core27_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core27_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core27_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core27_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core28_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core28_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core28_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core28_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core29_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core29_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core29_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core29_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core2_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core2_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core2_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core2_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core30_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core30_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core30_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core30_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core31_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core31_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core31_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core31_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core32_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core32_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core32_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core32_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core33_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core33_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core33_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core33_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core34_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core34_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core34_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core34_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core35_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core35_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core35_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core35_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core36_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core36_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core36_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core36_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core37_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core37_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core37_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core37_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core38_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core38_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core38_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core38_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core39_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core39_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core39_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core39_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core3_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core3_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core3_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core3_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core40_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core40_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core40_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core40_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core41_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core41_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core41_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core41_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core42_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core42_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core42_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core42_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core43_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core43_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core43_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core43_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core44_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core44_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core44_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core44_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core45_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core45_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core45_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core45_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core46_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core46_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core46_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core46_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core47_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core47_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core47_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core47_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core48_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core48_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core48_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core48_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core49_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core49_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core49_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core49_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core4_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core4_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core4_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core4_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core50_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core50_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core50_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core50_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core51_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core51_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core51_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core51_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core52_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core52_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core52_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core52_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core53_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core53_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core53_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core53_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core54_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core54_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core54_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core54_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core55_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core55_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core55_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core55_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core56_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core56_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core56_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core56_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core57_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core57_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core57_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core57_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core58_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core58_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core58_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core58_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core59_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core59_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core59_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core59_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core5_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core5_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core5_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core5_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core60_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core60_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core60_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core60_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core61_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core61_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core61_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core61_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core62_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core62_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core62_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core62_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core63_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core63_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core63_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core63_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core64_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core64_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core64_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core64_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core65_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core65_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core65_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core65_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core66_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core66_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core66_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core66_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core67_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core67_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core67_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core67_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core68_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core68_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core68_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core68_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core69_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core69_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core69_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core69_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core6_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core6_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core6_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core6_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core70_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core70_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core70_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core70_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core71_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core71_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core71_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core71_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core72_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core72_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core72_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core72_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core73_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core73_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core73_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core73_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core74_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core74_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core74_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core74_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core75_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core75_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core75_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core75_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core76_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core76_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core76_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core76_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core77_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core77_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core77_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core77_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core78_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core78_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core78_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core78_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core79_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core79_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core79_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core79_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core7_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core7_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core7_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core7_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core80_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core80_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core80_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core80_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core81_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core81_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core81_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core81_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core82_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core82_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core82_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core82_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core83_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core83_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core83_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core83_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core84_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core84_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core84_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core84_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core85_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core85_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core85_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core85_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core8_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core8_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core8_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core8_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22473996_Core9_TEMP",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22473996_Core9_TEMP Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core9_TEMP",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22473996_Core9_TEMP\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_ComputeDie0_AGGREGATE_CORE_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_ComputeDie0_AGGREGATE_CORE_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_ComputeDie0_AGGREGATE_CORE_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_ComputeDie0_AGGREGATE_CORE_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_ComputeDie0_DIE_CSTATE_RESIDENCY_C1E",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_ComputeDie0_DIE_CSTATE_RESIDENCY_C1E Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_ComputeDie0_DIE_CSTATE_RESIDENCY_C1E",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_ComputeDie0_DIE_CSTATE_RESIDENCY_C1E\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_ComputeDie1_AGGREGATE_CORE_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_ComputeDie1_AGGREGATE_CORE_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_ComputeDie1_AGGREGATE_CORE_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_ComputeDie1_AGGREGATE_CORE_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_ComputeDie1_DIE_CSTATE_RESIDENCY_C1E",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_ComputeDie1_DIE_CSTATE_RESIDENCY_C1E Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_ComputeDie1_DIE_CSTATE_RESIDENCY_C1E",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_ComputeDie1_DIE_CSTATE_RESIDENCY_C1E\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core0_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core0_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core0_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core0_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core0_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core0_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core0_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core0_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core10_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core10_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core10_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core10_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core10_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core10_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core10_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core10_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core11_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core11_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core11_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core11_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core11_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core11_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core11_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core11_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core12_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core12_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core12_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core12_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core12_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core12_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core12_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core12_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core13_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core13_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core13_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core13_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core13_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core13_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core13_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core13_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core14_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core14_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core14_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core14_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core14_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core14_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core14_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core14_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core15_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core15_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core15_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core15_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core15_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core15_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core15_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core15_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core16_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core16_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core16_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core16_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core16_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core16_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core16_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core16_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core17_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core17_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core17_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core17_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core17_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core17_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core17_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core17_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core18_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core18_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core18_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core18_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core18_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core18_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core18_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core18_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core19_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core19_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core19_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core19_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core19_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core19_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core19_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core19_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core1_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core1_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core1_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core1_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core1_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core1_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core1_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core1_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core20_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core20_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core20_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core20_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core20_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core20_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core20_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core20_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core21_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core21_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core21_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core21_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core21_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core21_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core21_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core21_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core22_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core22_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core22_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core22_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core22_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core22_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core22_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core22_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core23_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core23_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core23_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core23_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core23_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core23_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core23_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core23_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core24_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core24_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core24_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core24_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core24_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core24_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core24_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core24_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core25_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core25_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core25_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core25_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core25_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core25_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core25_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core25_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core26_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core26_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core26_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core26_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core26_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core26_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core26_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core26_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core27_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core27_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core27_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core27_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core27_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core27_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core27_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core27_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core28_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core28_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core28_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core28_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core28_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core28_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core28_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core28_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core29_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core29_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core29_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core29_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core29_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core29_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core29_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core29_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core2_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core2_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core2_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core2_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core2_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core2_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core2_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core2_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core30_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core30_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core30_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core30_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core30_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core30_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core30_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core30_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core31_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core31_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core31_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core31_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core31_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core31_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core31_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core31_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core32_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core32_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core32_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core32_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core32_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core32_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core32_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core32_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core33_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core33_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core33_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core33_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core33_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core33_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core33_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core33_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core34_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core34_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core34_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core34_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core34_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core34_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core34_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core34_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core35_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core35_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core35_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core35_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core35_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core35_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core35_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core35_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core36_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core36_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core36_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core36_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core36_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core36_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core36_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core36_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core37_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core37_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core37_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core37_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core37_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core37_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core37_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core37_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core38_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core38_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core38_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core38_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core38_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core38_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core38_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core38_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core39_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core39_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core39_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core39_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core39_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core39_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core39_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core39_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core3_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core3_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core3_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core3_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core3_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core3_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core3_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core3_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core40_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core40_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core40_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core40_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core40_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core40_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core40_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core40_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core41_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core41_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core41_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core41_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core41_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core41_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core41_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core41_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core42_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core42_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core42_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core42_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core42_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core42_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core42_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core42_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core43_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core43_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core43_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core43_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core43_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core43_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core43_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core43_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core44_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core44_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core44_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core44_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core44_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core44_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core44_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core44_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core45_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core45_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core45_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core45_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core45_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core45_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core45_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core45_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core46_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core46_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core46_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core46_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core46_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core46_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core46_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core46_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core47_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core47_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core47_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core47_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core47_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core47_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core47_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core47_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core48_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core48_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core48_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core48_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core48_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core48_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core48_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core48_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core49_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core49_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core49_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core49_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core49_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core49_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core49_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core49_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core4_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core4_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core4_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core4_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core4_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core4_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core4_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core4_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core50_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core50_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core50_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core50_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core50_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core50_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core50_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core50_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core51_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core51_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core51_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core51_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core51_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core51_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core51_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core51_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core52_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core52_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core52_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core52_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core52_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core52_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core52_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core52_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core53_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core53_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core53_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core53_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core53_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core53_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core53_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core53_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core54_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core54_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core54_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core54_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core54_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core54_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core54_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core54_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core55_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core55_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core55_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core55_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core55_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core55_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core55_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core55_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core56_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core56_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core56_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core56_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core56_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core56_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core56_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core56_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core57_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core57_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core57_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core57_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core57_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core57_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core57_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core57_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core58_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core58_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core58_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core58_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core58_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core58_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core58_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core58_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core59_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core59_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core59_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core59_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core59_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core59_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core59_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core59_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core5_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core5_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core5_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core5_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core5_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core5_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core5_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core5_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core60_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core60_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core60_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core60_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core60_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core60_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core60_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core60_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core61_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core61_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core61_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core61_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core61_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core61_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core61_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core61_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core62_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core62_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core62_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core62_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core62_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core62_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core62_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core62_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core63_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core63_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core63_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core63_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core63_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core63_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core63_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core63_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core64_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core64_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core64_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core64_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core64_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core64_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core64_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core64_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core65_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core65_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core65_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core65_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core65_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core65_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core65_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core65_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core66_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core66_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core66_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core66_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core66_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core66_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core66_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core66_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core67_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core67_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core67_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core67_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core67_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core67_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core67_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core67_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core68_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core68_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core68_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core68_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core68_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core68_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core68_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core68_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core69_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core69_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core69_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core69_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core69_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core69_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core69_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core69_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core6_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core6_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core6_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core6_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core6_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core6_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core6_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core6_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core70_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core70_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core70_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core70_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core70_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core70_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core70_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core70_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core71_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core71_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core71_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core71_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core71_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core71_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core71_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core71_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core72_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core72_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core72_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core72_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core72_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core72_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core72_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core72_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core73_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core73_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core73_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core73_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core73_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core73_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core73_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core73_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core74_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core74_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core74_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core74_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core74_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core74_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core74_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core74_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core75_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core75_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core75_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core75_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core75_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core75_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core75_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core75_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core76_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core76_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core76_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core76_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core76_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core76_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core76_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core76_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core77_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core77_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core77_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core77_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core77_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core77_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core77_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core77_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core78_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core78_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core78_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core78_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core78_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core78_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core78_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core78_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core79_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core79_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core79_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core79_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core79_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core79_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core79_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core79_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core7_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core7_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core7_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core7_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core7_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core7_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core7_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core7_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core80_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core80_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core80_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core80_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core80_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core80_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core80_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core80_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core81_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core81_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core81_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core81_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core81_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core81_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core81_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core81_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core82_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core82_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core82_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core82_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core82_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core82_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core82_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core82_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core83_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core83_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core83_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core83_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core83_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core83_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core83_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core83_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core84_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core84_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core84_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core84_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core84_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core84_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core84_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core84_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core85_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core85_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core85_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core85_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core85_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core85_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core85_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core85_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core8_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core8_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core8_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core8_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core8_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core8_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core8_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core8_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core9_CSTATE_RESIDENCY_C0",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core9_CSTATE_RESIDENCY_C0 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core9_CSTATE_RESIDENCY_C0",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core9_CSTATE_RESIDENCY_C0\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x22806802_Core9_CSTATE_RESIDENCY_C1",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x22806802_Core9_CSTATE_RESIDENCY_C1 Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core9_CSTATE_RESIDENCY_C1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x22806802_Core9_CSTATE_RESIDENCY_C1\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA1_RMID0_RDT_CMT",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA1_RMID0_RDT_CMT Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID0_RDT_CMT",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID0_RDT_CMT\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA1_RMID0_RDT_MBM_LOCAL",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA1_RMID0_RDT_MBM_LOCAL Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID0_RDT_MBM_LOCAL",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID0_RDT_MBM_LOCAL\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA1_RMID0_RDT_MBM_TOTAL",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA1_RMID0_RDT_MBM_TOTAL Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID0_RDT_MBM_TOTAL",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID0_RDT_MBM_TOTAL\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA1_RMID1_RDT_CMT",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA1_RMID1_RDT_CMT Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID1_RDT_CMT",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID1_RDT_CMT\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA1_RMID1_RDT_MBM_LOCAL",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA1_RMID1_RDT_MBM_LOCAL Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID1_RDT_MBM_LOCAL",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID1_RDT_MBM_LOCAL\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA1_RMID1_RDT_MBM_TOTAL",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA1_RMID1_RDT_MBM_TOTAL Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID1_RDT_MBM_TOTAL",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID1_RDT_MBM_TOTAL\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA1_RMID2_RDT_CMT",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA1_RMID2_RDT_CMT Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID2_RDT_CMT",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID2_RDT_CMT\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA1_RMID2_RDT_MBM_LOCAL",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA1_RMID2_RDT_MBM_LOCAL Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID2_RDT_MBM_LOCAL",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID2_RDT_MBM_LOCAL\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA1_RMID2_RDT_MBM_TOTAL",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA1_RMID2_RDT_MBM_TOTAL Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID2_RDT_MBM_TOTAL",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID2_RDT_MBM_TOTAL\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA1_RMID3_RDT_CMT",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA1_RMID3_RDT_CMT Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID3_RDT_CMT",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID3_RDT_CMT\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA1_RMID3_RDT_MBM_LOCAL",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA1_RMID3_RDT_MBM_LOCAL Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID3_RDT_MBM_LOCAL",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID3_RDT_MBM_LOCAL\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA1_RMID3_RDT_MBM_TOTAL",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA1_RMID3_RDT_MBM_TOTAL Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID3_RDT_MBM_TOTAL",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID3_RDT_MBM_TOTAL\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA1_RMID4_RDT_CMT",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA1_RMID4_RDT_CMT Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID4_RDT_CMT",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID4_RDT_CMT\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA1_RMID4_RDT_MBM_LOCAL",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA1_RMID4_RDT_MBM_LOCAL Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID4_RDT_MBM_LOCAL",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID4_RDT_MBM_LOCAL\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA1_RMID4_RDT_MBM_TOTAL",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA1_RMID4_RDT_MBM_TOTAL Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID4_RDT_MBM_TOTAL",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID4_RDT_MBM_TOTAL\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA1_RMID5_RDT_CMT",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA1_RMID5_RDT_CMT Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID5_RDT_CMT",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID5_RDT_CMT\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA1_RMID5_RDT_MBM_LOCAL",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA1_RMID5_RDT_MBM_LOCAL Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID5_RDT_MBM_LOCAL",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID5_RDT_MBM_LOCAL\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA1_RMID5_RDT_MBM_TOTAL",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA1_RMID5_RDT_MBM_TOTAL Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID5_RDT_MBM_TOTAL",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID5_RDT_MBM_TOTAL\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA1_RMID6_RDT_CMT",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA1_RMID6_RDT_CMT Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID6_RDT_CMT",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID6_RDT_CMT\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA1_RMID6_RDT_MBM_LOCAL",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA1_RMID6_RDT_MBM_LOCAL Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID6_RDT_MBM_LOCAL",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID6_RDT_MBM_LOCAL\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA1_RMID6_RDT_MBM_TOTAL",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA1_RMID6_RDT_MBM_TOTAL Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID6_RDT_MBM_TOTAL",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID6_RDT_MBM_TOTAL\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA1_RMID7_RDT_CMT",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA1_RMID7_RDT_CMT Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID7_RDT_CMT",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID7_RDT_CMT\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA1_RMID7_RDT_MBM_LOCAL",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA1_RMID7_RDT_MBM_LOCAL Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID7_RDT_MBM_LOCAL",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID7_RDT_MBM_LOCAL\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA1_RMID7_RDT_MBM_TOTAL",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA1_RMID7_RDT_MBM_TOTAL Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID7_RDT_MBM_TOTAL",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA1_RMID7_RDT_MBM_TOTAL\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA2_RMID0_RDT_CMT",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA2_RMID0_RDT_CMT Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA2_RMID0_RDT_CMT",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA2_RMID0_RDT_CMT\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA2_RMID1_RDT_CMT",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA2_RMID1_RDT_CMT Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA2_RMID1_RDT_CMT",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA2_RMID1_RDT_CMT\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA2_RMID2_RDT_CMT",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA2_RMID2_RDT_CMT Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA2_RMID2_RDT_CMT",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA2_RMID2_RDT_CMT\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA2_RMID3_RDT_CMT",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA2_RMID3_RDT_CMT Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA2_RMID3_RDT_CMT",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA2_RMID3_RDT_CMT\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA2_RMID4_RDT_CMT",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA2_RMID4_RDT_CMT Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA2_RMID4_RDT_CMT",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA2_RMID4_RDT_CMT\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA2_RMID5_RDT_CMT",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA2_RMID5_RDT_CMT Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA2_RMID5_RDT_CMT",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA2_RMID5_RDT_CMT\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA2_RMID6_RDT_CMT",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA2_RMID6_RDT_CMT Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA2_RMID6_RDT_CMT",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA2_RMID6_RDT_CMT\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA2_RMID7_RDT_CMT",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA2_RMID7_RDT_CMT Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA2_RMID7_RDT_CMT",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA2_RMID7_RDT_CMT\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA61_RMID0_RDT_CMT",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA61_RMID0_RDT_CMT Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID0_RDT_CMT",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID0_RDT_CMT\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA61_RMID0_RDT_MBM_LOCAL",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA61_RMID0_RDT_MBM_LOCAL Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID0_RDT_MBM_LOCAL",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID0_RDT_MBM_LOCAL\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA61_RMID0_RDT_MBM_TOTAL",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA61_RMID0_RDT_MBM_TOTAL Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID0_RDT_MBM_TOTAL",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID0_RDT_MBM_TOTAL\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA61_RMID1_RDT_CMT",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA61_RMID1_RDT_CMT Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID1_RDT_CMT",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID1_RDT_CMT\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA61_RMID1_RDT_MBM_LOCAL",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA61_RMID1_RDT_MBM_LOCAL Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID1_RDT_MBM_LOCAL",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID1_RDT_MBM_LOCAL\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA61_RMID1_RDT_MBM_TOTAL",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA61_RMID1_RDT_MBM_TOTAL Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID1_RDT_MBM_TOTAL",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID1_RDT_MBM_TOTAL\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA61_RMID2_RDT_CMT",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA61_RMID2_RDT_CMT Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID2_RDT_CMT",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID2_RDT_CMT\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA61_RMID2_RDT_MBM_LOCAL",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA61_RMID2_RDT_MBM_LOCAL Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID2_RDT_MBM_LOCAL",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID2_RDT_MBM_LOCAL\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA61_RMID2_RDT_MBM_TOTAL",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA61_RMID2_RDT_MBM_TOTAL Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID2_RDT_MBM_TOTAL",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID2_RDT_MBM_TOTAL\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA61_RMID3_RDT_CMT",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA61_RMID3_RDT_CMT Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID3_RDT_CMT",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID3_RDT_CMT\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA61_RMID3_RDT_MBM_LOCAL",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA61_RMID3_RDT_MBM_LOCAL Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID3_RDT_MBM_LOCAL",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID3_RDT_MBM_LOCAL\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA61_RMID3_RDT_MBM_TOTAL",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA61_RMID3_RDT_MBM_TOTAL Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID3_RDT_MBM_TOTAL",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID3_RDT_MBM_TOTAL\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA61_RMID4_RDT_CMT",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA61_RMID4_RDT_CMT Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID4_RDT_CMT",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID4_RDT_CMT\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA61_RMID4_RDT_MBM_LOCAL",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA61_RMID4_RDT_MBM_LOCAL Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID4_RDT_MBM_LOCAL",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID4_RDT_MBM_LOCAL\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA61_RMID4_RDT_MBM_TOTAL",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA61_RMID4_RDT_MBM_TOTAL Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID4_RDT_MBM_TOTAL",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID4_RDT_MBM_TOTAL\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA61_RMID5_RDT_CMT",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA61_RMID5_RDT_CMT Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID5_RDT_CMT",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID5_RDT_CMT\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA61_RMID5_RDT_MBM_LOCAL",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA61_RMID5_RDT_MBM_LOCAL Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID5_RDT_MBM_LOCAL",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID5_RDT_MBM_LOCAL\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA61_RMID5_RDT_MBM_TOTAL",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA61_RMID5_RDT_MBM_TOTAL Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID5_RDT_MBM_TOTAL",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID5_RDT_MBM_TOTAL\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA61_RMID6_RDT_CMT",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA61_RMID6_RDT_CMT Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID6_RDT_CMT",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID6_RDT_CMT\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA61_RMID6_RDT_MBM_LOCAL",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA61_RMID6_RDT_MBM_LOCAL Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID6_RDT_MBM_LOCAL",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID6_RDT_MBM_LOCAL\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA61_RMID6_RDT_MBM_TOTAL",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA61_RMID6_RDT_MBM_TOTAL Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID6_RDT_MBM_TOTAL",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID6_RDT_MBM_TOTAL\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA61_RMID7_RDT_CMT",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA61_RMID7_RDT_CMT Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID7_RDT_CMT",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID7_RDT_CMT\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA61_RMID7_RDT_MBM_LOCAL",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA61_RMID7_RDT_MBM_LOCAL Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID7_RDT_MBM_LOCAL",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID7_RDT_MBM_LOCAL\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA61_RMID7_RDT_MBM_TOTAL",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA61_RMID7_RDT_MBM_TOTAL Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID7_RDT_MBM_TOTAL",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA61_RMID7_RDT_MBM_TOTAL\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA62_RMID0_RDT_CMT",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA62_RMID0_RDT_CMT Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA62_RMID0_RDT_CMT",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA62_RMID0_RDT_CMT\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA62_RMID1_RDT_CMT",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA62_RMID1_RDT_CMT Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA62_RMID1_RDT_CMT",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA62_RMID1_RDT_CMT\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA62_RMID2_RDT_CMT",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA62_RMID2_RDT_CMT Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA62_RMID2_RDT_CMT",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA62_RMID2_RDT_CMT\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA62_RMID3_RDT_CMT",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA62_RMID3_RDT_CMT Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA62_RMID3_RDT_CMT",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA62_RMID3_RDT_CMT\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA62_RMID4_RDT_CMT",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA62_RMID4_RDT_CMT Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA62_RMID4_RDT_CMT",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA62_RMID4_RDT_CMT\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA62_RMID5_RDT_CMT",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA62_RMID5_RDT_CMT Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA62_RMID5_RDT_CMT",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA62_RMID5_RDT_CMT\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA62_RMID6_RDT_CMT",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA62_RMID6_RDT_CMT Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA62_RMID6_RDT_CMT",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA62_RMID6_RDT_CMT\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "PMT_Socket0_0x477e9373_CHA62_RMID7_RDT_CMT",
              "item": [
                {
                  "name": "GET PMT_Socket0_0x477e9373_CHA62_RMID7_RDT_CMT Metric Definition",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA62_RMID7_RDT_CMT",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions/PMT_Socket0_0x477e9373_CHA62_RMID7_RDT_CMT\nType: #MetricDefinition.v1_3_4.MetricDefinition"
            },
            {
              "name": "GET Metric Definitions",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricDefinitions",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/TelemetryService/MetricDefinitions\nType: #MetricDefinitionCollection.MetricDefinitionCollection"
        },
        {
          "name": "MetricReportDefinitions",
          "item": [
            {
              "name": "AvgPowerConsumptionDay",
              "item": [
                {
                  "name": "GET Average Power Consumption for 1 day with sensing interval of 1 hour.",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReportDefinitions/AvgPowerConsumptionDay",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                },
                {
                  "name": "PATCH Average Power Consumption for 1 day with sensing interval of 1 hour.",
                  "request": {
                    "method": "PATCH",
                    "header": [
                      {
                        "key": "Content-Type",
                        "value": "application/json",
                        "type": "text"
                      },
                      {
                        "key": "If-Match",
                        "value": "\"8bdaccee\"",
                        "type": "text"
                      }
                    ],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReportDefinitions/AvgPowerConsumptionDay",
                    "description": "Evidence: allow-header",
                    "body": {
                      "mode": "raw",
                      "raw": "{}",
                      "options": {
                        "raw": {
                          "language": "json"
                        }
                      }
                    }
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                          "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                          "    pm.execution.skipRequest();",
                          "  } else {",
                          "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                          "  }",
                          "}",
                          "",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricReportDefinitions/AvgPowerConsumptionDay\nType: #MetricReportDefinition.v1_4_6.MetricReportDefinition"
            },
            {
              "name": "AvgPowerConsumptionHour",
              "item": [
                {
                  "name": "GET Average Power Consumption for 1 hour with sensing interval of 5 minutes.",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReportDefinitions/AvgPowerConsumptionHour",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                },
                {
                  "name": "PATCH Average Power Consumption for 1 hour with sensing interval of 5 minutes.",
                  "request": {
                    "method": "PATCH",
                    "header": [
                      {
                        "key": "Content-Type",
                        "value": "application/json",
                        "type": "text"
                      },
                      {
                        "key": "If-Match",
                        "value": "\"d7885dea\"",
                        "type": "text"
                      }
                    ],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReportDefinitions/AvgPowerConsumptionHour",
                    "description": "Evidence: allow-header",
                    "body": {
                      "mode": "raw",
                      "raw": "{}",
                      "options": {
                        "raw": {
                          "language": "json"
                        }
                      }
                    }
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                          "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                          "    pm.execution.skipRequest();",
                          "  } else {",
                          "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                          "  }",
                          "}",
                          "",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricReportDefinitions/AvgPowerConsumptionHour\nType: #MetricReportDefinition.v1_4_6.MetricReportDefinition"
            },
            {
              "name": "AvgPowerConsumptionWeek",
              "item": [
                {
                  "name": "GET Average Power Consumption for 1 week with sensing interval of 12 hours.",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReportDefinitions/AvgPowerConsumptionWeek",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                },
                {
                  "name": "PATCH Average Power Consumption for 1 week with sensing interval of 12 hours.",
                  "request": {
                    "method": "PATCH",
                    "header": [
                      {
                        "key": "Content-Type",
                        "value": "application/json",
                        "type": "text"
                      },
                      {
                        "key": "If-Match",
                        "value": "\"38468eec\"",
                        "type": "text"
                      }
                    ],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReportDefinitions/AvgPowerConsumptionWeek",
                    "description": "Evidence: allow-header",
                    "body": {
                      "mode": "raw",
                      "raw": "{}",
                      "options": {
                        "raw": {
                          "language": "json"
                        }
                      }
                    }
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                          "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                          "    pm.execution.skipRequest();",
                          "  } else {",
                          "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                          "  }",
                          "}",
                          "",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricReportDefinitions/AvgPowerConsumptionWeek\nType: #MetricReportDefinition.v1_4_6.MetricReportDefinition"
            },
            {
              "name": "BroadcomEvent",
              "item": [
                {
                  "name": "GET Broadcom Controller Trigger Event",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReportDefinitions/BroadcomEvent",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                },
                {
                  "name": "PATCH Broadcom Controller Trigger Event",
                  "request": {
                    "method": "PATCH",
                    "header": [
                      {
                        "key": "Content-Type",
                        "value": "application/json",
                        "type": "text"
                      },
                      {
                        "key": "If-Match",
                        "value": "\"dcccad06\"",
                        "type": "text"
                      }
                    ],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReportDefinitions/BroadcomEvent",
                    "description": "Evidence: allow-header",
                    "body": {
                      "mode": "raw",
                      "raw": "{}",
                      "options": {
                        "raw": {
                          "language": "json"
                        }
                      }
                    }
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                          "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                          "    pm.execution.skipRequest();",
                          "  } else {",
                          "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                          "  }",
                          "}",
                          "",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricReportDefinitions/BroadcomEvent\nType: #MetricReportDefinition.v1_4_6.MetricReportDefinition"
            },
            {
              "name": "CPUMemMetrics",
              "item": [
                {
                  "name": "GET CPUMemMetrics",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReportDefinitions/CPUMemMetrics",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                },
                {
                  "name": "PATCH CPUMemMetrics",
                  "request": {
                    "method": "PATCH",
                    "header": [
                      {
                        "key": "Content-Type",
                        "value": "application/json",
                        "type": "text"
                      },
                      {
                        "key": "If-Match",
                        "value": "\"58581f37\"",
                        "type": "text"
                      }
                    ],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReportDefinitions/CPUMemMetrics",
                    "description": "Evidence: allow-header",
                    "body": {
                      "mode": "raw",
                      "raw": "{}",
                      "options": {
                        "raw": {
                          "language": "json"
                        }
                      }
                    }
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                          "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                          "    pm.execution.skipRequest();",
                          "  } else {",
                          "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                          "  }",
                          "}",
                          "",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricReportDefinitions/CPUMemMetrics\nType: #MetricReportDefinition.v1_4_6.MetricReportDefinition"
            },
            {
              "name": "CUPSMetrics",
              "item": [
                {
                  "name": "GET CUPSMetrics",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReportDefinitions/CUPSMetrics",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                },
                {
                  "name": "PATCH CUPSMetrics",
                  "request": {
                    "method": "PATCH",
                    "header": [
                      {
                        "key": "Content-Type",
                        "value": "application/json",
                        "type": "text"
                      },
                      {
                        "key": "If-Match",
                        "value": "\"0b311e83\"",
                        "type": "text"
                      }
                    ],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReportDefinitions/CUPSMetrics",
                    "description": "Evidence: allow-header",
                    "body": {
                      "mode": "raw",
                      "raw": "{}",
                      "options": {
                        "raw": {
                          "language": "json"
                        }
                      }
                    }
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                          "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                          "    pm.execution.skipRequest();",
                          "  } else {",
                          "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                          "  }",
                          "}",
                          "",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricReportDefinitions/CUPSMetrics\nType: #MetricReportDefinition.v1_4_6.MetricReportDefinition"
            },
            {
              "name": "MaxPowerConsumptionDay",
              "item": [
                {
                  "name": "GET Maximum Power Consumption for 1 day with sensing interval of 1 hour.",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReportDefinitions/MaxPowerConsumptionDay",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                },
                {
                  "name": "PATCH Maximum Power Consumption for 1 day with sensing interval of 1 hour.",
                  "request": {
                    "method": "PATCH",
                    "header": [
                      {
                        "key": "Content-Type",
                        "value": "application/json",
                        "type": "text"
                      },
                      {
                        "key": "If-Match",
                        "value": "\"5f59720a\"",
                        "type": "text"
                      }
                    ],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReportDefinitions/MaxPowerConsumptionDay",
                    "description": "Evidence: allow-header",
                    "body": {
                      "mode": "raw",
                      "raw": "{}",
                      "options": {
                        "raw": {
                          "language": "json"
                        }
                      }
                    }
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                          "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                          "    pm.execution.skipRequest();",
                          "  } else {",
                          "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                          "  }",
                          "}",
                          "",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricReportDefinitions/MaxPowerConsumptionDay\nType: #MetricReportDefinition.v1_4_6.MetricReportDefinition"
            },
            {
              "name": "MaxPowerConsumptionHour",
              "item": [
                {
                  "name": "GET Maximum Power Consumption for 1 hour with sensing interval of 5 minutes.",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReportDefinitions/MaxPowerConsumptionHour",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                },
                {
                  "name": "PATCH Maximum Power Consumption for 1 hour with sensing interval of 5 minutes.",
                  "request": {
                    "method": "PATCH",
                    "header": [
                      {
                        "key": "Content-Type",
                        "value": "application/json",
                        "type": "text"
                      },
                      {
                        "key": "If-Match",
                        "value": "\"1ab860b5\"",
                        "type": "text"
                      }
                    ],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReportDefinitions/MaxPowerConsumptionHour",
                    "description": "Evidence: allow-header",
                    "body": {
                      "mode": "raw",
                      "raw": "{}",
                      "options": {
                        "raw": {
                          "language": "json"
                        }
                      }
                    }
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                          "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                          "    pm.execution.skipRequest();",
                          "  } else {",
                          "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                          "  }",
                          "}",
                          "",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricReportDefinitions/MaxPowerConsumptionHour\nType: #MetricReportDefinition.v1_4_6.MetricReportDefinition"
            },
            {
              "name": "MaxPowerConsumptionWeek",
              "item": [
                {
                  "name": "GET Maximum Power Consumption for 1 week with sensing interval of 12 hours.",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReportDefinitions/MaxPowerConsumptionWeek",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                },
                {
                  "name": "PATCH Maximum Power Consumption for 1 week with sensing interval of 12 hours.",
                  "request": {
                    "method": "PATCH",
                    "header": [
                      {
                        "key": "Content-Type",
                        "value": "application/json",
                        "type": "text"
                      },
                      {
                        "key": "If-Match",
                        "value": "\"9dabd276\"",
                        "type": "text"
                      }
                    ],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReportDefinitions/MaxPowerConsumptionWeek",
                    "description": "Evidence: allow-header",
                    "body": {
                      "mode": "raw",
                      "raw": "{}",
                      "options": {
                        "raw": {
                          "language": "json"
                        }
                      }
                    }
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                          "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                          "    pm.execution.skipRequest();",
                          "  } else {",
                          "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                          "  }",
                          "}",
                          "",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricReportDefinitions/MaxPowerConsumptionWeek\nType: #MetricReportDefinition.v1_4_6.MetricReportDefinition"
            },
            {
              "name": "MinPowerConsumptionDay",
              "item": [
                {
                  "name": "GET Minimum Power Consumption for 1 day with sensing interval of 1 hour.",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReportDefinitions/MinPowerConsumptionDay",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                },
                {
                  "name": "PATCH Minimum Power Consumption for 1 day with sensing interval of 1 hour.",
                  "request": {
                    "method": "PATCH",
                    "header": [
                      {
                        "key": "Content-Type",
                        "value": "application/json",
                        "type": "text"
                      },
                      {
                        "key": "If-Match",
                        "value": "\"8f042100\"",
                        "type": "text"
                      }
                    ],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReportDefinitions/MinPowerConsumptionDay",
                    "description": "Evidence: allow-header",
                    "body": {
                      "mode": "raw",
                      "raw": "{}",
                      "options": {
                        "raw": {
                          "language": "json"
                        }
                      }
                    }
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                          "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                          "    pm.execution.skipRequest();",
                          "  } else {",
                          "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                          "  }",
                          "}",
                          "",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricReportDefinitions/MinPowerConsumptionDay\nType: #MetricReportDefinition.v1_4_6.MetricReportDefinition"
            },
            {
              "name": "MinPowerConsumptionHour",
              "item": [
                {
                  "name": "GET Minimum Power Consumption for 1 hour with sensing interval of 5 minutes.",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReportDefinitions/MinPowerConsumptionHour",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                },
                {
                  "name": "PATCH Minimum Power Consumption for 1 hour with sensing interval of 5 minutes.",
                  "request": {
                    "method": "PATCH",
                    "header": [
                      {
                        "key": "Content-Type",
                        "value": "application/json",
                        "type": "text"
                      },
                      {
                        "key": "If-Match",
                        "value": "\"c251f35f\"",
                        "type": "text"
                      }
                    ],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReportDefinitions/MinPowerConsumptionHour",
                    "description": "Evidence: allow-header",
                    "body": {
                      "mode": "raw",
                      "raw": "{}",
                      "options": {
                        "raw": {
                          "language": "json"
                        }
                      }
                    }
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                          "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                          "    pm.execution.skipRequest();",
                          "  } else {",
                          "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                          "  }",
                          "}",
                          "",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricReportDefinitions/MinPowerConsumptionHour\nType: #MetricReportDefinition.v1_4_6.MetricReportDefinition"
            },
            {
              "name": "MinPowerConsumptionWeek",
              "item": [
                {
                  "name": "GET Minimum Power Consumption for 1 week with sensing interval of 12 hours.",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReportDefinitions/MinPowerConsumptionWeek",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                },
                {
                  "name": "PATCH Minimum Power Consumption for 1 week with sensing interval of 12 hours.",
                  "request": {
                    "method": "PATCH",
                    "header": [
                      {
                        "key": "Content-Type",
                        "value": "application/json",
                        "type": "text"
                      },
                      {
                        "key": "If-Match",
                        "value": "\"0323c5bb\"",
                        "type": "text"
                      }
                    ],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReportDefinitions/MinPowerConsumptionWeek",
                    "description": "Evidence: allow-header",
                    "body": {
                      "mode": "raw",
                      "raw": "{}",
                      "options": {
                        "raw": {
                          "language": "json"
                        }
                      }
                    }
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                          "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                          "    pm.execution.skipRequest();",
                          "  } else {",
                          "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                          "  }",
                          "}",
                          "",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricReportDefinitions/MinPowerConsumptionWeek\nType: #MetricReportDefinition.v1_4_6.MetricReportDefinition"
            },
            {
              "name": "GET Metric Report Definitions",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReportDefinitions",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/TelemetryService/MetricReportDefinitions\nType: #MetricReportDefinitionCollection.MetricReportDefinitionCollection"
        },
        {
          "name": "MetricReports",
          "item": [
            {
              "name": "AvgPowerConsumptionDay",
              "item": [
                {
                  "name": "GET Average Power Consumption for 1 day with sensing interval of 1 hour.",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReports/AvgPowerConsumptionDay",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricReports/AvgPowerConsumptionDay\nType: #MetricReport.v1_5_2.MetricReport"
            },
            {
              "name": "AvgPowerConsumptionHour",
              "item": [
                {
                  "name": "GET Average Power Consumption for 1 hour with sensing interval of 5 minutes.",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReports/AvgPowerConsumptionHour",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricReports/AvgPowerConsumptionHour\nType: #MetricReport.v1_5_2.MetricReport"
            },
            {
              "name": "AvgPowerConsumptionWeek",
              "item": [
                {
                  "name": "GET Average Power Consumption for 1 week with sensing interval of 12 hours.",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReports/AvgPowerConsumptionWeek",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricReports/AvgPowerConsumptionWeek\nType: #MetricReport.v1_5_2.MetricReport"
            },
            {
              "name": "BroadcomEvent",
              "item": [
                {
                  "name": "GET MetricReport of BroadcomEvent - auto",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReports/BroadcomEvent",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricReports/BroadcomEvent\nType: #MetricReport.v1_5_2.MetricReport"
            },
            {
              "name": "CPUMemMetrics",
              "item": [
                {
                  "name": "GET CPUMemMetrics",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReports/CPUMemMetrics",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricReports/CPUMemMetrics\nType: #MetricReport.v1_5_2.MetricReport"
            },
            {
              "name": "CUPSMetrics",
              "item": [
                {
                  "name": "GET CUPSMetrics",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReports/CUPSMetrics",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricReports/CUPSMetrics\nType: #MetricReport.v1_5_2.MetricReport"
            },
            {
              "name": "MaxPowerConsumptionDay",
              "item": [
                {
                  "name": "GET Maximum Power Consumption for 1 day with sensing interval of 1 hour.",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReports/MaxPowerConsumptionDay",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricReports/MaxPowerConsumptionDay\nType: #MetricReport.v1_5_2.MetricReport"
            },
            {
              "name": "MaxPowerConsumptionHour",
              "item": [
                {
                  "name": "GET Maximum Power Consumption for 1 hour with sensing interval of 5 minutes.",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReports/MaxPowerConsumptionHour",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricReports/MaxPowerConsumptionHour\nType: #MetricReport.v1_5_2.MetricReport"
            },
            {
              "name": "MaxPowerConsumptionWeek",
              "item": [
                {
                  "name": "GET Maximum Power Consumption for 1 week with sensing interval of 12 hours.",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReports/MaxPowerConsumptionWeek",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricReports/MaxPowerConsumptionWeek\nType: #MetricReport.v1_5_2.MetricReport"
            },
            {
              "name": "MinPowerConsumptionDay",
              "item": [
                {
                  "name": "GET Minimum Power Consumption for 1 day with sensing interval of 1 hour.",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReports/MinPowerConsumptionDay",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricReports/MinPowerConsumptionDay\nType: #MetricReport.v1_5_2.MetricReport"
            },
            {
              "name": "MinPowerConsumptionHour",
              "item": [
                {
                  "name": "GET Minimum Power Consumption for 1 hour with sensing interval of 5 minutes.",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReports/MinPowerConsumptionHour",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricReports/MinPowerConsumptionHour\nType: #MetricReport.v1_5_2.MetricReport"
            },
            {
              "name": "MinPowerConsumptionWeek",
              "item": [
                {
                  "name": "GET Minimum Power Consumption for 1 week with sensing interval of 12 hours.",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReports/MinPowerConsumptionWeek",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/TelemetryService/MetricReports/MinPowerConsumptionWeek\nType: #MetricReport.v1_5_2.MetricReport"
            },
            {
              "name": "GET Metric Reports",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService/MetricReports",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/TelemetryService/MetricReports\nType: #MetricReportCollection.MetricReportCollection"
        },
        {
          "name": "GET Telemetry Service",
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/TelemetryService",
            "description": "Evidence: allow-header"
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const urlTools = require('url');",
                  "function parseURL(raw) {",
                  "  const value = urlTools.parse(raw);",
                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                  "  return value;",
                  "}",
                  "function targetBase() {",
                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                  "}",
                  "function authorizeSession(raw, baseURL) {",
                  "  const base = targetBase();",
                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                  "  let expanded = raw;",
                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                  "  const decoded = decodeURIComponent(session.pathname || '');",
                  "  if (session.origin !== base.origin || session.auth ||",
                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                  "  return session;",
                  "}",
                  "",
                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                  "pm.request.headers.remove('Authorization');",
                  "pm.request.headers.remove('X-Auth-Token');",
                  "if (mode === 'basic') {",
                  "  const username = pm.environment.get('USERNAME') || '';",
                  "  const password = pm.environment.get('PASSWORD') || '';",
                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                  "} else {",
                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                  "    pm.execution.skipRequest();",
                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                  "  }",
                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Response status is below 400', function () {",
                  "  pm.expect(pm.response.code).to.be.below(400);",
                  "});",
                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                  "if (pm.response.code !== 204) {",
                  "  pm.test('Response is JSON', function () {",
                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                  "  });",
                  "}",
                  ""
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        }
      ],
      "description": "URI: /redfish/v1/TelemetryService\nType: #TelemetryService.v1_4_0.TelemetryService"
    },
    {
      "name": "UpdateService",
      "item": [
        {
          "name": "FirmwareInventory",
          "item": [
            {
              "name": "BIOS",
              "item": [
                {
                  "name": "GET BIOS",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/UpdateService/FirmwareInventory/BIOS",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                },
                {
                  "name": "PATCH BIOS",
                  "request": {
                    "method": "PATCH",
                    "header": [
                      {
                        "key": "Content-Type",
                        "value": "application/json",
                        "type": "text"
                      },
                      {
                        "key": "If-Match",
                        "value": "\"d9a9fbbf\"",
                        "type": "text"
                      }
                    ],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/UpdateService/FirmwareInventory/BIOS",
                    "description": "Evidence: allow-header",
                    "body": {
                      "mode": "raw",
                      "raw": "{}",
                      "options": {
                        "raw": {
                          "language": "json"
                        }
                      }
                    }
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                          "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                          "    pm.execution.skipRequest();",
                          "  } else {",
                          "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                          "  }",
                          "}",
                          "",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/UpdateService/FirmwareInventory/BIOS\nType: #SoftwareInventory.v1_12_0.SoftwareInventory"
            },
            {
              "name": "BMC",
              "item": [
                {
                  "name": "GET BMC",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/UpdateService/FirmwareInventory/BMC",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                },
                {
                  "name": "PATCH BMC",
                  "request": {
                    "method": "PATCH",
                    "header": [
                      {
                        "key": "Content-Type",
                        "value": "application/json",
                        "type": "text"
                      },
                      {
                        "key": "If-Match",
                        "value": "\"7f4baf87\"",
                        "type": "text"
                      }
                    ],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/UpdateService/FirmwareInventory/BMC",
                    "description": "Evidence: allow-header",
                    "body": {
                      "mode": "raw",
                      "raw": "{}",
                      "options": {
                        "raw": {
                          "language": "json"
                        }
                      }
                    }
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                          "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                          "    pm.execution.skipRequest();",
                          "  } else {",
                          "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                          "  }",
                          "}",
                          "",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/UpdateService/FirmwareInventory/BMC\nType: #SoftwareInventory.v1_12_0.SoftwareInventory"
            },
            {
              "name": "Backup_BIOS",
              "item": [
                {
                  "name": "GET BIOS Backup",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/UpdateService/FirmwareInventory/Backup_BIOS",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                },
                {
                  "name": "PATCH BIOS Backup",
                  "request": {
                    "method": "PATCH",
                    "header": [
                      {
                        "key": "Content-Type",
                        "value": "application/json",
                        "type": "text"
                      },
                      {
                        "key": "If-Match",
                        "value": "\"941e80b9\"",
                        "type": "text"
                      }
                    ],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/UpdateService/FirmwareInventory/Backup_BIOS",
                    "description": "Evidence: allow-header",
                    "body": {
                      "mode": "raw",
                      "raw": "{}",
                      "options": {
                        "raw": {
                          "language": "json"
                        }
                      }
                    }
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                          "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                          "    pm.execution.skipRequest();",
                          "  } else {",
                          "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                          "  }",
                          "}",
                          "",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/UpdateService/FirmwareInventory/Backup_BIOS\nType: #SoftwareInventory.v1_12_0.SoftwareInventory"
            },
            {
              "name": "Backup_BMC",
              "item": [
                {
                  "name": "GET BMC Backup",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/UpdateService/FirmwareInventory/Backup_BMC",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                },
                {
                  "name": "PATCH BMC Backup",
                  "request": {
                    "method": "PATCH",
                    "header": [
                      {
                        "key": "Content-Type",
                        "value": "application/json",
                        "type": "text"
                      },
                      {
                        "key": "If-Match",
                        "value": "\"663f0297\"",
                        "type": "text"
                      }
                    ],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/UpdateService/FirmwareInventory/Backup_BMC",
                    "description": "Evidence: allow-header",
                    "body": {
                      "mode": "raw",
                      "raw": "{}",
                      "options": {
                        "raw": {
                          "language": "json"
                        }
                      }
                    }
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                          "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                          "    pm.execution.skipRequest();",
                          "  } else {",
                          "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                          "  }",
                          "}",
                          "",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/UpdateService/FirmwareInventory/Backup_BMC\nType: #SoftwareInventory.v1_12_0.SoftwareInventory"
            },
            {
              "name": "Capsule_BIOS",
              "item": [
                {
                  "name": "GET Capsule BIOS",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/UpdateService/FirmwareInventory/Capsule_BIOS",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                },
                {
                  "name": "PATCH Capsule BIOS",
                  "request": {
                    "method": "PATCH",
                    "header": [
                      {
                        "key": "Content-Type",
                        "value": "application/json",
                        "type": "text"
                      },
                      {
                        "key": "If-Match",
                        "value": "\"dc255c2b\"",
                        "type": "text"
                      }
                    ],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/UpdateService/FirmwareInventory/Capsule_BIOS",
                    "description": "Evidence: allow-header",
                    "body": {
                      "mode": "raw",
                      "raw": "{}",
                      "options": {
                        "raw": {
                          "language": "json"
                        }
                      }
                    }
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                          "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                          "    pm.execution.skipRequest();",
                          "  } else {",
                          "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                          "  }",
                          "}",
                          "",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/UpdateService/FirmwareInventory/Capsule_BIOS\nType: #SoftwareInventory.v1_12_0.SoftwareInventory"
            },
            {
              "name": "Golden_BIOS",
              "item": [
                {
                  "name": "GET BIOS Golden",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/UpdateService/FirmwareInventory/Golden_BIOS",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                },
                {
                  "name": "PATCH BIOS Golden",
                  "request": {
                    "method": "PATCH",
                    "header": [
                      {
                        "key": "Content-Type",
                        "value": "application/json",
                        "type": "text"
                      },
                      {
                        "key": "If-Match",
                        "value": "\"e22019cb\"",
                        "type": "text"
                      }
                    ],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/UpdateService/FirmwareInventory/Golden_BIOS",
                    "description": "Evidence: allow-header",
                    "body": {
                      "mode": "raw",
                      "raw": "{}",
                      "options": {
                        "raw": {
                          "language": "json"
                        }
                      }
                    }
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                          "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                          "    pm.execution.skipRequest();",
                          "  } else {",
                          "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                          "  }",
                          "}",
                          "",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/UpdateService/FirmwareInventory/Golden_BIOS\nType: #SoftwareInventory.v1_12_0.SoftwareInventory"
            },
            {
              "name": "Golden_BMC",
              "item": [
                {
                  "name": "GET BMC Golden",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/UpdateService/FirmwareInventory/Golden_BMC",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                },
                {
                  "name": "PATCH BMC Golden",
                  "request": {
                    "method": "PATCH",
                    "header": [
                      {
                        "key": "Content-Type",
                        "value": "application/json",
                        "type": "text"
                      },
                      {
                        "key": "If-Match",
                        "value": "\"2523ec10\"",
                        "type": "text"
                      }
                    ],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/UpdateService/FirmwareInventory/Golden_BMC",
                    "description": "Evidence: allow-header",
                    "body": {
                      "mode": "raw",
                      "raw": "{}",
                      "options": {
                        "raw": {
                          "language": "json"
                        }
                      }
                    }
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                          "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                          "    pm.execution.skipRequest();",
                          "  } else {",
                          "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                          "  }",
                          "}",
                          "",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/UpdateService/FirmwareInventory/Golden_BMC\nType: #SoftwareInventory.v1_12_0.SoftwareInventory"
            },
            {
              "name": "Golden_Motherboard_CPLD_1",
              "item": [
                {
                  "name": "GET Motherboard CPLD 1 Golden",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/UpdateService/FirmwareInventory/Golden_Motherboard_CPLD_1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                },
                {
                  "name": "PATCH Motherboard CPLD 1 Golden",
                  "request": {
                    "method": "PATCH",
                    "header": [
                      {
                        "key": "Content-Type",
                        "value": "application/json",
                        "type": "text"
                      },
                      {
                        "key": "If-Match",
                        "value": "\"e37537c3\"",
                        "type": "text"
                      }
                    ],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/UpdateService/FirmwareInventory/Golden_Motherboard_CPLD_1",
                    "description": "Evidence: allow-header",
                    "body": {
                      "mode": "raw",
                      "raw": "{}",
                      "options": {
                        "raw": {
                          "language": "json"
                        }
                      }
                    }
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                          "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                          "    pm.execution.skipRequest();",
                          "  } else {",
                          "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                          "  }",
                          "}",
                          "",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/UpdateService/FirmwareInventory/Golden_Motherboard_CPLD_1\nType: #SoftwareInventory.v1_12_0.SoftwareInventory"
            },
            {
              "name": "Motherboard_CPLD_1",
              "item": [
                {
                  "name": "GET Motherboard CPLD 1",
                  "request": {
                    "method": "GET",
                    "header": [],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/UpdateService/FirmwareInventory/Motherboard_CPLD_1",
                    "description": "Evidence: allow-header"
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    },
                    {
                      "listen": "test",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "pm.test('Response status is below 400', function () {",
                          "  pm.expect(pm.response.code).to.be.below(400);",
                          "});",
                          "const contentType = pm.response.headers.get('Content-Type') || '';",
                          "if (pm.response.code !== 204) {",
                          "  pm.test('Response is JSON', function () {",
                          "    pm.expect(contentType.toLowerCase()).to.include('json');",
                          "  });",
                          "}",
                          ""
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                },
                {
                  "name": "PATCH Motherboard CPLD 1",
                  "request": {
                    "method": "PATCH",
                    "header": [
                      {
                        "key": "Content-Type",
                        "value": "application/json",
                        "type": "text"
                      },
                      {
                        "key": "If-Match",
                        "value": "\"13c300c6\"",
                        "type": "text"
                      }
                    ],
                    "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/UpdateService/FirmwareInventory/Motherboard_CPLD_1",
                    "description": "Evidence: allow-header",
                    "body": {
                      "mode": "raw",
                      "raw": "{}",
                      "options": {
                        "raw": {
                          "language": "json"
                        }
                      }
                    }
                  },
                  "event": [
                    {
                      "listen": "prerequest",
                      "script": {
                        "type": "text/javascript",
                        "exec": [
                          "const urlTools = require('url');",
                          "function parseURL(raw) {",
                          "  const value = urlTools.parse(raw);",
                          "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                          "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                          "  return value;",
                          "}",
                          "function targetBase() {",
                          "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                          "  let host = String(pm.environment.get('BMC_HOST') || '');",
                          "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                          "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                          "}",
                          "function authorizeSession(raw, baseURL) {",
                          "  const base = targetBase();",
                          "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                          "  let expanded = raw;",
                          "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                          "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                          "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                          "  const decoded = decodeURIComponent(session.pathname || '');",
                          "  if (session.origin !== base.origin || session.auth ||",
                          "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                          "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                          "  return session;",
                          "}",
                          "",
                          "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                          "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                          "    pm.execution.skipRequest();",
                          "  } else {",
                          "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                          "  }",
                          "}",
                          "",
                          "",
                          "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                          "pm.request.headers.remove('Authorization');",
                          "pm.request.headers.remove('X-Auth-Token');",
                          "if (mode === 'basic') {",
                          "  const username = pm.environment.get('USERNAME') || '';",
                          "  const password = pm.environment.get('PASSWORD') || '';",
                          "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                          "} else {",
                          "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                          "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                          "    pm.execution.skipRequest();",
                          "    throw new Error('Log in to the current BMC before sending Session requests');",
                          "  }",
                          "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                          "}"
                        ]
                      }
                    }
                  ],
                  "protocolProfileBehavior": {
                    "followRedirects": false
                  }
                }
              ],
              "description": "URI: /redfish/v1/UpdateService/FirmwareInventory/Motherboard_CPLD_1\nType: #SoftwareInventory.v1_12_0.SoftwareInventory"
            },
            {
              "name": "GET Software Inventory Collection",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/UpdateService/FirmwareInventory",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/UpdateService/FirmwareInventory\nType: #SoftwareInventoryCollection.SoftwareInventoryCollection"
        },
        {
          "name": "Oem",
          "item": [
            {
              "name": "Supermicro",
              "item": [
                {
                  "name": "BMCConfig",
                  "item": [
                    {
                      "name": "Actions",
                      "item": [
                        {
                          "name": "POST SupermicroBMCConfig.Download",
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                  "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                  "    pm.execution.skipRequest();",
                                  "  } else {",
                                  "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                  "  }",
                                  "}",
                                  "",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            }
                          ],
                          "request": {
                            "method": "POST",
                            "header": [
                              {
                                "key": "Content-Type",
                                "value": "application/json",
                                "type": "text"
                              }
                            ],
                            "body": {
                              "mode": "raw",
                              "raw": "{}",
                              "options": {
                                "raw": {
                                  "language": "json"
                                }
                              }
                            },
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/UpdateService/Oem/Supermicro/BMCConfig/Actions/SupermicroBMCConfig.Download",
                            "description": "Redfish Action. Review the payload before explicitly enabling mutations."
                          },
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        },
                        {
                          "name": "POST SupermicroBMCConfig.Upload",
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                  "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                  "    pm.execution.skipRequest();",
                                  "  } else {",
                                  "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                  "  }",
                                  "}",
                                  "",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            }
                          ],
                          "request": {
                            "method": "POST",
                            "header": [
                              {
                                "key": "Content-Type",
                                "value": "application/json",
                                "type": "text"
                              }
                            ],
                            "body": {
                              "mode": "raw",
                              "raw": "{\n  \"UploadKeyWords\": \"\"\n}",
                              "options": {
                                "raw": {
                                  "language": "json"
                                }
                              }
                            },
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/UpdateService/Oem/Supermicro/BMCConfig/Actions/SupermicroBMCConfig.Upload",
                            "description": "Redfish Action. Review the payload before explicitly enabling mutations."
                          },
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ]
                    },
                    {
                      "name": "GET Supermicro BMC Configuration File",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/UpdateService/Oem/Supermicro/BMCConfig",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/UpdateService/Oem/Supermicro/BMCConfig\nType: #SupermicroBMCConfig.v1_0_0.SupermicroBMCConfig"
                },
                {
                  "name": "SSLCert",
                  "item": [
                    {
                      "name": "Actions",
                      "item": [
                        {
                          "name": "POST SmcSSLCert.Upload",
                          "event": [
                            {
                              "listen": "prerequest",
                              "script": {
                                "type": "text/javascript",
                                "exec": [
                                  "const urlTools = require('url');",
                                  "function parseURL(raw) {",
                                  "  const value = urlTools.parse(raw);",
                                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                                  "  return value;",
                                  "}",
                                  "function targetBase() {",
                                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                                  "}",
                                  "function authorizeSession(raw, baseURL) {",
                                  "  const base = targetBase();",
                                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                                  "  let expanded = raw;",
                                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                                  "  const decoded = decodeURIComponent(session.pathname || '');",
                                  "  if (session.origin !== base.origin || session.auth ||",
                                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                                  "  return session;",
                                  "}",
                                  "",
                                  "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                                  "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                                  "    pm.execution.skipRequest();",
                                  "  } else {",
                                  "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                                  "  }",
                                  "}",
                                  "",
                                  "",
                                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                                  "pm.request.headers.remove('Authorization');",
                                  "pm.request.headers.remove('X-Auth-Token');",
                                  "if (mode === 'basic') {",
                                  "  const username = pm.environment.get('USERNAME') || '';",
                                  "  const password = pm.environment.get('PASSWORD') || '';",
                                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                                  "} else {",
                                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                                  "    pm.execution.skipRequest();",
                                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                                  "  }",
                                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                                  "}"
                                ]
                              }
                            }
                          ],
                          "request": {
                            "method": "POST",
                            "header": [
                              {
                                "key": "Content-Type",
                                "value": "application/json",
                                "type": "text"
                              }
                            ],
                            "body": {
                              "mode": "raw",
                              "raw": "{}",
                              "options": {
                                "raw": {
                                  "language": "json"
                                }
                              }
                            },
                            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/UpdateService/Oem/Supermicro/SSLCert/Actions/SmcSSLCert.Upload",
                            "description": "Redfish Action. Review the payload before explicitly enabling mutations."
                          },
                          "protocolProfileBehavior": {
                            "followRedirects": false
                          }
                        }
                      ]
                    },
                    {
                      "name": "GET SSLCert",
                      "request": {
                        "method": "GET",
                        "header": [],
                        "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/UpdateService/Oem/Supermicro/SSLCert",
                        "description": "Evidence: allow-header"
                      },
                      "event": [
                        {
                          "listen": "prerequest",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "const urlTools = require('url');",
                              "function parseURL(raw) {",
                              "  const value = urlTools.parse(raw);",
                              "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                              "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                              "  return value;",
                              "}",
                              "function targetBase() {",
                              "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                              "  let host = String(pm.environment.get('BMC_HOST') || '');",
                              "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                              "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                              "}",
                              "function authorizeSession(raw, baseURL) {",
                              "  const base = targetBase();",
                              "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                              "  let expanded = raw;",
                              "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                              "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                              "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                              "  const decoded = decodeURIComponent(session.pathname || '');",
                              "  if (session.origin !== base.origin || session.auth ||",
                              "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                              "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                              "  return session;",
                              "}",
                              "",
                              "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                              "pm.request.headers.remove('Authorization');",
                              "pm.request.headers.remove('X-Auth-Token');",
                              "if (mode === 'basic') {",
                              "  const username = pm.environment.get('USERNAME') || '';",
                              "  const password = pm.environment.get('PASSWORD') || '';",
                              "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                              "} else {",
                              "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                              "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                              "    pm.execution.skipRequest();",
                              "    throw new Error('Log in to the current BMC before sending Session requests');",
                              "  }",
                              "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                              "}"
                            ]
                          }
                        },
                        {
                          "listen": "test",
                          "script": {
                            "type": "text/javascript",
                            "exec": [
                              "pm.test('Response status is below 400', function () {",
                              "  pm.expect(pm.response.code).to.be.below(400);",
                              "});",
                              "const contentType = pm.response.headers.get('Content-Type') || '';",
                              "if (pm.response.code !== 204) {",
                              "  pm.test('Response is JSON', function () {",
                              "    pm.expect(contentType.toLowerCase()).to.include('json');",
                              "  });",
                              "}",
                              ""
                            ]
                          }
                        }
                      ],
                      "protocolProfileBehavior": {
                        "followRedirects": false
                      }
                    }
                  ],
                  "description": "URI: /redfish/v1/UpdateService/Oem/Supermicro/SSLCert\nType: #SSLCert.v1_0_0.SSLCert"
                }
              ]
            }
          ]
        },
        {
          "name": "UpdateServiceCapabilities",
          "item": [
            {
              "name": "GET Update Service Capabilities",
              "request": {
                "method": "GET",
                "header": [],
                "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/UpdateService/UpdateServiceCapabilities",
                "description": "Evidence: allow-header"
              },
              "event": [
                {
                  "listen": "prerequest",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const urlTools = require('url');",
                      "function parseURL(raw) {",
                      "  const value = urlTools.parse(raw);",
                      "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                      "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                      "  return value;",
                      "}",
                      "function targetBase() {",
                      "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                      "  let host = String(pm.environment.get('BMC_HOST') || '');",
                      "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                      "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                      "}",
                      "function authorizeSession(raw, baseURL) {",
                      "  const base = targetBase();",
                      "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                      "  let expanded = raw;",
                      "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                      "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                      "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                      "  const decoded = decodeURIComponent(session.pathname || '');",
                      "  if (session.origin !== base.origin || session.auth ||",
                      "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                      "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                      "  return session;",
                      "}",
                      "",
                      "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                      "pm.request.headers.remove('Authorization');",
                      "pm.request.headers.remove('X-Auth-Token');",
                      "if (mode === 'basic') {",
                      "  const username = pm.environment.get('USERNAME') || '';",
                      "  const password = pm.environment.get('PASSWORD') || '';",
                      "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                      "} else {",
                      "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                      "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                      "    pm.execution.skipRequest();",
                      "    throw new Error('Log in to the current BMC before sending Session requests');",
                      "  }",
                      "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                      "}"
                    ]
                  }
                },
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Response status is below 400', function () {",
                      "  pm.expect(pm.response.code).to.be.below(400);",
                      "});",
                      "const contentType = pm.response.headers.get('Content-Type') || '';",
                      "if (pm.response.code !== 204) {",
                      "  pm.test('Response is JSON', function () {",
                      "    pm.expect(contentType.toLowerCase()).to.include('json');",
                      "  });",
                      "}",
                      ""
                    ]
                  }
                }
              ],
              "protocolProfileBehavior": {
                "followRedirects": false
              }
            }
          ],
          "description": "URI: /redfish/v1/UpdateService/UpdateServiceCapabilities\nType: #UpdateServiceCapabilities.v1_0_0.UpdateServiceCapabilities"
        },
        {
          "name": "GET Update Service",
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/UpdateService",
            "description": "Evidence: allow-header"
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const urlTools = require('url');",
                  "function parseURL(raw) {",
                  "  const value = urlTools.parse(raw);",
                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                  "  return value;",
                  "}",
                  "function targetBase() {",
                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                  "}",
                  "function authorizeSession(raw, baseURL) {",
                  "  const base = targetBase();",
                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                  "  let expanded = raw;",
                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                  "  const decoded = decodeURIComponent(session.pathname || '');",
                  "  if (session.origin !== base.origin || session.auth ||",
                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                  "  return session;",
                  "}",
                  "",
                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                  "pm.request.headers.remove('Authorization');",
                  "pm.request.headers.remove('X-Auth-Token');",
                  "if (mode === 'basic') {",
                  "  const username = pm.environment.get('USERNAME') || '';",
                  "  const password = pm.environment.get('PASSWORD') || '';",
                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                  "} else {",
                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                  "    pm.execution.skipRequest();",
                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                  "  }",
                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Response status is below 400', function () {",
                  "  pm.expect(pm.response.code).to.be.below(400);",
                  "});",
                  "const contentType = pm.response.headers.get('Content-Type') || '';",
                  "if (pm.response.code !== 204) {",
                  "  pm.test('Response is JSON', function () {",
                  "    pm.expect(contentType.toLowerCase()).to.include('json');",
                  "  });",
                  "}",
                  ""
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "PATCH Update Service",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "If-Match",
                "value": "\"64c429fb\"",
                "type": "text"
              }
            ],
            "url": "{{BMC_PROTOCOL}}://{{BMC_HOST}}:{{BMC_PORT}}/redfish/v1/UpdateService",
            "description": "Evidence: allow-header",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const urlTools = require('url');",
                  "function parseURL(raw) {",
                  "  const value = urlTools.parse(raw);",
                  "  const port = value.port || (value.protocol === 'https:' ? '443' : '80');",
                  "  value.origin = `${value.protocol}//${String(value.hostname).toLowerCase()}:${port}`;",
                  "  return value;",
                  "}",
                  "function targetBase() {",
                  "  const protocol = String(pm.environment.get('BMC_PROTOCOL') || 'https');",
                  "  let host = String(pm.environment.get('BMC_HOST') || '');",
                  "  if (host.includes(':') && !host.startsWith('[')) host = `[${host}]`;",
                  "  return parseURL(`${protocol}://${host}:${pm.environment.get('BMC_PORT') || '443'}`);",
                  "}",
                  "function authorizeSession(raw, baseURL) {",
                  "  const base = targetBase();",
                  "  if (typeof raw !== 'string') throw new Error('Unsafe Session URL');",
                  "  let expanded = raw;",
                  "  for (let i = 0; i < 4; i++) expanded = decodeURIComponent(expanded);",
                  "  if (expanded.includes('..') || expanded.includes('\\\\') || /[\\x00-\\x1f]/.test(expanded)) throw new Error('Unsafe Session URL');",
                  "  const session = parseURL(urlTools.resolve(baseURL || base.href, raw));",
                  "  const decoded = decodeURIComponent(session.pathname || '');",
                  "  if (session.origin !== base.origin || session.auth ||",
                  "      !session.pathname.startsWith('/redfish/') || decoded.includes('..') || decoded.includes('\\\\') ||",
                  "      session.hash || session.search) throw new Error('Session URL is outside the current BMC boundary');",
                  "  return session;",
                  "}",
                  "",
                  "if ((pm.environment.get('DISABLE_MUTATIONS') || 'true').toLowerCase() !== 'false') {",
                  "  if (pm.execution && typeof pm.execution.skipRequest === 'function') {",
                  "    pm.execution.skipRequest();",
                  "  } else {",
                  "    throw new Error('Mutation blocked. Set DISABLE_MUTATIONS=false to enable this request explicitly.');",
                  "  }",
                  "}",
                  "",
                  "",
                  "const mode = (pm.environment.get('AUTH_MODE') || 'session').toLowerCase();",
                  "pm.request.headers.remove('Authorization');",
                  "pm.request.headers.remove('X-Auth-Token');",
                  "if (mode === 'basic') {",
                  "  const username = pm.environment.get('USERNAME') || '';",
                  "  const password = pm.environment.get('PASSWORD') || '';",
                  "  pm.request.headers.upsert({key: 'Authorization', value: `Basic ${btoa(`${username}:${password}`)}`});",
                  "} else {",
                  "  const token = pm.environment.get('X_AUTH_TOKEN') || '';",
                  "  if (!token || pm.environment.get('SESSION_ORIGIN') !== targetBase().origin) {",
                  "    pm.execution.skipRequest();",
                  "    throw new Error('Log in to the current BMC before sending Session requests');",
                  "  }",
                  "  pm.request.headers.upsert({key: 'X-Auth-Token', value: token});",
                  "}"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        }
      ],
      "description": "URI: /redfish/v1/UpdateService\nType: #UpdateService.v1_16_0.UpdateService"
    }
  ]
}
